# Setup Impact Registry

> **Đây là nơi duy nhất (single source of truth) theo dõi việc admin setup có theo kịp các tính năng mới hay không.**
>
> Mọi feature spec mới (trong `.kiro/specs/<feature>/`) khi hoàn thành PHẢI rà soát mục "Cách dùng registry" bên dưới và thêm dòng vào bảng nếu feature có yêu cầu khởi tạo. Quy tắc này được ép qua DoD chung tại `.kiro/steering/definition-of-done.md` (mục "Setup impact").

## Cách dùng registry

Khi một feature mới (hoặc thay đổi lớn) được merge, trả lời các câu hỏi sau. Nếu BẤT KỲ câu nào là "có" → thêm một dòng vào bảng Registry, tạo task trong `tasks.md` của spec này (Phase G trở đi), và cập nhật `requirements.md`/`design.md` nếu hành vi setup thay đổi:

1. Feature có bảng/row cần **seed mặc định** khi instance khởi tạo không? (vd: role library, default settings row)
2. Feature có **feature flag / settings key** mà operator cần biết tồn tại để bật không?
3. Feature có **policy/grant mặc định** mà nên là dữ liệu trong DB thay vì fallback hardcode không?
4. Feature có cần **bước UI mới** trong Setup Wizard (Studio) không?
5. Feature có cần **capability flag** mới trong `GET /api/v1/setup/capabilities` không?
6. Instance **đã setup từ trước** có cần migration/backfill để có cùng trạng thái với instance setup mới không?

Sau khi cập nhật registry và spec, cập nhật docs tương ứng:
- `docs/en/agent-setup/prompt.md` (nếu hành vi setup thay đổi)
- `docs/en/api/hono-api-spec.md` (nếu endpoint `/api/v1/setup/*` thay đổi)
- CHANGELOG mục upgrade steps (nếu cần backfill)

## Registry

Trạng thái: `pending` (chưa làm) · `in-progress` · `done` (setup + backfill + docs xong) · `n/a` (đã xem xét, không cần)

| # | Feature spec | Phiên bản | Yêu cầu với setup | Trạng thái | Task | Ghi chú |
|---|--------------|-----------|-------------------|------------|------|---------|
| 107 | (tests: bỏ assertion đo wall-clock, siết lại guard ReDoS) | v0.25.x | Đã rà soát: thuần test + test-setup. `cloudflare-adapters.test.ts` đổi từ ngưỡng tuyệt đối `<100ms` sang so tỉ lệ adversarial/benign cùng độ dài; `apps/studio/src/test/setup.ts` thêm `configure({ asyncUtilTimeout: 5_000 })`; `apps/docs/.../search.test.ts` nới ngưỡng thành smoke test. KHÔNG đụng source production, schema, hay dependency | n/a | #408 | Rà soát 2026-08-22. (1) KHÔNG seed. (2) KHÔNG settings key / env mới. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability mới. (6) KHÔNG migration/backfill. Điểm cần biết: (a) **test ReDoS cũ không hề bảo vệ được gì** — nó đo input kết thúc bằng slash, mà `/\/+$/` match greedy ở anchor không backtrack (đo được 0.4ms, NHANH HƠN linear scan), nên regex lỗi vẫn pass; backtracking chỉ bùng khi có ký tự KHÁC slash sau chuỗi slash. (b) Đã negative-test: tiêm lại regex vào `search.ts` → test mới fail với ratio 35.699x so ngưỡng 100x; phục hồi thì 132/132 pass. (c) Flakiness của Studio là budget thời gian ẩn qua `waitFor` (mặc định 1000ms), không phải component lỗi — 2 suite khác nhau fail dưới tải mà pass khi chạy riêng. Nâng lên 5s không làm yếu assertion vì component hỏng thật thì không bao giờ render, chỉ fail chậm hơn; `testTimeout` đã là 15s nên còn dư. (d) Turbo cache từng che lỗi này: `pnpm test` trả cache hit, chỉ `--force` mới lộ. Verified: song song `turbo run test --force` 3/3 lần pass (trước khi sửa 2/2 fail), tuần tự 11/11, studio 67 file/378 test, docs 22/144, runtime 12/132. Shell: n/a |
| 106 | (tooling: chạy repo guard ở pre-commit thay vì chỉ CI) | v0.25.x | Đã rà soát: thuần tooling — thêm script `check:all` gộp `version:check` + `registry:check` + `settings:check` + `drift:check` + `scripts:test`, gọi từ `.husky/pre-commit` TRƯỚC `pnpm test`, và `ci.yml` gọi cùng script đó thay vì 5 step riêng. KHÔNG đụng source, schema, hay dependency | n/a | #406 | Rà soát 2026-08-22. (1) KHÔNG seed. (2) KHÔNG settings key / env mới. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability mới. (6) KHÔNG migration/backfill. Điểm cần biết: (a) commit local chậm thêm ~1s vì 5 guard, đổi lại không mất một vòng CI cho lỗi cơ học — 2 lần trong một buổi (#383 trùng row registry, #405 override thiếu bản khai ở `pnpm-workspace.yaml`) đều thuộc loại này. (b) CI dùng CHUNG `check:all` là phần chống mục ruỗng: nếu CI giữ danh sách riêng thì guard mới thêm vào CI sẽ lại không có ở hook. (c) Đã negative-test: cố tình thêm override chỉ vào `package.json` → exit 1 báo "missing from pnpm-workspace.yaml"; cố tình nhân đôi row 105 → exit 1 báo "duplicate row numbers". Verified: `pnpm check:all` exit 0 (scripts:test 10/10), `ci.yml` parse YAML hợp lệ, 0 step guard lẻ còn sót. Shell: n/a |
| 105 | (dependency security: đóng 2 advisory dev-only `brace-expansion`) | v0.25.x | Đã rà soát: thuần `pnpm.overrides` + lockfile, **KHÔNG một dòng source nào đổi**. Thêm `brace-expansion@1` `^1.1.16` (GHSA-3jxr-9vmj-r5cp, high) và `brace-expansion@5` `^5.0.8` (GHSA-mh99-v99m-4gvg, high). Kèm doc justification + trigger gỡ ở `docs/{en,vi}/security/dependency-overrides.md`, thêm mục "Known-unfixable alerts" / "Advisory không vá được" cho 2 advisory không vá được | n/a | — | Rà soát 2026-08-05. (1) KHÔNG seed. (2) KHÔNG settings key / env mới. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) **KHÔNG migration/backfill** — chỉ đổi version resolve trong `pnpm-lock.yaml`. Điểm cần biết: (a) **cả 2 advisory đều dev-only** — `brace-expansion` KHÔNG xuất hiện trong cây `pnpm audit --prod`, nên gate CI đã xanh từ trước và PR này không sửa gate nào; giá trị là dọn alert Dependabot còn mở + ghi lại lý do. (b) Key theo major (cùng dạng `nanoid@3`/`nanoid@5` đã có) vì 1.x export chính hàm còn 5.x export namespace object ⇒ ép cả cây về 5.x làm `minimatch@3` (ESLint + plugin) throw `TypeError: m is not a function`. (c) Trôi tự nhiên đã kéo phần lớn cây lên `1.1.18`/`5.0.9`; bản sót là một copy `5.0.7` do `minimatch@10.2.5` giữ. (d) GHSA-mh99 (`<= 5.0.7`) vẫn khớp nhánh 1.x theo semver — ghi vào doc thay vì thêm `ignoreGhsas`. Shell: n/a |
| 104 | (v1 public-surface + marketplace publish authz) | v0.25.x | Đã rà soát: 2 fix backport từ nhánh `claude/v1-evaluation-570dsb` (PR #277), thuần routing + authz, KHÔNG đụng schema. (a) **Delivery API/pageview beacon ẩn danh bị chết**: `app.route('/api/v1/deliver', …)` và `/pageviews` đăng ký SAU `app.route('/api/v1', api)` trong `apps/cms/src/index.ts`; middleware `use('*')` của sub-app `api` flatten thành `/api/v1/*` và chạy trước handler ⇒ mọi request không kèm credential chết ở `withTenant`/`withAuth` (400/401) trước khi tới handler. Chuyển 2 mount lên trước, và thu hẹp middleware beacon về đúng leaf `/pageviews/:site_id/hit` để `/pageviews/stats` (đã auth) không chạy `withDb`/`withRateLimit` hai lần. (b) **`POST /api/v1/marketplace/publish` không có gate quyền**: route ghi catalog toàn cục (`siteId` null) mà mọi tenant đọc, nhưng thiếu `requireConfigurePermission` (các route write anh em đều có) và nhận thẳng `signature`/`publisherKeyId`/`bundleSha256` từ client ⇒ user đăng nhập bất kỳ có membership bất kỳ có thể ghi đè listing, bỏ qua moderation, giả huy hiệu verified. Nay gate bằng `extensions:configure`, verify chữ ký với bundle ĐÃ LƯU qua `ExtensionVerifierService`, server tự suy ra `isOfficial`/`verifiedAt`, và từ chối `409` nếu submission cộng đồng chưa `approved` | n/a | — | Rà soát 2026-07-25, ghi CHANGELOG 2026-08-04 (code đã ship trong v0.25.0, entry đặt dưới mục `[0.25.0] › Fixed`). (1) KHÔNG seed. (2) KHÔNG feature-flag/settings key mới. (3) KHÔNG policy/grant DB mới — `extensions:configure` là capability đã tồn tại, dùng lại đúng gate của `/submissions/review`. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) **KHÔNG migration/backfill** — thuần code; instance cũ nhận vá ngay khi lên v0.25.x. **Đổi hành vi cần lưu ý khi upgrade**: (i) `/marketplace/publish` giờ trả `403` cho caller thiếu `extensions:configure` — script/CI nào đang publish bằng token thường sẽ hỏng và PHẢI đổi sang principal có quyền moderator; (ii) publish sẽ `409` nếu row là submission cộng đồng chưa duyệt; (iii) `isOfficial`/`verifiedAt` không còn nhận từ client. **Vì sao lọt lưới**: cả 2 đều pre-existing và test cũ không bắt được — golden-path E2E thiếu chặng đọc ẩn danh, còn `marketplace-perf.test.ts` là mock-based nên mã hoá đúng contract KHÔNG-auth cũ; fix chuyển nó sang harness DB thật. Verified trên Postgres thật (`golden-path.e2e`, `marketplace-updates.integration`, `marketplace-perf`): chạy CÙNG bộ test lên `origin/main` chưa vá thì fail đúng 3 assertion — deliver ẩn danh `400` thay vì `200`, publish thiếu quyền `200` thay vì `403`, publish submission `pending` `200` thay vì `409`; trên nhánh vá pass hết. `pnpm typecheck` 16/16 sạch. Shell: n/a |
| 102 | (landing: intent composer thay mock "Set intent") | v0.25.x | Đã rà soát: card "Intent-driven, not click-driven" ở `apps/landing` trước đây là mock tĩnh — một `<span class="btn-solid">` đọc "Set intent" không có handler, cùng một thanh "82% converged" hardcode. Thay bằng `IntentComposerViz.tsx` (client component): visitor sửa câu, bấm nút thật, câu được compile thành payload `intent-rule.v1` rồi evaluate trên một sample 8 item và chạy các beat drift → goal → resolve. **Compile chạy hoàn toàn client-side** trong `src/lib/intent-compile.ts` — KHÔNG gọi API, KHÔNG env mới, KHÔNG đụng CMS/DB | n/a | — | Rà soát 2026-08-20. (1) KHÔNG seed — sample collection là hằng số trong bundle landing, không phải dữ liệu tenant. (2) KHÔNG settings key / feature flag. (3) KHÔNG policy/grant. (4) KHÔNG bước wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill. **Vì sao không gọi API thật**: endpoint tương ứng `POST /api/v1/agent/intents/compile` thuộc control plane (`CONTROL_PLANE_PATHS`, admin-only) nên visitor ẩn danh không có đường vào, và `apps/landing` build `output: 'export'` (không có server runtime). Do đó matcher là bản local tất định phủ đúng 6 rule shape của `ruleSchema` (`apps/cms/src/services/intent-service.ts`), và UI ghi rõ "compiled locally". Đồng bộ ngữ nghĩa với code đã ship: `field_constraint` đo **ký tự** (`value.length` trong `drift-service.ts`) nên câu "50–200 words" compile ra 300–1200 chars **kèm warning hiện trên UI** thay vì ngầm coi word là đơn vị; fix attribution lấy theo `RULE_ROLE_ROUTING` (`reconciler-service.ts`). Multi-tenant: n/a — landing là site marketing tĩnh, không có `siteId`, không cache/queue/realtime. Verified: `pnpm -F @lumibase/landing test` 51/51 (mới: `src/lib/intent-compile.test.ts`), typecheck + lint + `next build` (static export) pass, và kiểm hành vi thật bằng Chrome headless qua CDP: beat chạy đúng thứ tự (compiling → evaluate → incident → reconcile → converged), số liệu đổi theo rule (products preset 3/8 drift; câu "Every doc: en/vi parity" → 1/8), `prefers-reduced-motion` giữ ở beat `incident`, và nội dung không bị `FeatureCard` cắt ở 1440/780/390px |
| 103 | (dependabot consolidated upgrade: nanoid/js-yaml advisories + uuid 14, vite 8, workers-types 5, fast-check 4, lucide 1, dnd-kit 10, jest-dom 7, next 16.3) | v0.25.x | Đã rà soát: gộp toàn bộ PR dependabot đang mở thành **một** thay đổi nhất quán lockfile, thay vì merge tuần tự (mỗi lần merge làm hỏng lockfile của PR kế tiếp). Đóng 2 advisory high đang sống trên `main` mà gate `pnpm audit --prod` đã fail: `nanoid` <5.1.16 (GHSA-28wg-ghj8-5hjv) và `js-yaml` <4.3.1 (GHSA-5p4m-2wfm-xmqj) — cả hai bị **root `pnpm.overrides` ghim thấp**, nên bản vá phải nằm ở overrides chứ không ở manifest của package. Chạm source ở 5 file do breaking change của workers-types v5 + lucide v1, và 13 file test do fast-check v4. **KHÔNG** đổi seed/settings/wizard/capability/migration | n/a | — | Rà soát 2026-08-11. (1) KHÔNG seed. (2) KHÔNG settings key/feature-flag mới — thuần version bump; `pnpm.overrides` là build-time, không phải cấu hình runtime của instance. (3) KHÔNG policy/grant DB. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill — không đụng schema; instance đã setup nhận bản vá ngay khi lên version mới. **Vì sao ưu tiên nâng hơn sợ impact**: 2 advisory high là lỗ hổng đang mở trên nhánh chính, còn impact hoá ra hữu hạn và định vị được chính xác — xem 3 class dưới. **Class 1 — workers-types v5 khai báo `Buffer: any`** và là `types` entry DUY NHẤT của Worker build (`apps/cms/tsconfig.json`), nên typings Buffer của Node không có trong scope: `Buffer.isBuffer()` mất khả năng narrow và `buf.toString('utf-8'\|'base64')` báo "Expected 0 arguments". Sửa 3 call site sang narrow theo view shape / dùng Web-standard tương đương (`ArrayBuffer.isView`, `TextDecoder`, `btoa`) — hành vi runtime KHÔNG đổi. v5 còn nới global `ExecutionContext` thêm `tracing`/`abort` bắt buộc; `c.executionCtx` của Hono là shape hẹp hơn và giá trị chỉ chảy về `subApp.fetch()` (vốn cần đúng shape Hono), nên helper `optionalExecutionCtx` chuyển sang **infer** return type thay vì assert type global. **Class 2 — lucide-react v1 bỏ brand mark**, `Github` không còn tồn tại; chỉ dùng ở 1 file (`apps/landing/src/components/Header.tsx`, 3 dòng) nên inline SVG tại chỗ — đổi sang glyph non-brand sẽ âm thầm đổi diện mạo link. Đã enumerate toàn bộ 121 import site lucide: `Github` là export bị bỏ DUY NHẤT đang dùng, 120 site còn lại không ảnh hưởng. **Class 3 — fast-check v4 bỏ các string builder theo unit**: `stringOf(unit,c)`→`string({unit,...c})`, `hexaString(c)`→`string({unit:<hex digit>,...c})`, `char()`→charset `constantFrom` tường minh; v4 cũng infer readonly tuple từ literal shape nên 2 generator phải annotate (`ApprovalScenario`, `capabilitiesArb`) — KHÔNG đổi thứ chúng sinh ra. Bump fast-check cho `docs/` và `studio/` luôn (dependabot bỏ sót, còn v3) — một monorepo mang 2 major của cùng thư viện test là drift chỉ càng đắt về sau. **Phát hiện 1 bug test thật, không phải regression của v4**: `fc.date()` để `noInvalidDate` mặc định false nên date arbitrary dù đã chặn min/max vẫn sinh được `Invalid Date`; bias sinh của v4 bắt đầu đụng vào — property sắp thứ tự của approvals-list so sánh timestamp NaN, và arb của studio card sẽ throw ở `new Date(NaN).toISOString()`. Cả hai nay set `noInvalidDate: true`. **DoD §2d shell**: KHÔNG đụng auth/token (C1), API base URL (C2), `/health` (C7), deep-link (C8) hay thêm endpoint nào (C6). C3/C4 có liên quan vì Studio lên **vite 8** — đã verify bằng build thật: vẫn ra `apps/studio/dist` (`frontendDist: ../../studio/dist`) và `server.port` vẫn 2026 (`devUrl: http://localhost:2026`), nên cả hai contract giữ. Verified: `pnpm turbo run typecheck` 16/16, `pnpm turbo run test` 11/11 (3229 pass, 0 fail), `pnpm turbo run lint` 6/6, `pnpm build` 9/9, `pnpm version:check` synced, `pnpm audit --prod --audit-level high` **No known vulnerabilities**. 10 advisory còn lại đều là dev-only transitive (ngoài phạm vi `--prod` mà CI gate). Landing header verify bằng browser: mark GitHub render đúng, `lucide-menu` render, 0 console error. **Bổ sung 2026-08-21 (hoà giải với #397, đã merge trước):** #397 dựng `pnpm-workspace.yaml` làm bản khai thứ hai của `overrides` cho pnpm 10, nhưng #396 ra đời trước file đó — sau rebase, `package.json` mang giá trị mới còn workspace yaml giữ giá trị cũ, **7 entry lệch nhau** và gate `pnpm settings:check` của chính #397 đỏ. Đã đồng bộ cả 15 override ở hai chỗ. Override `nanoid` giữ **dạng scoped** (`nanoid@3`/`nanoid@5`) của #397 và bỏ dạng phẳng của #396: dạng phẳng `nanoid: ^5.1.16` áp cho cả `next → postcss → nanoid@3`, tức kéo một transitive dep qua một major không ai review; dạng scoped đóng cả hai advisory (GHSA-2v37-7h3g-55p8 ở 3.x, GHSA-28wg-ghj8-5hjv ở 5.x) mà không đụng major nào. **Phát hiện quan trọng nhất: vite 8 trước đây chưa từng có hiệu lực.** `pnpm.overrides.vite` ở `^7.3.5` trong khi `apps/studio` + `apps/docs` khai `^8.1.3`; override của pnpm áp cả cho direct dependency, nên lockfile importer ghi `specifier: ^7.3.5 → 7.3.6` và **hai app build bằng Vite 7 suốt thời gian manifest tuyên bố Vite 8**. Nâng override lên `^8.2.0` cùng manifest mới thật sự flip toolchain (importer nay resolve 8.2.2). `settings:check` không bắt được class này vì hai bản khai override *giống nhau* — chúng chỉ cùng sai so với manifest. Đã cơ giới hoá bằng `scripts/check-override-drift.mjs` + gate CI `pnpm drift:check`: fail khi range override không giao với range khai báo trực tiếp. Guard dependency-free (cùng nguyên tắc với script parity: kiểm tra install settings không được phụ thuộc install thành công), có test riêng qua `node --test` gồm chính ca `vite`; viết test đã bắt được một bug trong parser range — `3` trong scope key là nhánh 3.x, không phải bản 3.0.0. **Sàn Node**: vite 8 cần `>=22.12.0`, eslint 10 cần `^22.13.0`, nhưng `engines.node` là `>=22` — dải nhận 22.0–22.12 và vỡ cả hai. Nâng `>=22.13.0`; `.nvmrc` từ `22` (floating) sang `24` khớp `NODE_VERSION` của CI. `packages/mcp-server` giữ `>=18` vì đó là contract với consumer của package published, không phải sàn toolchain. **Còn lại**: hấp thu số mới hơn của #399 ở 10 package (turbo 2.10.9, wrangler 4.123.0, next 16.3.1, …); thống nhất eslint về 10 (`apps/landing` còn 9 trong khi `apps/consumer` đã khai 10); `@dnd-kit/core` lên `^6.3.0` để peer của sortable 10 thoả **theo khai báo** thay vì tình cờ nhờ lockfile đã resolve 6.3.1; `apps/docs` lên jest-dom 7 để workspace không mang hai major của một thư viện test. Re-verified sau hoà giải: `pnpm -r typecheck` 16/16 · `pnpm test` 11/11 (cms 2474 pass/3 skip, studio 378 pass) · `pnpm lint` 6/6 · `pnpm build` 9/9 · `pnpm audit --prod --audit-level high` **No known vulnerabilities** · `settings:check` + `drift:check` + `scripts:test` (10/10) xanh. C3/C4 re-verify trên build thật: `apps/studio/dist` còn nguyên, `server.port` 2026 còn nguyên. **uuid 14** verify riêng vì rủi ro của nó là export map chứ không phải API: nó **không** vào bundle Worker (`audit/worker.ts` chỉ được đăng ký ở `serve.ts` — xem B10), nên đã verify ở build Node (`build:node`) — `function v7` có trong `dist/serve.cjs` và runtime sinh UUID v7 đúng RFC 9562. |
| 96 | high-load-cache-readiness (programme total — tasks 0–22) | v0.18.x–v0.20.x | Toàn chương trình P0–P2: delivery HTTP cache, permission bump, penetration defence, Cache Provider v2, async audit/flow, worker split, DB indexes | n/a | high-load-cache-readiness/tasks.md | Rà soát closeout 2026-08-02. (1) KHÔNG seed. (2) Env knobs only (`LUMIBASE_*` — xem `docs/en/deployment/docker.md`, `caching.md`); không settings-DB. (3) KHÔNG policy/grant mới. (4) KHÔNG bước wizard. (5) KHÔNG capability. (6) **CÓ migration `0013`** (flow-run history columns/index, additive) + **items indexes** `items_site_coll_updated_idx` / `items_deliver_idx` — idempotent; trên Postgres lớn tạo `items_*` với `CREATE INDEX CONCURRENTLY` ngoài transaction (CHANGELOG + `performance.md`). Không backfill dữ liệu. Chi tiết per-phase: rows #37, #93, #94. Shell: n/a. |
| 97 | (mcp-path-hardening-tripwire — blind-spot follow-up) | v0.25.x | Đã rà soát: siết cơ chế PHÁT HIỆN của guard đã thêm ở #82, không đổi hành vi tool. Registry-scan cũ probe bằng đúng chuỗi `..` rồi tìm `..` như một segment độc lập; khi call site ghép giá trị vào một segment lớn hơn (`/exports/report-${id}.json`) thì probe sinh ra `/exports/report-...json` — không có segment `..` — nên field bị coi là không chạm path và bị `continue` trước khi tới assertion. Source-scan cũng trượt đúng hình dạng đó vì regex chỉ khớp interpolation đứng ngay sau `/`. Nay probe bằng sentinel alphanumeric (`encodePathSegment` giữ nguyên) và soi phần path của URL ghi nhận, nên tìm được đối số dù được ghép ở đâu. Test-only, KHÔNG chạm DB/route/schema | n/a | — | Rà soát 2026-08-03. (1) KHÔNG seed. (2) KHÔNG feature-flag/settings key mới. (3) KHÔNG policy/grant DB. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill. **Vì sao cần**: #82 đóng class path-traversal nhưng chính guard lại có điểm mù — cả hai nửa (encoding + validation) cùng mù với một hình dạng call site duy nhất, nên một tool tương lai dạng `report-${id}.json` khai báo `z.string()` sẽ pass sạch. Đúng kiểu "quên, không phải sai" mà #82 sinh ra để chặn. **Verified bằng negative test**: chèn tạm tool `/exports/report-${id}.json` + `z.string()` ⇒ guard CŨ pass 32/32 (bỏ lọt), guard MỚI fail đúng `tmp_blindspot.id`; gỡ probe ⇒ 32/32 pass, không false positive. typecheck sạch |
| 95 | high-load-cache-readiness (B16 pages CRUD + task 1.3 edgeCache + task 8 Cache Provider v2) | v0.x | Pages API + tag-based cache purge + edgeCache adapter. Tasks 1.3, 8.1–8.7, 22.6/B16 | n/a | high-load-cache-readiness/tasks.md | Rà soát 2026-08-02. (1) KHÔNG seed. (2) KHÔNG env/settings mới (dùng CacheProvider/edgeCache runtime sẵn có; purge là admin HTTP). (3) KHÔNG policy/grant. (4) KHÔNG bước wizard. (5) KHÔNG capability. (6) KHÔNG migration — `lumibase_pages` đã có từ scaffold. Shell: n/a (Studio chưa wire UI pages; API dưới `/api/v1` CORS sẵn). |
| 94 | high-load-cache-readiness (P1 — Req 10, Request_Context_Bundle) | v0.24.x | Đã triển khai (task 11): request-scoped de-duplication cache cho identity lookups. `withAuth` ghi `c.set('principal', { user, membership })` từ `users` row + `user_sites` membership nó đã resolve; `withSiteMembership` đọc từ bundle trước khi query, `withStudioAccess` tái dùng `c.get('access')` mà `withSiteMembership` đã tính thay vì gọi `PermissionService.bundle()` lần hai. Mỗi request giờ tối đa 1× `users` + 1× `userSites` + 1× `bundle()`. Guard semantics KHÔNG đổi; fallback query khi bundle vắng (giữ middleware độc lập). Code + test only | n/a | high-load-cache-readiness/tasks.md task 11 | Rà soát 2026-07-23. (1) KHÔNG seed. (2) KHÔNG settings/env key mới — thuần per-request context var (`Variables.principal`), không cấu hình. (3) KHÔNG policy/grant DB mới — dedup KHÔNG nới lỏng quyền: request bị từ chối trước vẫn bị từ chối cùng status (behavioural matrix test `request-context-bundle.test.ts`), tripwire `security-guards.wiring.test.ts` giữ nguyên. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill — thuần middleware; instance cũ nhận dedup ngay khi lên v0.24.x. Verified: `pnpm -F @lumibase/cms test` middleware 146/146 + routes 199/199, `typecheck` cms sạch |
| 92 | deprecation-agent-guidance | v0.24.x | Ghi rõ cho LLM/`withDeprecation`: cơ chế RFC 8594 đã có; **không** wire global/healthy routes — chỉ gắn khi có chỉ thị deprecate/retire/sunset endpoint cụ thể. Cập nhật `docs/en/agent-setup/prompt.md`, `.cursorrules`, `AGENTS.md`, `CLAUDE.md`, JSDoc middleware, OWASP API9 note | n/a | — | Rà soát 2026-07-30. (1)–(6) KHÔNG seed/settings/policy/wizard/capability/migration — thuần docs + JSDoc. Shell impact: n/a (không đụng auth/token/API base/Studio build). Verified: `withDeprecation` chỉ dùng trong test + docs, chưa mount production route |
| 91 | (follow-up code review của #326: password rule id + control byte + stamp i18n) | v0.24.x | Đã rà soát: sửa hệ quả review của batch majors — `params.rule` của rule độ dài mật khẩu ở `studio/.../setup/schemas/account.ts` trả về `'length'` (khớp `PasswordRuleId`, khớp emitter song song ở `recovery/backup-code-page.tsx`), thay byte điều khiển thô bằng escape text trong `shared/schemas/site-config.ts`, và stamp lại 2 cặp docs bị drift. **KHÔNG** đổi seed/settings/wizard/capability/migration | n/a | — | Rà soát 2026-08-01. (1) KHÔNG seed. (2) KHÔNG settings key. (3) KHÔNG policy/grant. (4) KHÔNG bước wizard mới — `accountSchema` vẫn là schema của bước Account sẵn có; chỉ đổi **định danh** `params.rule` (metadata của issue), message người dùng thấy không đổi, và không có consumer runtime nào đọc `params.rule` nên UX bước Account giữ nguyên. (5) KHÔNG capability. (6) KHÔNG migration/backfill — không đụng DB. `site-config.ts` giữ nguyên semantics regex (đã verify identical trên toàn bộ 256 code point), nên `displayTitle` sanitize y như trước. DoD §2d shell: KHÔNG đụng auth/token, API base URL, build Studio, hay endpoint nào → mọi contract C1–C8 giữ. Verified: `pnpm typecheck` 16/16, `pnpm -F @lumibase/studio test` 67 file/378 test, `pnpm -F @lumibase/cms test` 2398 pass, `pnpm docs:i18n:detect` up-to-date 91→93 / planned 51→49. Test mới `setup/schemas/__tests__/account.test.ts` khoá contract `params.rule` ↔ `PasswordRuleId` (đã chứng minh bắt được regression: 3/4 fail khi trả lại bug) |
| 90 | (dependabot majors batch: zod 4 + jose 6 + React 19 + node-cron 4 + Node 22) | v0.24.x | Đã rà soát: bump deps/runtime (Zod 4, jose 6, React 19 peers, node-cron 4, engines Node ≥22). Chạm Studio setup schemas (`admin-path`/`account`) và CMS auth JWT nhưng **không** đổi seed/settings/wizard/capability/migration | n/a | — | Rà soát 2026-07-31. (1) KHÔNG seed. (2) KHÔNG settings key. (3) KHÔNG policy/grant. (4) KHÔNG bước wizard mới — chỉ đổi Zod error API (`error`/`prefault`) trên schema sẵn có. (5) KHÔNG capability. (6) KHÔNG migration/backfill. **DoD §2d shell**: jose 6 `KeyLike`→`CryptoKey` trên verify; session HS256 `Uint8Array` giữ; token-store/api.ts (C1), không đổi VITE_API_URL/CORS (C2/C6), `/health` (C7), deep-link (C8). **Missed cron**: node-cron v4 bỏ `recoverMissedExecutions` — audit/pageview có thể miss tick khi restart. Engines: root + create-lumibase `>=22`, `.nvmrc`=22. |
| 89 | (public/anonymous realm + per-realm access grants + publishable API keys) | v0.24.x | Đã rà soát: realm `public` (anonymous) — request không auth resolve thành system role `public`; API cấp quyền theo realm `/api/v1/access/grants*`; picker Studio tại `/access/public`; API key dạng publishable (tiền tố `lbk_pub_`) kèm allowlist origin. **CÓ migration** `0012_public_role_least_privilege.sql` (2 CHECK constraint) → xem (6) | done | — | Rà soát 2026-07-28. (1) KHÔNG seed lúc setup — role `public` + policy `public` được provision **lười** bởi `enablePublicAccess()` khi operator bật public access, nên instance mới vẫn đóng theo mặc định (fail-closed: thiếu role ⇒ `withAuth` giữ nguyên 401). (2) KHÔNG settings key/feature flag — trạng thái bật/tắt chính là sự tồn tại của role `public`, không có cờ song song để lệch nhau. (3) **CÓ policy/grant là dữ liệu DB** (đúng ý câu hỏi 3): role `public` + policy `public` + row `permissions` do `grantPublicRead` tạo — cố ý là row DB thay vì fallback hardcode, để RBAC/audit/RLS soi được và operator thu hồi được. `PUBLIC_ALLOWED_ACTIONS = ['read']`; row-rule mặc định `status _eq published`. (4) KHÔNG bước wizard mới — picker nằm ở Access → Public & subscribers, không phải Setup Wizard. (5) KHÔNG capability `/setup/capabilities` mới. (6) **CÓ — instance đã setup cần chạy migration `0012`**: thêm `roles_public_least_privilege` và `policies_public_least_privilege`, ghim `admin_access`/`app_access` (và `enforce_tfa` cho policy) = false khi `system_key`/`key` = 'public'. Dùng `is distinct from` nên predicate là FALSE-or-TRUE, không NULL với các row `system_key` NULL — additive, không backfill dữ liệu. **Lưu ý**: nếu một instance đã tự tạo role/policy tên 'public' CÓ cờ elevation thì `ALTER TABLE` sẽ fail — hạ cờ trước khi migrate. Vì sao cần constraint dù service layer đã chặn: role `public` là thứ request **chưa xác thực** resolve thành, nên một cờ elevation trên nó là đường bypass admin không cần đăng nhập; constraint giữ cả với SQL viết tay, import config và code path tương lai. Multi-tenant: role/policy/permission `public` đều có `site_id` — bật ở site A không mở gì ở site B. DoD §2d (shell): endpoint mới nằm dưới `/api/v1` nên được `cors` toàn cục phủ (C6 giữ); Studio page dùng `getApiClient()` chứ không đọc `localStorage`/`VITE_API_URL` trực tiếp (C1/C2 giữ). Publishable key: KHÔNG migration — dùng `metadata` jsonb sẵn có (`allowedOrigins`); tiền tố token là source of truth chứ không phải cờ metadata, nên một key cũ không thể bị nhầm thành publishable. Verified: `pnpm -F @lumibase/cms test` 2385 pass, `pnpm -F @lumibase/studio test` 370 pass, `pnpm typecheck` 16/16. **Bổ sung 2026-07-28 (sau code review)**: sửa `roleId` bị hardcode `null` trong `MagicContext` (grant public không tới được content API), screen cờ elevation trên chính role khi attach vào publishable key, validate collection khi grant, chặn `DELETE /roles/:id` với role `public`, đổi refusal của grant từ 409 sang 400, và thêm ô sửa `allowedOrigins` trong Studio. Rà soát lại 6 câu hỏi: (1)–(5) KHÔNG đổi — vẫn không seed, không settings key, không bước wizard, không capability mới; policy/grant vẫn là dữ liệu DB như mô tả ở trên. (6) **KHÔNG migration mới** — toàn bộ là route/service/UI, `0012` vẫn là migration duy nhất của feature này. DoD §2d: không thêm endpoint nào; Studio bắt đầu gọi `PATCH /api-keys/:id/allowed-origins` (đã có từ trước) qua `getApiClient()`/SDK nên C1/C2 giữ, và `PATCH` nằm trong `allowMethods` mặc định của Hono 4.12.31 (`["GET","HEAD","PUT","POST","DELETE","PATCH"]`, `cors` không override) nên C6 giữ. Verified: `pnpm -F @lumibase/cms test` 2396 pass, `pnpm typecheck` 16/16, `pnpm test` 11/11. **Bổ sung lần 2 — enable-before-grant**: `POST /access/grants/public` giờ trả 409 `PUBLIC_ACCESS_DISABLED` khi realm chưa bật, thay vì tự provision realm như tác dụng phụ. Ảnh hưởng tới setup: **tăng cường (1)+(2) đã ghi ở trên** — instance mới vẫn đóng theo mặc định, và giờ chỉ có **một** đường bật realm (`/enable`, có audit `public_access_enabled`) chứ không phải hai, nên `/disable` trở thành sticky: một lệnh grant sau đó không thể âm thầm mở lại. Vẫn KHÔNG seed, KHÔNG settings key, KHÔNG bước wizard, KHÔNG capability, **KHÔNG migration**. Không phá client hiện có: picker Studio vốn đã render realm togglable đang tắt ở chế độ read-only (`public-access-page.tsx` — `locked = realm.togglable && !realm.enabled`), nên UI vốn đã giả định đúng thứ tự này; chỉ server lệch nhịp. `openapi.yaml` đã cập nhật 400/409/404 cho path này. Verified: `pnpm -F @lumibase/cms test` 2398 pass, `pnpm typecheck` 16/16 |
| 88 | (studio setup-state gate: stable unreachable UI) | v0.24.x | Đã rà soát: Studio `SetupStateGate` / `AdminReadyGate` — khi `GET /setup/state` fail, giữ màn hình lỗi ổn định (không auto-refetch on focus/reconnect; sticky error khi manual retry). UI-only trên endpoint sẵn có | n/a | — | Rà soát 2026-07-28. (1) KHÔNG seed. (2) KHÔNG settings key. (3) KHÔNG policy/grant. (4) KHÔNG bước wizard mới — chỉ đổi UX gate khi API unreachable. (5) KHÔNG capability mới. (6) KHÔNG migration/backfill. Shared hook `useSetupStateQuery` + test `setup-state-gate.test.tsx` |
| 87 | (landing sponsor-rewards store — issue #296) | v0.24.x | Đã rà soát: thay stub no-op `saveToDatabase()` trong `apps/landing/src/lib/rewards.ts` bằng module `src/lib/rewards/` sau một interface `SponsorStore` — `InMemorySponsorStore` (default, dev/test) + `D1SponsorStore` (Cloudflare D1, persistent, claim atomic bằng `UPDATE … WHERE reward_token = ? AND claimed = 0 RETURNING tier`). Kèm migration D1 `apps/landing/migrations/0001_sponsors.sql` + binding `SPONSORS_DB` (comment sẵn trong `apps/landing/wrangler.toml`) + 24 unit test. **Hoàn toàn nằm trong `apps/landing`** — KHÔNG đụng CMS, KHÔNG đụng Postgres của `packages/database`, KHÔNG đụng `site_id`/RLS (dữ liệu sponsor thuộc website marketing, không phải dữ liệu tenant) | n/a | — | Rà soát 2026-07-25. (1) KHÔNG seed vào DB của LumiBase — bảng `sponsors` nằm ở **D1 riêng của landing** (`lumibase-sponsors`), rỗng khi tạo, không thuộc luồng setup wizard. (2) KHÔNG settings key/feature-flag của CMS; cấu hình duy nhất là **binding hạ tầng** `SPONSORS_DB` ở `apps/landing/wrangler.toml`, vắng binding → fallback in-memory (kèm `console.warn` khi `NODE_ENV=production`). (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill cho instance đã setup — migration `0001_sponsors.sql` là của D1 landing (`CREATE TABLE IF NOT EXISTS`), áp bằng `wrangler d1 migrations apply`, độc lập với `packages/database/drizzle/*`; số migration Postgres KHÔNG bị chiếm. **Lưu ý vận hành**: landing hiện build `output: 'export'` (static) nên 2 route dùng module này vẫn nằm ở `src/_api-routes-disabled/`, và trang `/rewards/claim` đã bị xoá ở `31b2be9a` (luôn 404) — persistence chỉ thật sự bật khi route được enable trên server runtime (vd `@cloudflare/next-on-pages`) + gọi `configureSponsorStore(resolveSponsorStore(env))`; quy trình ở mục "Sponsor rewards store" trong `apps/landing/README.md`. Verified: `pnpm typecheck` 16/16 sạch, `pnpm test` 11/11 (landing 24/24 mới), `pnpm -F @lumibase/landing lint` + `build` (static export 10 route) pass |
| 86 | (agent-run-worker-keyprovider) | v0.24.x | Đã rà soát: thread `KeyProvider` vào mọi call site `new AISecureHarness` thực thi skill — `AgentRunWorkerDeps` thêm `keys?: KeyProvider` (`services/agent-run-worker.ts`) nhận từ `runtime.keys` ở `serve.ts`, cộng 3 call site request-path vốn cũng thiếu (`routes/ai.ts` chat + approval-execution, `routes/mcp.ts`). Trước đó 4/4 call site đều không truyền `keys` ⇒ cả họ deployment skill (`triggerDeployment`/`listDeploymentTargets`/`listDeployments`/`getDeploymentStatus`) fail closed `DEPLOYMENTS_NOT_CONFIGURED` trên mọi đường. Kèm tripwire source-scan `apps/cms/src/__tests__/ai-harness-keys-context.test.ts` + behavioural `agent-run-worker-keys.test.ts` + 1 dòng checklist DoD §2b (parity runtime provider request-path ↔ worker). Code + test + docs only | n/a | — | Rà soát 2026-07-25. (1) KHÔNG seed row DB — `keys` là provider runtime dựng từ env (`ENCRYPTION_KEY`/`ENCRYPTION_KEY_FILE`), không phải dữ liệu. (2) KHÔNG settings key / env mới — dùng đúng KEK env mà `crypto-service`/`token-vault` đã yêu cầu từ v0.13.x. (3) KHÔNG policy/grant DB mới — capability `deployments:read`/`deployments:write` và HITL của `triggerDeployment` không đổi; đây chỉ là wiring provider. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill — không đụng schema; instance đã setup nhận vá ngay khi lên bản mới. **Đổi hành vi**: deployment skill từ "luôn lỗi" → chạy thật ⇒ instance nào chưa cấu hình KEK sẽ thấy lỗi decrypt token thay vì `DEPLOYMENTS_NOT_CONFIGURED` (KEK vốn đã bắt buộc cho target đã lưu). Multi-tenant: `KeyProvider` là **tài nguyên dùng chung toàn deployment** (KEK env, không phải dữ liệu tenant) — cô lập tenant nằm ở `siteId` của `DeploymentService`, worker lấy `siteId` từ payload job (test khẳng định `agent_tool_calls.siteId === payload.siteId`). Verified: `pnpm -F @lumibase/cms test` 2277/2277 pass, `pnpm typecheck` 16/16 sạch, tripwire đã kiểm chứng fail khi bỏ `keys` khỏi `routes/mcp.ts` và khỏi worker |
| 85 | (studio wiring: Settings → Deployments) | v0.24.x | Đã rà soát: nối dây trang Studio `deployments-page.tsx` (mở rộng #28 deployment-integrations) — trang đã hoàn chỉnh nhưng **không có route** trong `apps/studio/src/router.tsx` và **không có entry sidebar** trong `modules/settings/layout.tsx`, tức dead code kể từ khi ship. Thêm route `/settings/deployments` + biến thể admin-path `/$adminPath/settings/deployments`, thêm nav item vào nhóm Integrations. Thuần Studio (client) — KHÔNG đụng schema/route CMS/seed (backend `/api/v1/deployments` đã mount sẵn) | n/a | — | Rà soát 2026-07-25. (1) KHÔNG seed — deployment target tạo theo nhu cầu, bảng rỗng là trạng thái hợp lệ (giữ nguyên kết luận #28). (2) KHÔNG settings key mới. (3) KHÔNG policy/grant DB mới — route CMS giữ guard sẵn có. (4) KHÔNG bước UI wizard mới — trang nằm trong Settings, không phải wizard. (5) KHÔNG capability `/setup/capabilities` mới (theo tiền lệ #28). (6) KHÔNG migration/backfill — instance đã setup tự có trang khi Studio cập nhật. Kèm tripwire chống tái diễn cả class (DoD §6): `settings/__tests__/deployments-page.test.tsx` khẳng định MỌI `*-page.tsx` trong `modules/settings/` được `router.tsx` tham chiếu và MỌI nav item có route khớp, cộng test route-tree thật `deployments-route.test.tsx` (memory history) — trang orphan sẽ đỏ CI thay vì ship vô hình. Số dòng cấp lại 84→85 khi rebase lên main (PR #298 "Google Cloud single-VM" đã chiếm 84) — đúng class trùng số mà DoD §2 cảnh báo |
| 84 | (deployment: Google Cloud single-VM) | v0.24.x | Đã rà soát: thêm đường deploy Google Cloud single-VM — overlay `docker/docker-compose.gcp.yml` (build image prod trên VM, phối hợp credential minio/imgproxy/cms, ráp `DATABASE_URL` từ `POSTGRES_PASSWORD`, wiring Gemini), `docker/.env.prod.example`, script provision `docker/scripts/gcp-vm-setup.sh`, doc `docs/en/deployment/google-cloud-vm.md` + mirror `docs/vi/deployment/google-cloud-vm.md`, index vào `docs/{en,vi}/README.md` + sidebar `apps/docs/docs.config.json`; thêm LLM passthrough (`LLM_PROVIDER`/`LLM_MODEL`/`GEMINI_API_KEY`) vào compose dev (default `echo`). Config/docs only | n/a | — | Rà soát 2026-06-25, reconcile 2026-07-25 (merge main v0.24.x — đánh số lại row 27 → 84 vì 27 đã thuộc `wysiwyg-extension-data-types`). (1)(3) KHÔNG seed/row/policy: chỉ là cấu hình deploy + biến môi trường; `LLM_PROVIDER=gemini` route AI call qua GeminiProvider sẵn có, thiếu key → fallback echo. (2) KHÔNG feature-flag DB mới; các biến LLM đã được `llm-provider.ts` đọc từ env từ trước (không phải `settings`). (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability mới. (6) KHÔNG backfill: không migration, không bảng/cột. Overlay đồng thời vá lỗ hổng prod hiện có — base compose để S3/Meili/imgproxy ở giá trị dev mặc định mà `validateProductionConfig` reject; overlay thread secret non-default từ `.env` vào mọi service dùng chung |
| 83 | (brand casing: `Lumibase` → `LumiBase`) | v0.24.x | Đã rà soát: chuẩn hoá casing thương hiệu trong prose/display string toàn repo (118 file). Chạm setup ở **giá trị mặc định `displayTitle`** — `apps/cms/src/modules/setup/service.ts:1194`, `apps/studio/src/modules/setup/simple-setup-wizard.tsx:139`, `apps/studio/src/modules/setup/steps/step-project.tsx:52` (+ placeholder ở `step-project.tsx:156`, `settings/site-page.tsx:181`) đổi `'Lumibase'` → `'LumiBase'`. Thuần chuỗi hiển thị, KHÔNG đụng schema/route/logic | n/a | — | Rà soát 2026-07-25. (1) **KHÔNG seed thêm row**, nhưng site tạo mới trong setup nhận `displayTitle = 'LumiBase'` thay vì `'Lumibase'` — chỉ nhãn hiển thị, không phải key/identifier, không ràng buộc unique. (2) KHÔNG settings key mới. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) **KHÔNG migration/backfill** — instance đã setup giữ nguyên `displayTitle` cũ trong DB (không rewrite dữ liệu người dùng); ai muốn đổi thì sửa ở Studio → Settings → Site. Ngoại lệ **cố ý giữ nguyên**: type sinh cho user `LumibaseCollections`/`LumibaseSchema` (`packages/sdk/src/typegen/index.ts` + code block trong `docs/{en,vi}/features/typegen.md`) vì đổi là breaking cho ai đã `import type { LumibaseSchema }`; và `.codex/environments/environment.toml` (`name = "Lumibase-codex"`, identifier tool ngoài). Kèm: rename asset `docs/UX Psychology Principles for Lumibase.pdf` → `...LumiBase.pdf` (git `R100`) đồng bộ URL-encoded link trong `docs/README.md` — nếu chỉ đổi text mà không đổi tên file sẽ 404 trên FS case-sensitive (Linux/CI), macOS không phát hiện. Verified: `pnpm typecheck` 16/16 sạch, `pnpm -F @lumibase/cms test` setup 95/95, JSON/YAML đã sửa parse OK, không còn `Lumibase` ngoài 2 ngoại lệ trên |
| 81 | owasp-api-top-10-audit | v0.24.x | Đã triển khai: doc audit OWASP API Top 10 2023 (`docs/en/security/owasp-api-top-10-audit.md`) + vá 4 gap thuần code/middleware. (API4) fail-closed mode cho general rate limiter qua env `LUMIBASE_RATE_LIMIT_FAIL_CLOSED` (default fail-open giữ nguyên) → `503 RATE_LIMIT_UNAVAILABLE`. (API7) `resolveAndValidateOutboundUrl()` + DNS resolution chống DNS-rebinding, wire vào `guardedFetch()` (resolver `node:dns` lazy trên Node, skip trên Workers). (API9) `/test-auth` trả `404` khi production + middleware `withDeprecation` (RFC 8594 `Deprecation`/`Sunset`). Kèm FE docs: API spec thêm mã `503 RATE_LIMIT_UNAVAILABLE` + note deprecation headers; tutorial `docs/{en,vi}/tutorials/nextjs-quickstart.md` thêm mục "Production & security notes for frontends" (§1-6: giữ key server-side, xử lý 429/503, watch Deprecation/Sunset, CORS, /test-auth dev-only, vá Next.js SSRF ≥16.2.11). Code + doc only | n/a | — | Rà soát 2026-07-23. (1) KHÔNG seed. (2) **CÓ 1 env mới nhưng KHÔNG phải settings-DB key** — `LUMIBASE_RATE_LIMIT_FAIL_CLOSED` là env `Bindings` optional (họ `LUMIBASE_RATE_LIMIT_*`), default off → hành vi fail-open không đổi. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill — thuần code/middleware, không đụng schema; instance cũ nhận vá ngay khi lên v0.24.x. **Đổi hành vi**: (a) `/test-auth` → `404` ở production (dev-tooling, trước đây mount public mọi môi trường); (b) khi bật `FAIL_CLOSED`, cache outage → `503` thay vì cho qua ⇒ operator phải đảm bảo cache sẵn sàng trước khi bật. Kèm bump dependency đóng advisory HIGH mà gate CI `Dependency vulnerability audit` bắt (pre-existing, thuộc FE app + transitive, KHÔNG thuộc CMS API): `next → 16.2.11` (consumer+landing), pnpm `overrides` `fast-uri >=3.1.4` + `sharp >=0.35.0` — thuần version/lockfile, KHÔNG seed/settings/migration/backfill. Verified: `pnpm -F @lumibase/cms test` (ssrf-guard/rate-limit/deprecation) 22/22, `pnpm typecheck` sạch 16/16, `pnpm audit --prod --audit-level high` exit 0, build landing+consumer pass |
| 82 | (mcp-path-hardening-tripwire) | v0.24.x | Đã rà soát: cơ giới hóa DoD §6 cho class path-traversal của `@lumibase/mcp-server` (nối tiếp #21b). Thêm `src/__tests__/path-hardening.wiring.test.ts` với 2 guard chạy trên MỌI tool hiện có và tương lai: (a) source-scan — mọi interpolation `/${…}` trong `tools/*.ts` phải qua `encodePathSegment`/`encodeMediaKey`; (b) registry-scan — với mọi tool đã đăng ký, arg nào chạm tới một path segment thì schema PHẢI từ chối `..`. Guard phát hiện gap thật còn sót: `cdc_subscription_replay.subscription_id` khai báo `z.string()` + `encodeURIComponent` ⇒ `..` lọt tới `/cdc/subscriptions/../replay`; vá bằng `idPathSegmentSchema` + `encodePathSegment`. Code + test only, KHÔNG chạm DB | n/a | — | Rà soát 2026-07-25. (1) KHÔNG seed. (2) KHÔNG feature-flag/settings key mới. (3) KHÔNG policy/grant DB — chỉ siết validation/encoding tham số tool, không đổi capability hay autonomy. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: không migration/bảng/cột. **Vì sao cần tripwire**: #21b (v0.13.x) vá theo từng file nên tool CDC thêm sau ở v0.21.x tái phát đúng class đó — cùng kiểu "quên, không phải sai" mà DoD §2c/§6 cảnh báo. Chỉ encode là KHÔNG đủ: `encodeURIComponent('..') === '..'`, nên guard (b) kiểm tầng validation tách riêng. Guard (b) có assertion chống vacuous (`guarded.length > 40`) để refactor làm probe hụt sẽ fail thay vì pass rỗng. Verified: `pnpm -F @lumibase/mcp-server test` 32/32, typecheck sạch |
| 79 | setup-complete-rate-brake | v0.23.x | Đã triển khai (`.kiro/specs/setup-complete-rate-brake/`): thêm per-IP rate brake cho `POST /api/v1/setup/complete` (10 req/60s/IP, in-memory sliding window `checkCompleteRateLimit`, bucket riêng độc lập với `/state`), áp **trước** `c.req.json()` + password hashing để chặn brute-force `setupToken` và CPU-spam trong cửa sổ trước-khi-initialized (CWE-770; analog Strapi #26494). Kèm: wire seam test-only `setupServiceOverride` (`env.ts` + handler) — trước đây comment nhắc tới nhưng code chưa đọc. Race guard first-admin (`FOR UPDATE` + unique index) **KHÔNG đụng**. Code + test only | done | [setup-complete-rate-brake/tasks.md](../setup-complete-rate-brake/tasks.md) | Rà soát 2026-07-17. (1) KHÔNG seed. (2) KHÔNG settings key mới — ngưỡng hard-coded (giống `/state`); chưa env-config (nới sau nếu cần). (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill. **Đổi hành vi endpoint `/setup/complete`**: thêm nhánh `429 { errors: [{ code: 'RATE_LIMITED' }] }` + `retry-after: 60` ⇒ **client setup phải xử lý 429 (backoff, không loop cứng khi sai password policy/thiếu token)**. Giới hạn: brake in-memory **per-isolate** — nhiều isolate/instance ⇒ hạn mức thực = `10 × số_isolate`; DB lock + unique index vẫn là hard guard chống admin trùng. Verified: `pnpm -F @lumibase/cms test setup/` 95/95 (5 test mới `complete-rate-brake.test.ts`), typecheck cms sạch |
| 78 | graphql-cost-limit | v0.23.x | Đã triển khai (`.kiro/specs/graphql-cost-limit/`): thêm `costLimitRule` (static AST cost, dependency-free) cạnh `depthLimitRule` sẵn có trong `hardeningPlugin.onValidate` (`apps/cms/src/graphql/yoga.ts`) — chặn query rộng-nhưng-nông (nhiều field song song, `first`/`limit` lớn, alias trùng, field ẩn trong fragment) mà depth-limit bỏ lọt (CWE-770). List multiplier suy từ `first`/`last`/`limit`/`pageSize` literal (clamp `[1, maxListMultiplier]`), `Variable`/vắng mặt → `defaultListSize`. Config qua env `LUMIBASE_GQL_MAX_COST`/`LUMIBASE_GQL_DEFAULT_LIST_SIZE`/`LUMIBASE_GQL_MAX_LIST_MULTIPLIER` (default 1000/20/100). Code + test only | n/a | [graphql-cost-limit/tasks.md](../graphql-cost-limit/tasks.md) | Rà soát 2026-07-17. (1) KHÔNG seed. (2) **CÓ 3 env mới nhưng KHÔNG phải settings-DB key** — là env `Bindings` optional có default an toàn (giống họ `LUMIBASE_RATE_LIMIT_*`), rule chạy mọi môi trường với default nếu không set. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill — thuần validation rule, không đụng schema; instance cũ nhận cost-limit ngay khi lên v0.23.x. Verified: `pnpm -F @lumibase/cms test graphql/__tests__/hardening.test.ts` 13/13 (4 depth/introspection cũ + 9 cost mới), `typecheck` cms sạch |
| 77 | mcp-value-assessment (Sóng 1 insights + Sóng 2 content-versions + vá coverage) | v0.23.x | Phơi bày feature sẵn có qua MCP: (Sóng 1) 5 read-only insights tools ở stdio server; (Sóng 2) 6 content-version governed skills ở harness (`create/update/delete/promoteVersion` dangerous); (vá coverage) editorial (`list_reviews`/`submit_review`/`approve_content`/`reject_content`), releases (CRUD + `publish_release`), deployments read-only, shares (`create_share`/`revoke_share`), TM `update_tm`/`delete_tm`, `get_flow_run`, `list_transform_presets`, `get_effective_preset`/`list_preset_bookmarks`, `get_site` | n/a | — | Rà soát 2026-07-16. (1) KHÔNG seed row DB — tool/skill là code, gọi lại REST/service sẵn có (`InsightsService`, `ContentVersionService`, `ReleaseService`, editorial/deployments/shares routes). (2) KHÔNG settings key mới — governed endpoint vẫn gate bởi `contentOs.mcp` đã tồn tại. (3) KHÔNG policy/grant DB mới — dùng capability sẵn có; risk tự phân loại qua `dangerous` flag + tiền tố `delete`. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill — không đụng schema; instance cũ nhận tool/skill mới ngay khi lên v0.23.x. Version writes + trigger deploy đi qua HITL/`agent_approvals`; stdio server **cố ý không** có version tools/trigger-deploy (cần HITL). Loại trừ có chủ đích (realtime/signed-URL/binary/security-admin/auth/dev-infra) ghi ở `docs/en/mcp/index.md`. Test: `governed-skills.test.ts`, `mcp-server/src/__tests__/tools.test.ts` (29 case endpoint parity) |
| 76 | (ai-providers + meilisearch-on-aws) | v0.23.x | Đã rà soát: thêm 2 LLM provider tuỳ chọn `nvidia` (OpenAI-compatible, tái dùng `OpenAIProvider` + `NVIDIA_BASE_URL` cho NIM self-host) và `vertex` (Vertex AI, tái dùng helper Gemini `generateContent`, auth OAuth bearer) vào `llm-provider.ts`; + guide deploy MeiliSearch lên AWS (adapter Docker đã tồn tại) | n/a | — | Rà soát 2026-07-15. (1) KHÔNG seed: không bảng/row mới. (2) **CÓ env mới nhưng KHÔNG phải settings-DB key** — `NVIDIA_API_KEY`/`NVIDIA_BASE_URL`/`VERTEX_ACCESS_TOKEN`/`VERTEX_PROJECT_ID`/`VERTEX_LOCATION` là env `Bindings` (giống họ `LLM_PROVIDER`/`OPENAI_API_KEY` sẵn có, gap #18/email pattern), default off → `echo`. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới — AI Copilot cấu hình qua env sau setup, không thuộc first-time wizard. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill: thuần code + docs; instance cũ không set provider mới → hành vi không đổi. Lưu ý billing: `nvidia`/`vertex` gọi cloud NGOÀI AWS (NVIDIA / Google Cloud), không tiêu credit AWS; chỉ `nvidia` self-host qua `NVIDIA_BASE_URL` + MeiliSearch mới ăn khớp hạ tầng AWS. Verified: `pnpm -F @lumibase/cms test llm-provider` 15/15 + `llm-generation-skills` pass, `typecheck` sạch |
| 74 | lumibase-pageview-counter (Track A) | v0.21.x | Đếm lượt truy cập: module built-in `apps/cms/src/modules/pageviews/` + 3 bảng `lumibase_pageview_events/_daily/_uniques` (migration `0010_pageviews`) + runtime atomic counter (`CacheProvider.increment` + Cloudflare `PageviewCounter` DO) + endpoint public `POST /api/v1/pageviews/:site_id/hit` + `GET /pageviews/stats` + settings key `pageviews.*` (scope `module`) + cron flush `*/5` | done | — | Rà soát 2026-07-11. (1) KHÔNG seed row DB: 3 bảng rỗng khi init; per-site config qua settings key `pageviews` khi cần (default `db-rollup`, `enabled:true` khi vắng). (2) **CÓ settings key mới** `pageviews.*` (`strategy`/`userTable`/`respectConsent`/`hashSalt`/`flushIntervalS`/`botFilter`) — nhưng có default an toàn, operator không cần bật thủ công; đọc qua `routes/settings.ts` sẵn có. (3) KHÔNG policy/grant DB mới — `/hit` public (rate-limited), `/stats` gated `withAuth`. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) **CÓ migration `0010_pageviews`, KHÔNG cần backfill**: `CREATE TABLE IF NOT EXISTS` + FK guarded — instance cũ thiếu bảng → no-op cho tới hit đầu tiên. **Vận hành CF**: DO migration `tag="v2" new_sqlite_classes=["PageviewCounter"]` + binding `PAGEVIEW_COUNTER` + cron `*/5` phải deploy (đã thêm mọi env profile trong `wrangler.toml`); thiếu binding → `hot-counter`/`hll` fail-soft về `db-rollup`. Consent: dùng category `analytics` **sẵn có** trong `CONSENT_TYPES` (không migration). Verified Postgres 16 scratch: migration áp sạch; e2e recordHit→flush→stats đúng views/uniques + tenant-isolated. Panel UI `lumibase-pageview-counter` nằm ở submodule `extensions/` (commit riêng) |
| 75 | lumibase-pageview-counter (Track B — signing/auto-install) | v0.21.x | Ký & xác thực official extension: bảng `lumibase_publisher_keys` (migration `0011_extension_signing`) + 4 cột `lumibase_extensions.is_official/auto_install/enabled_by_default/verified_at` + verify-everywhere (sandbox fail-closed cho official) + auto-install `lumibase-*` (setup step 9d seed official key + reconciler on site-create) + CLI `@lumibase/extension-cli` | done | — | Rà soát 2026-07-11. (1) **CÓ seed CÓ ĐIỀU KIỆN**: setup step 9d seed 1 row official key vào `lumibase_publisher_keys` **chỉ khi** deploy cấu hình `MARKETPLACE_PUBLIC_KEYS` chứa key id `lumibase-official-v1` (idempotent `onConflictDoNothing`); vắng → skip, official verify không có key → auto-install fail-soft. Auto-install extension chạy qua reconciler khi tạo site (fail-soft nếu chưa có published row). (2) **CÓ settings/env mới**: env `LUMIBASE_EXT_SIGNATURE_POLICY` (`require` default | `warn`) + `MARKETPLACE_PUBLIC_KEYS` (đã có, giờ merge với DB registry). (3) **CÓ dữ liệu DB thay env**: `lumibase_publisher_keys` — DB row override env cho `official`/`revoked` (env-tamper không thể phong official/gỡ revoke). (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) **CÓ migration `0011_extension_signing`, cần bước upgrade (không backfill dữ liệu)**: `CREATE TABLE IF NOT EXISTS` + `ADD COLUMN IF NOT EXISTS` (cột default `false`/null) — instance cũ nhận default; **để official verify hoạt động, instance cũ phải (a) set `MARKETPLACE_PUBLIC_KEYS` chứa key official + (b) chạy 1 lần reconcile / re-run setup key-seed**. `isOfficial` LUÔN derive server-side (namespace `lumibase-` + chữ ký official) — không tin manifest. `/submit` chặn `lumibase-*` (RESERVED_NAMESPACE). Verified: keygen→sign→verify round-trip OK; reason matrix (tamper/wrong-key/revoked/rsa-downgrade/missing) pass |
| 38 | cwe-top-100-remediation | v0.19.0 | Đã rà soát: đợt vá bảo mật cuối cùng đóng 12 CWE còn lại (78/78 mitigated) — CORS credentialed no-wildcard (CWE-942), bỏ CDC fallback key + fail-closed (CWE-321), AI approval decide/reject atomic (CWE-362/367), password policy tối thiểu 12 ký tự (CWE-521), **`users.token_version` cho JWT revocation** (CWE-613/620) + Cloudflare Access role resolve từ site-membership thật (CWE-302), rate limiter chung per-principal/IP (CWE-400), audit metadata redact payload (CWE-359), dependabot + `pnpm audit` gate CI (CWE-1104), health/metrics gate `METRICS_TOKEN` (CWE-284/668), materialize-service SQL injection fix (CWE-89) | n/a | — | Rà soát 2026-07-07. (1) KHÔNG seed. (2) KHÔNG feature-flag/settings key mới — `LUMIBASE_RATE_LIMIT_*`/`METRICS_TOKEN`/`ENCRYPTION_KEY` là env, không settings-DB. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) **CÓ migration, KHÔNG cần backfill dữ liệu**: `0002_add_user_token_version` thêm `users.token_version integer DEFAULT 0 NOT NULL` — instance cũ nhận giá trị mặc định `0`, JWT hiện hành vẫn hợp lệ cho tới lần đổi mật khẩu kế tiếp (không tự động vô hiệu hoá token cũ). **Vận hành bắt buộc:** phải set `ENCRYPTION_KEY` trước khi start CDC route (fail-closed `503` nếu thiếu) — nếu instance cũ dựa vào fallback key mặc định, cần cấu hình biến này trước khi upgrade lên v0.19.0 |
| 37 | high-load-cache-readiness (Phase 0 + P0) | v0.18.x | Đã rà soát: Phase 0 chỉ thêm k6 scenario/seed load-test và baseline artifacts; không thay đổi setup runtime. P0 gồm delivery HTTP caching, permission cache key-versioning, API-key lastUsed debounce, setup-state process cache, list count opt-in, body-size limits. KHÔNG yêu cầu khởi tạo | n/a | high-load-cache-readiness/tasks.md Phase 0/P0 | Rà soát 2026-08-02. (1) KHÔNG seed: không bảng/row mới. (2) KHÔNG settings key — mọi knob là **env** (`LUMIBASE_DELIVER_SMAXAGE/SWR`, `LUMIBASE_APIKEY_TOUCH_INTERVAL`, `LUMIBASE_MAX_JSON_BODY`). (3) KHÔNG policy/grant DB. (4) KHÔNG bước wizard mới. (5) KHÔNG capability mới. (6) KHÔNG backfill: thay đổi thuần code/middleware; instance cũ đọc default env, hành vi cải thiện ngay không cần migration. Permission cache key đổi prefix version-hoá nhưng key cũ tự hết hạn theo TTL 60s — không cần flush thủ công. Delivery response default không đổi shape (chỉ thêm header); list `meta` default `total_count` giữ tương thích. Chi tiết DoD: `high-load-cache-readiness/dod-review.md` |
| 93 | high-load-cache-readiness (Req 19 — cache penetration) | v0.x | Chống cache penetration trên đường đọc công khai: shape guard + tombstone (`CacheEntry`/`setNegative`) + deliver IP rate limit. Tasks 22.1–22.10 | n/a | high-load-cache-readiness/tasks.md 22.x | Rà soát 2026-08-01. (1) KHÔNG seed. (2) **CÓ 2 env mới** `LUMIBASE_NEGATIVE_CACHE_TTL` / `LUMIBASE_DELIVER_RATE_LIMIT` (không phải settings-DB; default 30s / 1200 req·min⁻¹·IP⁻¹; `0` = tắt). (3) KHÔNG policy/grant. (4) KHÔNG bước wizard. (5) KHÔNG capability. (6) KHÔNG migration/backfill — thuần runtime/middleware; instance cũ nhận hành vi ngay. Site-id guard cố ý rộng hơn nanoid(21) sau survey (`__default__`, `site-a`, …). |
| 36 | lumibase-table-prefix | v0.13.x | Toàn bộ 80 bảng hệ thống mang tiền tố `lumibase_`; lịch sử migration gộp thành một init `0000_lumibase_init`. Xem [ADR-010](../../docs/en/architecture/decisions/adr-010-lumibase-table-prefix.md) | done | — | 2026-07-01. (1) KHÔNG seed mới — setup wizard dùng Drizzle table objects nên không đổi code. (2) KHÔNG feature-flag/settings key. (3) KHÔNG policy/grant DB mới. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability mới. (6) **KHÔNG backfill — greenfield, CHỈ cài mới**: không có đường nâng cấp từ DB pre-prefix; DB cũ phải drop & tạo lại. Cài mới: `migrate` áp `0000_lumibase_init` (tạo bảng đã prefix) rồi `psql -f rls-policies.sql`. Verified Postgres 16 scratch: 82 bảng `lumibase_*`, 0 trần, GIN/partial index + 3 CHECK đầy đủ, `generate` báo no-drift, RLS 29 policy idempotent, FK/CHECK enforce, `pnpm test` xanh toàn workspace |
| 2 | content-os | v0.5.0 | Tạo row `contentOs` flags mặc định (all false) trong `settings` | done | tasks.md G.2 | Done 2026-06-13: insert trong setup tx (bước 8c). Instance cũ: row vắng vẫn đọc all-OFF — không cần backfill |
| 3 | content-os | v0.5.0 | Baseline autonomy grants kèm `evidence: { source: 'setup_bootstrap' }` | done | tasks.md G.3 | Done 2026-06-13: L1 (PROPOSE) cho mọi (role, capability) seed — L1 là mức duy nhất không bao giờ NÂNG quyền dangerous skill dùng chung capability (grant áp cho cả hai context safe/dangerous). CỐ Ý không backfill instance cũ (sẽ siết safe từ L2 xuống L1) |
| 4 | content-os | v0.5.0 | Seed default constitution draft khi setup | done | tasks.md G.4 | Done 2026-06-13: seed "Baseline Constitution v1" status `draft` (bước 9c) — draft không có tác động runtime; template `BASELINE_CONSTITUTION_TEMPLATE` (3 rules, tất cả `blocking: false` — report-only kể cả khi activate). Không cần bước UI wizard. Instance cũ: không backfill, operator tạo qua Mission Control |
| 5 | admin-setup-wizard | v0.4.x | Persist lockout policy vào `settings` (hiện chỉ nằm trong audit metadata) | done | tasks.md G.5 | Done 2026-06-13: chốt open-question-8 — row `login_security_policy` dưới site `__default__`; reader (`loadLockoutPolicyFromSettings`) lookup theo key bất kể siteId nên không prejudge multi-tenancy |
| 6 | ai-first-cms-engine | v0.3.x | Đã rà soát: `ai_approvals` không cần seed; HITL hoạt động không cần config setup | n/a | — | Rà soát 2026-06-13 |
| 7 | clickhouse-cdc | v0.4.x | Đã rà soát: CDC pipeline registry cấu hình qua env/API riêng, không thuộc setup wizard | n/a | — | Rà soát 2026-06-13 |
| 8 | content-os-ui | v0.5.x | Đã rà soát (gồm task 15 rollout switchboard + phase 3 tasks 16-20 agents/intent-lifecycle/planner/evaluate/promotion-check): UI-only trên endpoint + settings row sẵn có — không yêu cầu khởi tạo mới | n/a | — | Rà soát 2026-06-13 |
| 9 | studio-ops-ui | v0.5.x | Đã rà soát: UI-only trên `/materialize`, `/tm`, `/marketplace/publish` sẵn có — không seed, không flag, không bước wizard mới | n/a | — | Rà soát 2026-06-13 |
| 10 | admin-setup-wizard | v0.6.x | Seed RBAC role `Administrator` (`adminAccess=true`) và bind bootstrap admin qua `user_roles` trong setup transaction | done | — | Done 2026-06-14: bước 9a trong setup tx, `systemKey='administrator'` idempotent qua `roles_site_system_key_unique`. Trước fix, bootstrap user không có role nào → PermissionService resolve `admin=false` → mọi schema/items request 403 ("Failed to load collections"). Thoả requirements.md Req 3. **Cần backfill** instance đã setup trước v0.6.x — xem Lưu ý backfill |
| 11 | admin-setup-wizard | v0.6.x | Đã rà soát: Studio standalone (Cloudflare Pages) gọi `GET /setup/state` cross-origin. Fix là deploy/connectivity (`VITE_API_URL` + `CORS_ALLOWED_ORIGINS`), không seed/flag/policy/bước wizard/capability/backfill DB | n/a | — | Rà soát 2026-06-14: studio mặc định same-origin nên `fetchSetupState` fail trên studio.lumibase.dev → "Couldn't reach the server". Sửa bằng base URL build-time (`getApiBaseUrl`) + CORS; không có trạng thái khởi tạo trong DB |
| 12 | site-settings | v0.6.x | Mở rộng bảng `sites` (identity/branding/theme/customCss) + endpoint `GET/PATCH /api/v1/site` + Studio → Settings → Site. Setup ghi `displayTitle/siteUrl/defaultLanguage` thẳng vào `sites` (bước 8a) | done | — | Done 2026-06-14. (1) Không seed riêng: row `sites` đã do setup tạo, cột mới có default. (2) Không feature-flag. (4) Không bước UI wizard mới (config sau setup); setup chỉ persist identity sẵn thu thập vào `sites`. (6) Migration `0028_site_configuration` idempotent (`ADD COLUMN IF NOT EXISTS`), cột nullable/có default → instance cũ KHÔNG cần backfill; identity cũ vẫn đọc được từ key `project_configuration`. Per-user theme override là enhancement sau |
| 13 | admin-setup-wizard | v0.6.x | Đã rà soát: thêm field `loginStallMs` (Directus `LOGIN_STALL_TIME` parity, default 500ms) vào Lockout_Policy — stall response cho 2 nhánh `INVALID_CREDENTIALS` trong `/auth/login`. Không seed/flag/bước wizard/capability/backfill mới | n/a | — | Rà soát 2026-06-15. (1)(3) Không seed/row riêng: field sống trong policy, `parseLockoutPolicy` fill default 500ms, fallback `STANDARD_LOCKOUT_POLICY` cũng có 500ms. (2) Dùng settings key `login_security_policy` sẵn có (gap #5), không phải key mới. (4) CỐ Ý chưa thêm bước UI wizard — admin chỉnh qua security settings API; Studio form schema chỉ khai báo field optional để policy mang sẵn `loginStallMs` không bị strip khi round-trip. (5) Không capability mới. (6) KHÔNG cần backfill: instance cũ thiếu field → codec fill 500ms khi đọc; thiếu row → fallback preset. Bounds `[0, 5000]` ms, `0` = tắt |
| 14 | (security hardening) | v0.6.x | Đã rà soát: XSS sanitisation (DOMPurify ở Studio WYSIWYG/markdown) + extension hardening (bundleUrl schema, hook timeout, error boundary). Code-only; không seed, không settings key, không bước wizard, không capability. Dựa trên env `EXTENSION_BUNDLE_ORIGINS` **đã tồn tại** từ trước — không phải biến mới. Không cần backfill | n/a | — | Rà soát 2026-06-15 |
| 16 | lumibase-firebase-sync | v0.6.x | Đã rà soát: extension sync content (`items`) sang Firebase (Firestore/RTDB). Pipeline cấu hình qua API riêng (`POST /api/v1/firebase-sync/pipelines`) per-site, giống clickhouse-cdc (#7) — không thuộc setup wizard. Credentials Firebase do operator nhập lúc tạo pipeline (mã hoá AES-GCM bằng `ENCRYPTION_KEY` **đã tồn tại**) | n/a | — | Rà soát 2026-06-17. (1) Không seed: bảng `lumibase_firebase_sync_pipelines`/`_log` rỗng khi khởi tạo, pipeline tạo theo nhu cầu. (2) Không feature-flag: tính năng luôn sẵn, chỉ chạy khi có pipeline `active`. (3) Không policy/grant: gated bằng `requireSiteAdmin` sẵn có. (4) Không bước UI wizard. (5) Không capability mới. (6) KHÔNG cần backfill: migration `0029_lumibase_firebase_sync` chỉ tạo bảng mới (`CREATE TABLE IF NOT EXISTS`), instance cũ không có pipeline → no-op. Real-time sync hook trong ItemService chỉ kích hoạt khi `ENCRYPTION_KEY` được cấu hình |
| 15 | admin-setup-wizard | v0.6.x | Simple-wizard bước 2 redesign: (a) seed RBAC role `Member` (`systemKey='member'`, `adminAccess=false`, `appAccess=true`) trong setup transaction — chỉ khi có invite role=member; (b) tạo user `status='invited'` cho mỗi teammate được mời trong cùng setup tx, bind vào site `__default__` qua `user_sites`; (c) `POST /api/v1/setup/complete` nhận thêm field optional `invites[]` ({email, role:'admin'\|'member'}, max 20) và trả `invitedCount` | done | PLAN-simple-step2-redesign.md | Done 2026-06-15: bước 9a-bis trong setup tx (service.ts). Member role idempotent qua `roles_site_system_key_unique`. Invitee bind qua `user_sites` (verified: auth middleware + Users list + PermissionService primary-role đều đọc bảng này; `user_roles` là secondary path, không bắt buộc). Atomic — invite fail thì rollback cả admin (Req 1.5). Audit `user_invited` mỗi invite (best-effort post-commit). **KHÔNG cần backfill**: invites là tùy chọn lúc setup, instance cũ không bị ảnh hưởng; Member role chỉ seed khi cần. Wizard bước 2 đổi 3-card radio → dropdown preset + Docs link + section mời user; bước cuối thêm link Docs |
| 45 | (deployment) | v0.6.x | Đã rà soát: thêm môi trường shared-domain `dev`/`staging`/`demo` — Studio (Pages) + CMS (Worker routes) cùng hostname `<env>.lumibase.dev`, SPA gọi `/api` same-origin (`VITE_API_URL=""`, `CORS_ALLOWED_ORIGINS=""`). Code/config + CI only | n/a | — | Rà soát 2026-06-16. Liên quan #11 (split-origin connectivity) nhưng đây là biến thể same-origin cho non-prod. (1)(3) Không seed/row/policy — `LUMIBASE_ENV="staging"` cho cả 3 (non-dev, non-prod): auth vẫn enforce, không dev-auth bypass. (2) Không feature-flag mới; `LUMIBASE_RELEASE_CHANNEL` chỉ là nhãn env. (4) Không bước wizard mới. (5) Không capability mới. (6) KHÔNG backfill: thay đổi nằm ở `apps/cms/wrangler.toml` routes + 2 workflow CI (`deploy-cms.yml`, `deploy-studio-env.yml`) + docs `shared-domain-environments.md`; production split-origin giữ nguyên. Cloudflare Worker/Pages/DNS/secrets tạo thủ công (xem docs) |
| 17 | (extensions system) | v0.6.x | Đã rà soát: cơ chế auto-detect extension từ source folder (`@lumibase/extensions` discovery util + Zod manifest schema `ExtensionManifestSchema` trong `@lumibase/shared/schemas`), 6 extension mẫu trong submodule `extensions/` (interface/display/panel/module/hook/endpoint), dev-only Vite plugin `lumibase:dev-extensions` cho Studio, và workflow CI `sync-extensions-submodule.yml` auto-bump con trỏ submodule. Code/config only — KHÔNG đụng bảng `extensions` DB hay install flow hiện có | n/a | — | Rà soát 2026-06-16. (1)(3) Không seed/row/policy: discovery đọc manifest từ filesystem ở DEV; bảng `extensions` (DB-backed, R2 bundle) cho production giữ nguyên, không seed mặc định. (2) Không feature-flag mới; dev-loader gated bằng `import.meta.env.DEV` (no-op ở production build). (4) Không bước UI wizard mới. (5) Không capability mới — extension vẫn khai báo capabilities trong manifest và admin grant như cũ. (6) KHÔNG backfill: instance cũ không bị ảnh hưởng; production vẫn load extension qua API/DB. Submodule auto-sync chỉ bump con trỏ trên `main` repo cha, không ghi vào DB |
| 18 | email-service | v0.6.x | Email service dùng chung + template/layout store (`email_templates`, `email_layouts`) + module `/api/v1/email/*` + trang Studio Settings → Email. Email mời teammate (best-effort) khi gọi `POST /api/v1/users/invite` | n/a | — | Rà soát 2026-06-16. (1) KHÔNG seed: template/layout là site-scoped do operator tạo; luồng invite có **fallback message dựng sẵn** (`modules/email/invite.ts`) nên hoạt động out-of-box mà không cần seed row (seed SQL không thể thỏa `site_id NOT NULL` chung). (2) KHÔNG feature-flag mới trong `settings`; cấu hình email hoàn toàn qua **env** (`LUMIBASE_SMTP_URL`, `LUMIBASE_MAIL_FROM`, `LUMIBASE_MAIL_REPLY_TO`, `LUMIBASE_MAIL_ENABLED`, `LUMIBASE_RUNTIME`) — đã thêm vào `Bindings`. (3) KHÔNG policy/grant DB. (4) KHÔNG bước UI wizard mới — quản lý sau setup ở Settings → Email; CỐ Ý không gửi invite email trong setup wizard (transport thường chưa cấu hình lúc đó). (5) KHÔNG capability `/setup/capabilities` mới (transport probe nằm ở `/email/capabilities`). (6) KHÔNG cần backfill: migration `0029_email_templates` chỉ `CREATE TABLE IF NOT EXISTS` (idempotent); instance cũ thiếu transport → degraded mode (EmailService null) → invite degrade im lặng. Transport tái dùng Nodemailer + MailChannels, không thêm dependency |
| 19 | (graphql api) | v0.7.x | Đã rà soát: thêm GraphQL endpoint `POST/GET /api/v1/graphql` (GraphQL Yoga) cho content items — query + mutation. Mount trong sub-app `api` đã có sẵn chuỗi middleware tenant/auth/RLS; resolver delegate vào `ItemService`. Schema build động per-site từ `collections/fields` runtime. SDK thêm adapter `.with(graphql())`. Xem ADR-009 + `docs/en/api/graphql-api-spec.md` | n/a | — | Rà soát 2026-06-17. (1)(3) Không seed/row/policy: schema sinh từ collections/fields sẵn có, không bảng/row khởi tạo mới; permission dùng nguyên Policy DSL hiện hành qua ItemService. (2) Không feature-flag mới; GraphiQL chỉ bật khi non-production (đọc `LUMIBASE_ENV`/`LUMIBASE_DEV_AUTH` sẵn có). (4) Không bước UI wizard mới. (5) Không capability mới. (6) KHÔNG backfill: endpoint là surface mới chồng lên dữ liệu hiện có, instance cũ không cần khởi tạo gì — collection nào tồn tại sẽ tự xuất hiện trong schema |
| 26 | json-field-search | v0.9.x | Đã rà soát: nested JSONB path filter + containment/key-existence operators trong `buildFilter`/`fieldExpression`. CHỈ sửa query builder. KHÔNG bảng/seed/settings/wizard/capability/migration/backfill | n/a | — | Rà soát 2026-06-22. (1) Không seed: chỉ mở rộng filter builder. (2) Không settings key — limits là hằng số code. (3) Không policy/grant: dùng permission/scope hiện hữu. (4) Không bước wizard. (5) Không capability flag. (6) KHÔNG migration/backfill: tận dụng GIN index `items_data_gin_idx` sẵn có cho `@>`/key-existence; path-extract (`#>>`) chạy seq-scan (chấp nhận v1, index theo path là tuỳ chọn vận hành). Additive — filter cũ byte-compatible |
| 25 | fk-dependent-records | v0.9.x | Đã rà soát: FK dependent-records handler (reverse resolver + 409 DEPENDENT_RECORDS_EXIST + resolve endpoints + Studio dialog). KHÔNG bảng mới (tái dùng `relations`/`items`), KHÔNG migration, KHÔNG seed/settings/wizard/capability/backfill | n/a | — | Rà soát 2026-06-22. (1) Không seed: không bảng mới — đảo chiều logic quan hệ sẵn có. (2) Không settings key. (3) Không policy/grant DB: thao tác trên dependents kiểm quyền trên `manyCollection` qua PermissionService sẵn có. (4) Không bước UI wizard. (5) Không capability flag. (6) KHÔNG migration/backfill: `relations.on_delete` đã tồn tại, chỉ thêm enforcement tầng ứng dụng + endpoints/Studio dialog. Instance cũ không đổi cho tới khi gọi endpoint mới |
| 24 | external-jwt-auth | v0.9.x | Đã rà soát: trusted external JWT issuers (bảng `auth_external_issuers` + `/api/v1/admin/auth/issuers` + verify trong withAuth). KHÔNG seed, KHÔNG settings key, KHÔNG bước wizard, KHÔNG capability, KHÔNG backfill | n/a | — | Rà soát 2026-06-22. (1) Không seed: bảng rỗng khi init; issuer do admin đăng ký theo nhu cầu (như clickhouse-cdc #7, firebase-sync #16). (2) Không settings key: cấu hình sống trong bảng, không env bắt buộc mới. (3) Không policy/grant DB mặc định: admin endpoints gated `requireSiteAdmin`; external principal lấy quyền từ roleMapping → roles hiện hữu (default-deny, không cửa hậu admin). (4) Không bước UI wizard — cấu hình sau setup qua admin API. (5) Không capability flag. (6) KHÔNG backfill: migration `0042_auth_external_issuers` (đánh số lại 0034→0042 vì main đã tới 0039) chỉ CREATE TABLE IF NOT EXISTS; instance cũ không có issuer → nhánh external skip mọi token → auth hiện tại (CF Access/custom JWT) giữ nguyên. Lưu ý: migration thêm 1 bảng (hand-written 0012+) |
| 23 | save-default-preference | v0.9.x | Đã rà soát: default save action (per-user `users.preferences.saveAction` + site `sites.default_save_action`). KHÔNG seed, KHÔNG settings key (cột typed có default), KHÔNG bước wizard, KHÔNG capability, KHÔNG backfill | n/a | — | Rà soát 2026-06-22. (1) Không seed: `users.preferences` mặc định `{}`; `sites.default_save_action` có DB default `'stay'`. (2) Không settings row — là cột typed trên `sites`. (3) Không policy/grant: preferences self-service gated bằng auth; site config gated `requireSiteAdmin` sẵn có. (4) Không bước UI wizard — cấu hình sau setup (Settings → Account / Settings → Site). (5) Không capability. (6) KHÔNG backfill: cột `NOT NULL DEFAULT 'stay'` (khớp hành vi editor hiện tại — instance cũ không đổi cảm nhận); user thiếu key rơi về site default qua `resolveSaveAction`. Migration `0041_save_default_preference` (đánh số lại 0033→0041 vì main đã tới 0039) additive `ADD COLUMN IF NOT EXISTS` (hand-written 0012+) |
| 22 | content-releases | v0.9.x | Đã rà soát: Content Releases (hai bảng `releases`/`release_items` + `/api/v1/releases` + scheduler sweep). KHÔNG seed, KHÔNG settings key, KHÔNG bước wizard, KHÔNG capability, KHÔNG backfill | n/a | — | Rà soát 2026-06-22. (1) Không seed: cả hai bảng rỗng khi init; Release do người tạo. (2) Không feature-flag. (3) Không policy/grant DB — gated bằng auth sẵn có; editorial gate tái dùng. (4) Không bước UI wizard — quản lý Release thuộc AppShell vận hành, không phải first-time setup. (5) Không capability flag (publish-by-agent là tương lai). (6) KHÔNG backfill: migration `0040_content_releases` (đánh số lại 0032→0040 vì main đã tới 0039) chỉ `CREATE TABLE IF NOT EXISTS` + guarded FK (idempotent), instance cũ thiếu hai bảng → no-op; dữ liệu `items`/`revisions` không đổi. Lưu ý duy nhất: **migration thêm 2 bảng** (hand-written, 0012+) |
| 46 | code-first-config | v0.9.x | Đã rà soát: Config Manifest export/diff/apply (`/api/v1/config/export`, `/api/v1/config/import`) + CLI. Đọc/ghi schema config sẵn có (collections/fields/relations/settings/webhooks), KHÔNG bảng mới, KHÔNG seed, KHÔNG bước wizard, KHÔNG backfill | n/a | — | Rà soát 2026-06-22. (1) Không seed: chỉ đọc/ghi config đã tồn tại; manifest rỗng trên instance mới là hợp lệ. (2) Không feature-flag mới. (3) Không policy/grant DB: endpoint gated bằng `requireSiteAdmin()` (PermissionService.bundle admin) sẵn có. (4) Không bước UI wizard mới. (5) Không capability mới. (6) KHÔNG backfill: endpoint là surface mới chồng lên config hiện hữu — export hoạt động ngay với mọi instance, apply là hành động chủ động của operator. Không migration (tái dùng bảng `collections`/`fields`/`relations`/`settings`/`webhooks`) |
| 47 | regulated-content-readiness | v0.7.x | Đã rà soát: tập năng lực dữ liệu nhạy cảm (field encryption hardening + KeyProvider, field classification + access audit, content scheduling, editorial workflow, erasure/retention/SAR, SEO delivery, **envelope mode**). Tất cả **opt-in, mặc định off** → hành vi Tier 1 không đổi (Req 16.5). KHÔNG seed, KHÔNG bước wizard, KHÔNG backfill DB bắt buộc | done | regulated-content-readiness/tasks.md | Rà soát 2026-06-17, cập nhật 2026-06-18. (1) Không seed: `encryption_keys` dùng KeyProvider fallback `v0` từ `ENCRYPTION_KEY` (Req 4.4); các bảng `field_access_log`/`content_reviews`/`erasure_requests` rỗng lúc khởi tạo. (2) **Env/secret mới** (opt-in, không bắt buộc): `ENCRYPTION_KEY_<id>` + `ENCRYPTION_ACTIVE_KEY_ID` (key rotation = KEK provider). Settings opt-in: **`encryption.envelope`** (per-site, mặc định `{enabled:false}`), per-collection `meta.degradedReadOnFailure`/`editorialWorkflow`/`requireSeparateReviewer`, `retention.policies[]`, `erasureDualControl`, `seo.jsonLdType`. `validateProductionConfig` validate key base64 AES-GCM + active key id resolve. (3) **Envelope mode (task 3.6)**: điều khiển bằng **setting có UI** (`encryption.envelope`), KHÔNG dùng env var (env `LUMIBASE_ENVELOPE_ENCRYPTION` không còn là control chính của hot-path). Đổi setting qua `POST /api/v1/admin/encryption/envelope` yêu cầu **step-up password**; kích background migration (queue mới **`encryption-envelope-migration`**, batched/resume-cursor/idempotent) + drain inline bounded; audit event mới **`envelope_mode_changed`**. KEK vẫn từ env/secret (không lưu DB). (4) Không bước wizard — cấu hình sau setup (classification, key rotation qua `/api/v1/admin/encryption/keys/rotate`, envelope qua Studio → Settings → Encryption). (5) Capability flag tuỳ chọn, chưa thêm. (6) Migration `0031_regulated_content_readiness` additive + `IF NOT EXISTS`/duplicate-object guard (Req 16.4); cột mới (`items.dek_wrapped` nullable) → KHÔNG backfill. Ciphertext cũ đọc qua `v0` không-AAD (Req 3.2); record envelope tự mô tả qua `dek_wrapped` nên đọc đúng kể cả sau khi tắt setting (miễn KEK còn) |
| 21 | mcp-full-coverage | v0.10.x | Đã rà soát: mở rộng MCP phủ toàn bộ tính năng trên cả hai surface — standalone server `@lumibase/mcp-server` (~80 tool REST-passthrough) + governed skills qua `AISecureHarness` (relations, RBAC roles/policies, intents, flows, api-keys, users/teams, settings/translations/webhooks, extensions). Thêm `AccessService`/`ConfigService`/`ExtensionsService` + util `api-key-token`; cờ `dangerous` cho `SkillDefinition`; capability namespace mới `access:*`/`api-keys:*`/`users:*`/`teams:*`/`config:*`/`intents:*`/`flows:*`/`extensions:*` | n/a | — | Rà soát 2026-06-22. (1) KHÔNG seed: tool/skill là surface chồng lên dữ liệu & endpoint hiện có; standalone server không chạm DB. (2) KHÔNG feature-flag mới — governed endpoint vẫn gated bằng `contentOs.mcp` (gap #2, default OFF). (3) KHÔNG policy/grant DB khởi tạo: capability mới resolve mặc định L1 (PROPOSE) qua autonomy resolver (gap #3) khi chưa có grant — write/delete luôn vào HITL. (4) KHÔNG bước UI wizard mới. (5) Capability mới do RBAC policy cấp như mọi capability khác — không thêm bước `/setup/capabilities`. (6) KHÔNG cần backfill: không migration, không bảng/cột mới. Mọi tool destructive yêu cầu `confirm:true`; irreversible (`deleteRole`/`deletePolicy`/`deleteRelation`/`revokeApiKey`/`removeUser`) hard-cap L2. NDJSON backup/restore + marketplace install/publish + binary media giữ standalone-only |
| 21b | (mcp-path-hardening) | v0.13.x | Đã rà soát: hardening tham số path của `@lumibase/mcp-server` (mở rộng PR #150 / entry #21). Thêm `mediaKeySchema`/`encodeMediaKey` vào `tools/path.ts`; validate (`idPathSegmentSchema`/`collectionNameSchema`/`fieldNameSchema`) + `encodePathSegment` cho mọi segment động trong `_crud.ts` (factory dùng chung) + `items`/`collections`/`fields`/`users-teams`/`api-keys`/`access`/`agent`/`admin`/`relations`/`extensions`/`search-media`/`content-config` (settings key: dùng `idPathSegmentSchema` thay `encodeURIComponent` để chặn `.`/`..` mà `encodeURIComponent` bỏ sót — route `/settings/:key` single-segment, key dạng `contentOs.mcp` vẫn hợp lệ). Đóng path-traversal/confused-deputy (input bị thao túng normalize sang endpoint anh em). Code-only, standalone server, KHÔNG chạm DB | n/a | — | Rà soát 2026-06-30. (1) KHÔNG seed. (2) KHÔNG feature-flag/settings key mới. (3) KHÔNG policy/grant DB — chỉ siết validation/encoding tham số tool; không đổi capability hay autonomy. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: không migration/bảng/cột. `mediaKeySchema` cố ý cho phép `/` nội bộ (key đa-segment) nhưng chặn `..`/leading-`/`/`\` — mirror guard server `isInvalidKey` (`routes/media.ts`); `encodeMediaKey` encode từng segment, giữ separator để route `:key{.+}` vẫn khớp |
| 48 | translation-memory-ui | v0.6.x | Đã rà soát: backend TM (`PATCH/DELETE /api/v1/tm/:id` + pagination GET `/tm` + hằng `TM_DEFAULT_THRESHOLD`) đã có từ trước. Bổ sung FE + SDK: SDK namespace `tm.*`, TM management page (edit/entrySource filter/pagination), TM suggestion popover + side-by-side Translation mode + completion % trong item editor, learn-on-save. **Settings key mới opt-in `translations.learnTm`** (mặc định BẬT khi vắng). Code-only trên bảng `translation_memory` + `settings` sẵn có. Không seed, không bước wizard, không capability, không backfill | n/a | — | Rà soát 2026-06-18, cập nhật 2026-07-06 (implement FE/SDK + learnTm). (1) KHÔNG seed: bảng TM rỗng lúc khởi tạo, entry do người dùng/learn-on-save tạo. (2) **Settings key `translations.learnTm`** (per-site, `{enabled:boolean}`): row vắng ⇒ FE đọc mặc định `true` (fallback trong code, không cần backfill/seed). Không phải feature-flag gating — chỉ bật/tắt việc học TM khi lưu. (3) KHÔNG policy/grant DB: endpoint TM dùng chuỗi middleware tenant/auth + permission translations/admin sẵn có; ghi TM scope `siteId`. (4) KHÔNG bước UI wizard mới — learnTm cấu hình sau setup (Settings); mặc định BẬT là hành vi hợp lý, không cần hỏi lúc setup. (5) KHÔNG capability mới. (6) KHÔNG backfill: không migration/bảng/cột; locale list tái dùng settings key `locales` (`value.supported`) đã có, KHÔNG thêm cột `sites.locales`. Content item lưu đa ngữ qua shape `data[field][locale]` sẵn có (interface `translatable-text`) — KHÔNG cơ chế lưu mới |
| 49 | insights-dashboard | v0.6.x | Bảng mới `dashboards` + `panels` (migration `0033_insights_dashboards`, viết tay — đánh số sau main `0031_regulated_content_readiness`). Service aggregate an toàn (whitelist field + scope siteId, không raw SQL). Endpoint `/api/v1/dashboards`. (1) Không seed dashboard mặc định khi setup — dashboard tạo theo nhu cầu. (2) Không feature-flag. (4) Không bước wizard mới. (6) Migration idempotent (`CREATE TABLE IF NOT EXISTS`), bảng mới rỗng → KHÔNG cần backfill | done | — | Done 2026-06-18: backend + FE module `insights` + nav. Migration chưa chạy (không có Postgres local) — verify qua schema typecheck. CỐ Ý không seed dashboard mẫu |
| 50 | content-versioning | v0.6.x | Bảng mới `content_versions` (migration `0034_content_versions`, viết tay — đánh số sau main). Named draft branches; promote qua `ItemService.patch` (ghi revision). (1) Không seed. (2) Không flag. (4) Không bước wizard. (6) Migration idempotent, bảng mới rỗng → KHÔNG cần backfill | done | — | Done 2026-06-18: schema + service + 7 endpoint trên items.ts + FE tab "Versions" trên item editor. Migration chưa chạy (không có Postgres local) — verify qua schema typecheck |
| 51 | visual-flow-builder | v0.6.x→v0.18.x | Đã rà soát (hoàn tất 2026-07-06): shared graph converter/validator (2026-06-18) + toàn bộ trigger: `GET /flows/operations` (registry), graph/cron gate khi POST/PATCH, **event trigger** (ItemService → queue mới **`flow-events`** → consumer chạy flow + ghi `flow_runs`), **schedule trigger** (cron 5-field tự viết, sweep trong `runSchedulerTick`, advance `next_run_at` trước enqueue), **webhook trigger** (`POST /flows/:id/trigger`, token per-flow constant-time, bypass `withAuth`), run detail `GET /flows/:id/runs/:runId` + editor canonical-graph/palette-registry/RunHistoryPanel. KHÔNG bảng/migration mới (tái dùng `flows`/`flow_runs` + cột `next_run_at` sẵn có) | n/a | — | Rà soát 2026-06-18, cập nhật 2026-07-06. (1) KHÔNG seed flow mẫu. (2) KHÔNG feature-flag/settings key. (3) KHÔNG policy/grant DB — flows CRUD sau `requireFlowAdmin`; webhook trigger auth bằng token trong `triggerOptions` (không session). (4) KHÔNG bước wizard. (5) KHÔNG capability mới. (6) KHÔNG backfill. **Lưu ý vận hành:** queue mới `flow-events` — Docker/Node tự đăng ký consumer trong `serve.ts` (`registerFlowEventWorker`); CF Workers cần wire queue consumer tương ứng khi bật event/schedule flows trên CF (schedule sweep đi cùng `runSchedulerTick` hiện hành) |
| 52 | (search hardening) | v0.8.x→v0.11.x | Đã rà soát: search backend hardening — (a) tenant-isolate index name `{siteId}__{collection}` ở cả index/search/delete + validate collection thuộc site trong `/api/v1/search`; (b) thêm consumer cho queue `content-indexing` (`registerContentIndexingWorker` trong `serve.ts`) vì trước đó jobs enqueue không ai xử lý; (c) làm giàu document (`_collection/_title/_updatedAt`); (d) `configureIndex` áp stop words tiếng Việt + typo tolerance (bỏ dấu là built-in MeiliSearch, không cần config). **Mở rộng (PR sau)**: (e) `lumibase reindex` CLI để build lại index từ DB; (f) cross-collection search (bỏ `collection` → fan-out toàn site, cap 20); (g) SDK `search()`; (h) Studio content `SearchPalette` (nút TopBar + **⌘P** — tách khỏi nav command palette ⌘K của keyboard-shortcuts); (i) gỡ param `?search=` dead-code khỏi `/items` + GraphQL list | n/a | — | Rà soát 2026-06-20, cập nhật 2026-06-27. (1) KHÔNG seed: index MeiliSearch tạo lazy khi document đầu tiên được index; `configureIndex` idempotent áp 1 lần/index trong worker; reindex CLI chạy thủ công khi cần. (2) KHÔNG feature-flag mới; search bật khi `MEILISEARCH_HOST/_API_KEY` (env đã tồn tại). (3) KHÔNG policy/grant DB — `/search` dùng chuỗi middleware tenant/auth sẵn có. (4) KHÔNG bước UI wizard mới (SearchPalette là UI runtime, không phải setup step). (5) KHÔNG capability mới. (6) Backfill: instance cũ có index theo tên-trần (chưa scope siteId) sẽ thành rác sau khi đổi scheme — chạy `lumibase reindex` để build lại index đã scope; index cũ vô hại (không ai query tới) nhưng nên dọn thủ công trong MeiliSearch |
| 27 | wysiwyg-extension-data-types | v0.11.x | Đã rà soát: bổ sung bộ field interface Directus-parity (selection/text/presentation/group/map/tree) + **M2A Builder** (`relation-m2a`) liên kết nhiều collection với nút **Create new / Add existing** (`RelationDrawer`) + field grouping/width trong item form. Studio frontend-only | n/a | — | Rà soát 2026-06-26. (1) KHÔNG seed: interface đăng ký runtime trong `apps/studio/src/modules/content/interfaces/registry.tsx`. (2) KHÔNG feature-flag. (3) KHÔNG policy/grant — form dùng per-field permission sẵn có. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability mới. (6) KHÔNG backfill: field metadata (`interface`/`options`/`special`/`group`/`width`) đã có sẵn cột trong bảng `fields` từ trước — không migration/schema change. Tạo item mới qua drawer dùng `client.items().create()` hiện hành |

| 20 | (compliance docs) | v0.7.x | Đã rà soát: bộ tài liệu song ngữ `docs/{en,vi}/compliance/` phân tích quyền người dùng (EU GDPR/ePrivacy, US CCPA-CPRA/CAN-SPAM, VN PDPD/ANM/giấy phép nội dung, Google/Apple) + gap-analysis ↔ tính năng + checklist triển khai. Docs-only + 1 category sidebar `apps/docs/docs.config.json` + cross-link. KHÔNG đụng schema/route/seed | n/a | — | Rà soát 2026-06-18. (1) Không seed: không bảng/row khởi tạo. (2) Không feature-flag/settings key mới. (3) Không policy/grant DB. (4) Không bước UI wizard mới. (5) Không capability `/setup/capabilities` mới. (6) KHÔNG backfill: chỉ thêm file Markdown + 1 category sidebar + cross-link từ README/user-management/permissions-rbac; không thay đổi runtime. Checklist trong `implementation-checklist.md` (P0 erasure/export/consent/unsubscribe) là **đề xuất tương lai** — khi triển khai sẽ tạo dòng registry riêng |
| 53 | (consent management) | v0.8.x | Triển khai P0.3 từ compliance checklist (#20): bảng `user_consents` + `ConsentService` + self-service endpoint `GET /api/v1/me/consents` và `PUT /api/v1/me/consents/:type` (4 loại: marketing/analytics/personalization/functional). Mỗi thay đổi ghi audit `consent_granted`/`consent_withdrawn`. RLS site-isolation áp cho bảng mới | n/a | — | Rà soát 2026-06-24. (1) KHÔNG seed: consent tạo theo nhu cầu per-user (mặc định vắng = chưa đồng ý). (2) KHÔNG feature-flag/settings key mới. (3) KHÔNG policy/grant DB — endpoint nằm trong sub-app `api` đã có `withAuth`; chỉ thao tác consent của chính caller. (4) KHÔNG bước UI wizard mới — preference center là enhancement sau. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: migration `0031_user_consents` chỉ `CREATE TABLE IF NOT EXISTS` (idempotent) + thêm `'user_consents'` vào `rls-policies.sql`; instance cũ nhận bảng rỗng, đọc consent vắng = chưa đồng ý. Lưu ý đa tenant: `consent_type` là current-state per `(site_id,user_id,type)`, lịch sử đầy đủ ở `audit_log`. P1.1 (v0.8.x) mở rộng thêm loại `sale_share` (CCPA Do-Not-Sell) — cùng bảng/endpoint, không thay đổi setup |
| 29 | (automated-decision transparency) | v0.8.x | Triển khai P2.2 từ checklist (#20): `AutomatedDecisionsService` + `GET /api/v1/me/automated-decisions` hiển thị revisions agent trên nội dung của user kèm provenance (GDPR Điều 22). Read-only | n/a | — | Rà soát 2026-06-25. (1) KHÔNG seed. (2) KHÔNG flag. (3) KHÔNG policy/grant — trong `api` (đã `withAuth`), chỉ đọc dữ liệu của caller. (4) KHÔNG wizard. (5) KHÔNG capability mới. (6) KHÔNG backfill: read-only trên `revisions`/`items` hiện có, không bảng/migration mới |
| 54 | (data classification + redaction) | v0.8.x | P2.4+P2.3 — **đã hợp nhất vào `regulated-content-readiness` của main** khi merge: `fields.classification` (`none`/`internal`/`pii`/`phi`, `pii`/`phi` bắt buộc encrypted) + masking gốc + `field_access_log`. Bản cột/redaction riêng của nhánh này đã gỡ; chỉ giữ alignment enum ở field-create API (`collections.ts`) | n/a | — | Rà soát 2026-06-25 (sau merge main). Classification + masking do main cung cấp |
| 55 | (restriction of processing) | v0.8.x | Triển khai P2.1 từ checklist (#20): bảng `processing_restrictions` + `RestrictionService` + tự phục vụ `GET`/`PUT /api/v1/me/restriction`. Audit `processing_restricted`/`processing_unrestricted`. RLS áp cho bảng mới | n/a | — | Rà soát 2026-06-25. (1) KHÔNG seed. (2) KHÔNG flag/settings key. (3) KHÔNG policy/grant DB — tự phục vụ trong `api` (đã `withAuth`). (4) KHÔNG bước wizard mới. (5) KHÔNG capability mới. (6) KHÔNG backfill: migration `0034` chỉ `CREATE TABLE IF NOT EXISTS` + thêm `'processing_restrictions'` vào `rls-policies.sql`. Helper `isRestricted` là hook thực thi; gắn vào agent run/marketing là follow-up |
| 56 | (compliance docs II) | v0.8.x | Triển khai P1.3/P1.4/P2.5 từ checklist (#20): thêm `docs/{en,vi}/compliance/data-map.md`, `data-residency.md`, `dpa-template.md` + 3 mục sidebar `apps/docs/docs.config.json` + link README. Docs-only | n/a | — | Rà soát 2026-06-24. Docs-only: không seed/flag/policy/wizard/capability/backfill. Chỉ thêm file Markdown + sidebar + cross-link |
| 57 | (data retention) | v0.8.x | Triển khai P1.2 từ compliance checklist (#20): `RetentionService` dọn `activity` + `notifications` đã xử lý quá mốc cấu hình + admin `GET`/`POST /api/v1/retention/run`. Audit `retention_pruned` | n/a | — | Rà soát 2026-06-24. (1) KHÔNG seed. (2) Thêm 2 env **mới** `LUMIBASE_ACTIVITY_RETENTION_DAYS`/`LUMIBASE_NOTIFICATION_RETENTION_DAYS` (mặc định không đặt = TẮT, không xoá gì) — operator phải opt-in; không phải settings-key DB nên không cần seed/migration. (3) KHÔNG policy/grant DB — `requireSiteAdmin`. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability mới. (6) KHÔNG backfill: không bảng mới; mặc định tắt nên instance cũ không bị xoá dữ liệu ngoài ý muốn. **Lưu ý:** chưa gắn cron — gọi thủ công hoặc gắn `runScheduledRotation` (follow-up) |
| 58 | (account erasure) | v0.8.x | P0.1 — **đã hợp nhất vào feature `regulated-content-readiness` của main** khi merge: bảng `erasure_requests` (`schema/regulated.ts`) + `services/erasure-service.ts` + admin `/api/v1/admin/erasure` & `/api/v1/admin/sar`. Bản erasure tự phục vụ riêng của nhánh này đã được gỡ để tránh trùng lặp | n/a | — | Rà soát 2026-06-25 (sau merge main). Erasure do main cung cấp; xem setup-impact của `regulated-content-readiness`. Follow-up: thêm luồng tự phục vụ `/me/erasure` chồng lên service admin |
| 59 | (personal-data export) | v0.8.x | Triển khai P0.2 từ compliance checklist (#20): `DataExportService` + endpoint `GET /api/v1/me/data-export` gom hồ sơ/consents/hoạt động/revisions/thông báo của chính người gọi thành JSON tải về. Audit `data_exported` | n/a | — | Rà soát 2026-06-24. (1) KHÔNG seed. (2) KHÔNG feature-flag/settings key. (3) KHÔNG policy/grant DB — endpoint trong `api` (đã `withAuth`), chỉ xuất dữ liệu của caller; loại trừ secret (`passwordHash`/`tfa`). (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability mới. (6) KHÔNG backfill: read-only trên dữ liệu hiện có, không bảng/migration mới |
| 60 | (email suppression) | v0.8.x | Triển khai P0.4 từ compliance checklist (#20): bảng `email_suppressions` + `SuppressionService` + endpoint công khai one-click `GET`/`POST /api/v1/email/unsubscribe` (token JWT ký bằng `JWT_SECRET`) + quản trị `GET`/`POST`/`DELETE /api/v1/email/suppressions`. Send path lọc người nhận `marketing`. Audit `email_unsubscribed`/`email_suppressed`/`email_unsuppressed`. RLS áp cho bảng mới | n/a | — | Rà soát 2026-06-24. (1) KHÔNG seed: danh sách suppression rỗng khi khởi tạo, điền theo nhu cầu. (2) KHÔNG feature-flag/settings key mới — tái dùng `JWT_SECRET` **đã tồn tại** để ký token unsubscribe. (3) KHÔNG policy/grant DB — endpoint công khai dựa trên token (giống share links), quản trị dùng `requireSiteAdmin` sẵn có. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: migration `0032_email_suppressions` chỉ `CREATE TABLE IF NOT EXISTS` (idempotent) + thêm `'email_suppressions'` vào `rls-policies.sql`; instance cũ nhận bảng rỗng. Lưu ý: suppression chỉ lọc email `category: 'marketing'`; email giao dịch (mời, bảo mật) luôn được gửi |

| 28 | deployment-integrations | v0.6.x | Đã rà soát: tích hợp deploy Vercel/Netlify/HTTP — bảng `deployment_targets` + `deployments` (`schema/deployments.ts`), `DeploymentService` + providers + token-vault (token mã hoá qua KeyProvider), `routes/deployments.ts` (`/api/v1/deployments` authenticated + `/api/v1/deployments/webhook` public pre-auth ký request), status-poller cron 30s (serve.ts), Studio Settings page, và 4 AI skills (`listDeploymentTargets`/`listDeployments`/`getDeploymentStatus`/`triggerDeployment`) | có | `deployments:read`, `deployments:write` | Rà soát 2026-06-29 (tách từ nhánh `docs-i18n-en-vi-sync`, cherry-pick `89cd137`). (1) KHÔNG seed: targets do admin tạo sau setup, không default. (2) KHÔNG env bắt buộc lúc setup — token nhập per-target qua Studio; KeyProvider đã tồn tại từ trước (ADR-002). (3) KHÔNG policy/grant DB mới — routes `requireSiteAdmin`/`withAuth` sẵn có; webhook public dựa chữ ký provider. (4) KHÔNG bước wizard mới — cấu hình ở Settings sau setup. (5) **CÓ capability mới** `deployments:read`/`deployments:write` — cần thêm vào role admin (upgrade note CHANGELOG); `triggerDeployment` phân loại `dangerous` → HITL `before_execute` dưới autopilot (ToolRegistry); không agent role mặc định nào có `deployments:write`. (6) KHÔNG backfill: migration `0038_deployment_integrations` chỉ `CREATE TABLE IF NOT EXISTS` (idempotent), instance cũ nhận 2 bảng rỗng. **Migration đánh số lại 0032→0038** vì main đã tới 0037. **RLS:** thêm `'deployment_targets'`,`'deployments'` vào `rls-policies.sql` (site-isolated). **Webhook secret (tùy chọn):** setting per-site `deployment.webhook.<provider>` = `{secret}` — verify chữ ký thật (Vercel HMAC-SHA1, Netlify JWS/HS256) trên raw body; không cấu hình thì webhook bị từ chối, status đồng bộ qua poller |
| 28b | (deployment-flow-ops) | v0.13.x | Đã rà soát: flow operations `deploy:trigger` / `deploy:status` (deployment-integrations Req 5.2, mở rộng #28) trong `flow-service.ts` — auto-deploy khi có content event. Reuse `DeploymentService` (SSRF / encrypted-token / audit guard giữ nguyên); `db`/`siteId`/`keys`/`runId` bơm từ run env qua `routes/flows.ts`; `triggerSource='auto'` + link `runId` cho provenance. Kèm hardening 2c: thêm `/api/v1/flows` vào `CONTROL_PLANE_PATHS` (backstop admin-only, không chỉ dựa `requireFlowAdmin` per-route) + assertion tripwire | n/a | — | Rà soát 2026-07-03. (1) KHÔNG seed: không target/deploy mặc định; flow do operator tạo. (2) KHÔNG env bắt buộc lúc setup — `keys` (KeyProvider) đã có sẵn từ runtime (ADR-002); token vẫn per-target như #28. (3) KHÔNG policy/grant DB mới — reuse capability `deployments:*` của #28; đường vào flow gated `requireFlowAdmin` + control-plane backstop mới. (4) KHÔNG bước wizard mới — flow cấu hình ở Studio sau setup. (5) KHÔNG capability `/setup/capabilities` mới (dùng lại `deployments:read/write` #28). (6) KHÔNG backfill: không migration/bảng/cột — chỉ đăng ký 2 flow handler + siết guard. Debounce/coalescing (Req 5.4, task 10.3) vẫn **Open** — mặc định mỗi event một deploy |
| 61 | custom-domains | v0.13.x | Đã rà soát: custom domains + free `*.lumibase.dev` subdomains qua Cloudflare for SaaS — bảng `lumibase_site_domains` (`schema/domains.ts`), `routes/domains.ts` (`/api/v1/domains` list/create/verify/primary/delete), `CloudflareSaaSClient` (`services/domains/cloudflare-saas.ts`), tenant middleware resolve theo full Host qua KV `site-host:<fqdn>` (`middleware/tenant.ts` + `services/domains/host-cache.ts`), Studio Settings → Domains page, SDK `client.domains.*`. Docs enterprise ở repo `enterprise-core` | n/a | — | Rà soát 2026-06-30 (cập nhật khi merge main: table đổi tên `site_domains`→`lumibase_site_domains` theo prefix mới; migration `0001_site_custom_domains` sau khi main squash history về `0000_lumibase_init`). (1) KHÔNG seed: domains do admin tạo sau setup, không default. (2) **CÓ env tùy chọn** (chỉ ảnh hưởng hosted platform, KHÔNG bắt buộc lúc setup): `CLOUDFLARE_API_TOKEN` (secret), `CLOUDFLARE_ZONE_ID`, `LUMIBASE_SAAS_FALLBACK`, `LUMIBASE_FREE_DOMAIN_SUFFIX` — thiếu token → custom domain rơi về `pending_dns` + hướng dẫn DNS thủ công (self-hosted fallback), feature vẫn hoạt động. (3) KHÔNG policy/grant DB mới — route gated bởi `withAuth`/`withTenant` sẵn có, query scope `siteId`. (4) KHÔNG bước wizard mới — cấu hình ở Settings sau setup. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: migration chỉ `CREATE TABLE IF NOT EXISTS` (idempotent), instance cũ nhận 1 bảng rỗng; cột legacy `lumibase_sites.domain` giữ nguyên (backward-compat). **RLS:** thêm `'lumibase_site_domains'` vào `rls-policies.sql` (site-isolated). **wrangler:** thêm route wildcard `*.lumibase.dev/*` cho free subdomains + vars CF (token là secret out-of-band). **Release prereq:** `CLOUDFLARE_API_TOKEN` secret + zone Cloudflare for SaaS phải tồn tại trước khi deploy hosted |
| 62 | keyboard-shortcuts | v0.6.x | Đã rà soát: hệ phím tắt toàn cục Studio (Cmd/Ctrl+S save-and-stay, Cmd/Ctrl+Alt+S mở Settings, Cmd/Ctrl+K command palette) + UI tùy biến tại Settings → Keyboard shortcuts + endpoint mới `GET/PATCH /api/v1/me/preferences`. Mặc định LumiBase hardcode trong Studio (`lib/keybindings/actions.ts`), override per-user lưu trong `users.preferences.keybindings` (JSONB) | n/a | — | Rà soát 2026-06-24. (1) KHÔNG seed: defaults nằm trong code Studio; `users.preferences` đã tồn tại (core.ts) và mặc định `{}` → merge over defaults. (2) KHÔNG feature-flag: tính năng luôn bật; chưa override thì dùng default keymap. (3) KHÔNG policy/grant DB — endpoint `/me/preferences` chỉ đọc/ghi row của chính user (`eq(users.id, auth.userId)`), gated bởi `withAuth` sẵn có. (4) KHÔNG bước UI wizard mới — cấu hình sau setup ở Settings → Keyboard shortcuts. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG cần backfill: dùng cột `users.preferences` sẵn có, KHÔNG migration; instance cũ thiếu `keybindings` → đọc all-default. SDK thêm `client.me.getPreferences/updatePreferences`; shared `UserPreferencesSchema` validate chord layout-independent (chuẩn hóa từ `event.code`) |
| 63 | (reserved collection prefix) | v0.13.x | Đã rà soát: chặn tạo/đổi tên collection có tên bắt đầu bằng prefix `lumibase_` (do platform sở hữu — bảng CDC/Firebase sync, config nội bộ). Guard đặt ở `SchemaService.ensureName` (`apps/cms/src/services/schema-service.ts`) nên áp đồng nhất cho cả builder (routes `collections.ts`) lẫn AI harness (skill `createCollection`); vi phạm ném `RESERVED_NAME` (HTTP 422). Thêm `.refine()` Zod ở route cho phản hồi sớm + cập nhật mô tả skill `createCollection` | n/a | — | Rà soát 2026-06-30. (1) KHÔNG seed: chỉ là validation guard, không bảng/row khởi tạo. (2) KHÔNG feature-flag/settings key — luôn bật. (3) KHÔNG policy/grant DB. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: không migration/schema change; collection `lumibase_*` do người dùng tạo trước đây (nếu có) không bị ảnh hưởng — guard chỉ chặn tạo/đổi tên MỚI. Prefix khớp đúng các bảng hệ thống hiện hữu (`lumibase_cdc_*`, `lumibase_firebase_sync_*`) |

| 31 | (security hardening) | v0.14.x | Đã rà soát: funnel mọi khởi tạo `ItemService` qua factory RBAC-tường-minh (`item-service-factory.ts`) — `itemServiceForRequest(c)` luôn gắn `permissionCtx`, `itemServiceForSystem(deps, reason)` bắt buộc khai báo `SystemContextReason` cho worker/cron/compliance. Vá lỗ hổng còn sót ở `routes/mcp.ts` (skill AI qua MCP bypass row/field RBAC vì thiếu `permissionCtx`, cùng class lỗi PR #151). Test source-scan `item-service-rbac-context.test.ts` fail CI nếu có `new ItemService(...)` trực tiếp ngoài allowlist. Code + test + docs only | n/a | — | Rà soát 2026-07-02. (1) KHÔNG seed. (2) KHÔNG feature-flag/settings key mới. (3) KHÔNG policy/grant DB — chỉ đảm bảo đường request-scoped luôn truyền `permissionCtx` sẵn có để `PermissionService` enforce đúng như `/items`; không đổi capability/autonomy. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: không migration/bảng/cột; system context (`scheduler`/`background-worker`/`compliance-erasure`) giữ nguyên hành vi fail-open cố ý (quyết định phân quyền đã xảy ra upstream: cron config / veto đã chốt / agent run governed / admin+dual-control). Bảng phân loại 9 call site + checklist trong `docs/vi/security/runtime-security-guards-plan.md` |
| 30 | push-notifications | v0.7.x | Đã rà soát: in-app realtime (SiteRoom `notification` frame) + Web Push (VAPID) cho 4 nhóm sự kiện agent (approval/veto/incident/goal-run) + UI Settings → Notifications (check/verify/guide) + script `push-test.mjs`. Bảng mới `push_subscriptions`, routes `/api/v1/push/*`, broadcaster `emitAgentNotification`. KHÔNG seed, KHÔNG bước wizard, KHÔNG backfill bắt buộc | n/a | — | Rà soát 2026-06-22, cập nhật khi merge main. (1) Không seed: `push_subscriptions` rỗng lúc khởi tạo, mỗi browser tự đăng ký qua `POST /api/v1/push/subscriptions`. (2) **Env/secret mới opt-in** (không bắt buộc): `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` (đã thêm vào `Bindings`). Vắng → Web Push tắt end-to-end (endpoint vapid-public-key trả 404, toggle Studio ẩn, broadcaster bỏ qua leg push); in-app realtime vẫn chạy nơi có `SITE_ROOM`. **VAPID là tài nguyên DÙNG CHUNG toàn deployment** (định danh app-server với push service, không phải dữ liệu tenant); subscription + fan-out cô lập theo `site_id` + RLS. KHÔNG dùng settings key DB. (3) Không policy/grant: routes nằm trên chuỗi auth/tenant `/api/v1` sẵn có. (4) Không bước UI wizard — operator bật/tắt qua Settings → Notifications / chuông Studio sau khi đăng nhập; key sinh bằng `node apps/cms/scripts/generate-vapid-keys.mjs`. (5) Không capability `/setup/capabilities` mới. (6) KHÔNG cần backfill: migration `0039_push_subscriptions` (đánh số lại 0032→0035→0039 vì main tiến tới 0038) chỉ `CREATE TABLE IF NOT EXISTS` + guard duplicate-object cho FK (idempotent) + thêm `'push_subscriptions'` vào `rls-policies.sql`; instance cũ không có subscription → broadcaster no-op tới khi user đăng ký. Web Push crypto (RFC 8291/8292) dựng trên Web Crypto nên chạy cả CF Workers lẫn Node — không thêm dependency |
| 64 | realtime-audience-channels | v0.15.x | Đã rà soát: thêm **audience plane** cho realtime end-user FE (subject/channel addressing) tách khỏi studio plane. Mở rộng protocol `lumibase-sync-v1` (join/leave + frame `notification` dạng `{payload}`, không phá studio), thêm `RealtimeProvider` vào runtime (CF DO stub + Docker in-proc hub qua `ws`), route `POST /api/v1/realtime/audience-ticket`, `NotificationService` nối `notifications.recipient/pushed`, SDK `AudienceClient`, Node WS server trong `serve.ts`. Env mới **tùy chọn** `LUMIBASE_REALTIME_MAX_CONNECTIONS_PER_SUBJECT` (mặc định 0 = không giới hạn) + dependency mới `ws` | n/a | — | Rà soát 2026-07-02. **Bổ sung cho #30 push-notifications, KHÔNG trùng**: #30 là ops alert cho admin studio (frame `{notification}`, broadcast studio-plane, Web Push); feature này là realtime end-user FE nhắm `subjectId`/`channel` (frame `{payload}`). Hai plane cô lập trong cùng `SiteRoom` — `publishNotification` (của #30) đã được scope studio-plane khi merge để không rò sang audience. (1) KHÔNG seed: không bảng/row khởi tạo; `notifications` (đã tồn tại, có sẵn `pushed`) rỗng lúc setup. (2) KHÔNG feature-flag trong `settings`: realtime bật khi có `JWT_SECRET` (env đã tồn tại) + binding `SITE_ROOM` (CF) hoặc Node WS server (Docker); env mới là tùy chọn, đã thêm vào `Bindings`. (3) KHÔNG policy/grant DB: authz audience ở route cấp ticket (`resolveAudienceGrant` đọc claim đã verify của FE principal `isFrontendUser`); channel allowlist ký trong ticket, DO/hub chỉ enforce. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG cần backfill: không migration, không bảng/cột mới (`notifications.pushed` đã tồn tại); studio plane byte-compatible. Subject ↔ citizenID mapping do app FE sở hữu — LumiBase KHÔNG ép schema bảng user FE |
| 32 | cms-user-management-realms | v0.7.x | Đã rà soát: tách realm quản lý user — (a) role hệ thống mới `subscriber` (`systemKey='subscriber'`, `adminAccess=false`, `appAccess=false`) làm role tối thiểu cho frontend end-user; (b) viết lại public self-service `POST /auth/register` (rate-limit per-IP, email verification, anti-enumeration) + endpoint mới `POST /auth/verify-email`; (c) gán `aud` claim (`studio`/`frontend`/`email-verify`) cho Custom JWT, `withStudioAccess` chặn cứng token `frontend`. Xem ADR-011 + `docs/en/security/user-management.md` | n/a | — | Rà soát 2026-06-18. (1) KHÔNG seed trong setup tx: role `subscriber` tạo **lazy/idempotent** lúc đăng ký đầu tiên qua `ensureSubscriberRole` (mirror `upsertMemberRole` lazy của gap #15), idempotent qua `roles_site_system_key_unique` — CỐ Ý không thêm vào setup transaction để giảm blast radius. (2) KHÔNG feature-flag mới. (3) KHÔNG policy/grant seed: `subscriber` rỗng quyền, operator gắn content policy sau. (4) KHÔNG bước UI wizard mới — đăng ký là luồng end-user runtime, không thuộc wizard. (5) KHÔNG capability `/setup/capabilities` mới. (6) **KHÔNG cần migration & KHÔNG cần backfill**: `aud` là claim JWT additive (token cũ không có `aud` → `isFrontendAudience` trả false → vẫn qua được như trước); email-verify token là **stateless JWT** (không bảng mới); role `subscriber` tạo on-demand. Instance cũ: route `/register` trước đây chết (đọc `c.get('auth')` undefined vì `withAuth` bypass) → fix này làm nó hoạt động, không phá dữ liệu cũ. Yêu cầu env `JWT_SECRET` (đã có) + SMTP (tùy chọn; thiếu thì user ở `invited` chờ admin kích hoạt). **Bổ sung cùng đợt** (vẫn `n/a`, không migration/backfill): (a) forgot/reset password cho end-user — audience JWT mới `password-reset` (stateless, TTL 1h), route public `/auth/forgot-password` + `/auth/reset-password`; (b) cấp quyền đọc content cho subscriber — policy hệ thống `subscriber` (`policies_site_key_unique`, lazy idempotent) + endpoint admin `GET/POST/DELETE /api/v1/users/subscriber-access` để gắn permission `read` (Policy DSL, mặc định `status=published`). Không seed trong setup tx (policy tạo on-demand khi admin cấp quyền lần đầu); operator quyết collection nào mở cho subscriber. **+ resend verification** (`POST /auth/resend-verification`, public, generic 202, rate-limit `resend-rate`) tái dùng token verify hiện có — không state mới; email verify/reset render template DB (`email_verification`/`password_reset`) nếu có, fallback inline → không cần seed template trong setup. **+ TTL session theo realm** (`sessionTtlFor`): studio mặc định `12h`, frontend `30d`, override qua env tùy chọn `STUDIO_SESSION_TTL`/`FRONTEND_SESSION_TTL` (giá trị sai → fallback default, không vỡ login) → không state/migration, không bắt buộc trong setup. **+ refresh token (gia hạn êm)**: bảng mới `lumibase_refresh_tokens` (migration `0005_refresh_tokens`, có `site_id` — multi-tenant), endpoint public `/auth/refresh` + `/auth/logout`, rotation + reuse-detection, transport cookie httpOnly + body; refresh TTL per-realm mặc định studio 30d/frontend 90d (env `STUDIO_REFRESH_TTL`/`FRONTEND_REFRESH_TTL`); reset password thu hồi toàn bộ refresh token. Setup **không** seed gì cho bảng này (token sinh on-demand khi login) → setup impact `n/a`, chỉ thêm 1 migration. **+ cookie cross-domain**: env tùy chọn `REFRESH_COOKIE_SAMESITE`/`REFRESH_COOKIE_DOMAIN`/`REFRESH_COOKIE_SECURE` (mặc định Lax/host-only/Secure) cho frontend khác domain → không state/migration **+ SDK auto-refresh & Studio wiring** (`createLumiClient` refresh-on-401 + Studio login/logout) — code-only, không state. **Merge main (post table-prefix squash)**: main đã gộp toàn bộ migration về baseline `0000_lumibase_init` (bảng prefix `lumibase_`), nên migration của nhánh này đánh lại số trên lineage mới: `0005_refresh_tokens` + `0006_password_changed_at_and_email_unique`; migration no-op `realign_snapshot_baseline` cũ bị bỏ (baseline mới đã có snapshot chuẩn). KHÔNG seed/backfill. **⚠️ CẬP NHẬT (review hardening, migration `0006_password_changed_at_and_email_unique`):** thêm cột `users.password_changed_at` (nullable → KHÔNG backfill) cho single-use reset token (H1); **VÀ** unique index `users_email_lower_unique` trên `lower(email)` (H3) — **ĐÂY LÀ ĐIỂM BACKFILL/RỦI RO VẬN HÀNH DUY NHẤT:** migration `0006` sẽ **FAIL** nếu instance cũ đã có email trùng nhau (không phân biệt hoa/thường); phải **de-dup trước khi migrate** (`SELECT lower(email), count(*) FROM users GROUP BY 1 HAVING count(*)>1;` rồi gộp/xoá) — xem header migration + CHANGELOG upgrade note. Fresh instance: KHÔNG ảnh hưởng (bảng rỗng). Còn lại vẫn `n/a`: pin `aud` phiên (studio/frontend) trong `verifyCustomJwt` (M5) → token cũ KHÔNG có `aud` bị từ chối, người dùng đăng nhập lại (chi phí một lần, không mất dữ liệu); `lumibase_refresh_tokens` thêm vào `rls-policies.sql` site_isolation (M6) — chạy lại `rls-policies.sql` idempotent; rotation atomic (M1), `/refresh` re-check membership + realm (M4), subscriber-role upsert target `system_key` index (L4) — tất cả code-only, không state/seed |

| 65 | (security-bug-priority: guard fixes) | v0.15.x | Đã rà soát: port 3 security fix từ các PR mở — (a) `withSiteMembership()` chèn vào chuỗi `/api/v1` sau `withAuth()` (PR #184): user principal phải có `user_sites` membership với site chọn qua `X-Lumi-Site`, API key giữ nguyên site-match của `withAuth`, dev token + bootstrap + CF Access admin giữ carve-out; (b) khôi phục `adminOnly` trên dynamic extension dispatch `extensionsRouter.all('/:name/*')` (PR #152); (c) sửa `/auth/register` (phần bug của PR #130). Kèm tripwire test `security-guards.wiring.test.ts`, guide `docs/en/security/route-guards.md`, DoD mục 2c | n/a | — | Rà soát 2026-07-03. (1) KHÔNG seed: không bảng/row mới; role `member` đã được setup wizard seed sẵn (`upsertMemberRole`). (2) KHÔNG feature-flag/settings key. (3) KHÔNG policy/grant DB mới — dùng lại `user_sites`/`roles.systemKey`/RBAC bundle sẵn có. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: không migration. **Lưu ý vận hành**: instance dùng CF Access mà principal không có `users` row vẫn qua được nhờ carve-out admin (mirror `requireSiteAdmin`). **⚠️ Phần (c) — `/auth/register` — đã được SIÊU CHỈNH bởi row #32 (`cms-user-management-realms`)**: PR #190 tạm khoá register về admin-only như stopgap khi endpoint đang crash; sau khi merge #130, register trở lại **public self-service** (role resolve server-side về `subscriber` zero-quyền + `invited` tới khi verify). Trạng thái cuối cùng theo row #32, KHÔNG phải admin-only. Tripwire test đã được đồng bộ theo thiết kế public register |

| 33 | collection-create-modes | v0.9.x (proposal) | Bước chọn chế độ tạo collection (View / DB View / Flexible View) + Default_Field_Catalogue + Localize_Dropdown dùng chung. View_Mode không cần khởi tạo. DB/Flexible mode đề xuất 2 cột mới `collections.read_only`/`source_object` | pending | [collection-create-modes/tasks.md](../collection-create-modes/tasks.md) Phase 5.1 | Rà soát 2026-06-29 (PROPOSAL, chưa code). (1) Có thể cần seed `localize` field type khi nền tảng field-localization xong (chờ ADR). (2) Localize_Dropdown đọc Tenant_Locales từ feature #34 — chưa có nguồn truth chính thức (fallback `['en','vi']` hard-code hiện tại). (4) Bước UI mới = mode selector TRƯỚC wizard (không thuộc setup wizard). (6) DB/Flexible mode cần migration thủ công 2 cột (`read_only`,`source_object`) → backfill default an toàn (`false`/`null`); View_Mode KHÔNG migration. Phụ thuộc #34, #35 |
| 34 | tenant-localization-config | v0.9.x (proposal) | Nguồn truth Tenant_Locales (khuyến nghị `settings.key='locales'`) + UI Settings → Languages + sửa Admin_Path. Lấp GAP: hiện không có bảng/API available-locales, `settings.get('locales')` fallback hard-code `['en','vi']` | pending | [tenant-localization-config/tasks.md](../tenant-localization-config/tasks.md) Phase 5.1 | Rà soát 2026-06-29 (PROPOSAL, chưa code). (1) Setup KHỞI TẠO `settings.locales=[default_language]` khi xong (Req 4.2). (2) Settings key `locales` mới — operator cần biết. (4) Tùy chọn thêm bước "ngôn ngữ" vào setup wizard; nếu không thì default `[default_language]`. (6) Nếu dùng `settings` (khuyến nghị) → KHÔNG migration; instance cũ thiếu key → fallback `[default_language]`. Admin_Path từ `/setup/path` thành giá trị sửa-được. Phụ thuộc lẫn nhau với #33 |
| 35 | db-view-introspection | v0.9.x (proposal) | Endpoint introspection DB (`/db/objects`, `/collections/:name/introspect`) + chấm than (⚠) cho Uncatalogued_Field + bootstrap-on-click tạo record `fields`. Lấp GAP: hiện không có introspection, field chỉ đọc từ bảng `fields` | pending | [db-view-introspection/tasks.md](../db-view-introspection/tasks.md) Phase 5.1 | Rà soát 2026-06-29 (PROPOSAL, chưa code). (1) KHÔNG seed. (2) KHÔNG settings key. (3) Quyền dùng = quyền quản trị schema sẵn có; bootstrap qua agent cần HITL `ai_approvals` (rule #4). (4) KHÔNG bước setup wizard mới. (5) Có thể cần capability flag báo runtime hỗ trợ introspection. (6) KHÔNG bảng mới của riêng nó (đọc `information_schema`/`PRAGMA`); dùng chung 2 cột với #33. Adapter introspect qua runtime abstraction (rule #3). Phụ thuộc #33 |
| 66 | (marketplace community: install-trust/download/vote/submit) | v0.16.x | Đã rà soát: mở rộng marketplace — badge `verified` (trusted source: signature + publisher key + hash) trên catalog, nút download package (`GET /extensions/:slug/download`, bump `downloadCount`, 302 → bundle), upvote (`POST`/`DELETE /extensions/:slug/vote`, idempotent qua unique `(userId, marketplaceSlug)`), community submit (`POST /submit` → row global `pending`/unpublished) + moderation (`GET /submissions`, `POST /submissions/:id/review` cần `extensions:configure`). Thêm cột `extensions.download_count`/`submission_status`/`submitted_by` + bảng mới `extension_votes` (migration `0003_marketplace_votes_downloads`) | n/a | — | Rà soát 2026-07-04. (1) KHÔNG seed: votes/submissions tạo theo nhu cầu; `download_count` mặc định 0. (2) KHÔNG feature-flag/settings key mới. (3) KHÔNG policy/grant DB mới — vote/submit chỉ cần `withAuth` (user đã đăng nhập); moderation dùng lại capability `extensions:configure` sẵn có (không role mặc định nào cần bổ sung ngoài admin đã có). (4) KHÔNG bước UI wizard mới — thao tác ở Studio → Marketplace / site công khai sau setup. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG cần backfill dữ liệu: migration `0003` chỉ `ADD COLUMN` (có default `0`/nullable) + `CREATE TABLE extension_votes` (forward-only qua journal); instance cũ nhận `download_count=0`, bảng votes rỗng, listing chưa ký hiện `verified=false`. **Submodule move** của `apps/marketplace` → `lumibase-ai/marketplace` là thao tác git thuần (script `scripts/marketplace-submodule-migration.sh`), KHÔNG ảnh hưởng setup/DB |
| 67 | content-versioning | v0.17.x | Bảng `content_versions` (đã trong `0000_lumibase_init`); versions tạo theo nhu cầu | n/a | — | Rà soát 2026-07-06. (1) KHÔNG seed — version tạo khi user cần. (2) KHÔNG settings/flag. (3) KHÔNG policy DB mới (reuse RBAC update item). (4) KHÔNG wizard. (5) KHÔNG capability. (6) KHÔNG backfill — bảng đã có trong init |
| 39 | insights-dashboard | v0.17.x | Bảng `dashboards`/`panels` (đã trong init) | n/a | — | Rà soát 2026-07-06. (1) KHÔNG seed dashboard mẫu — tạo theo nhu cầu. (2)–(5) KHÔNG. (6) KHÔNG backfill — bảng đã có trong init |
| 40 | visual-flow-builder | v0.17.x | Event/schedule/webhook trigger runtime; scheduled handler gọi `runDueScheduledFlows` trên tick | n/a | — | Rà soát 2026-07-06. (1) KHÔNG seed flow. (2) KHÔNG settings key. (3) KHÔNG policy DB mới. (4) KHÔNG wizard. (5) KHÔNG capability. (6) KHÔNG backfill. **Lưu ý vận hành**: schedule trigger cần runtime gọi `runDueScheduledFlows` (CF Cron / Docker tick) — dùng chung tick scheduler hiện có, không cần bước setup |
| 41 | translation-memory-ui | v0.17.x | Settings key `translations.learnTm` (default ON khi vắng) | n/a | — | Rà soát 2026-07-06. (1) KHÔNG seed — vắng row = learn ON (default an toàn). (2) **Settings key `translations.learnTm`** (bảng `settings` sẵn có) — operator tắt được; không bắt buộc setup. (3)–(5) KHÔNG. (6) KHÔNG backfill — vắng row đọc default ON |
| 42 | image-transform-dsl | v0.17.x | Bảng `transform_presets` (migration `0004_transform_presets`); preset tạo theo nhu cầu | n/a | — | Rà soát 2026-07-06. (1) KHÔNG seed preset bắt buộc — delivery không param = ảnh gốc; thumbnail cũ (queue) không đổi. (2) KHÔNG settings key bắt buộc (signed-transform chưa bật mặc định). (3)–(5) KHÔNG. (6) **Migration `0004` thêm bảng `transform_presets`** — instance cũ chạy `db:migrate` để có bảng; KHÔNG backfill dữ liệu (bảng trống hợp lệ, `?preset=` chưa có sẽ 404 mềm → phục vụ gốc) |
| 43 | realtime-subscriptions | v0.17.x | DO `SITE_ROOM` (đã có binding + `new_sqlite_classes`); filter `evaluateRule` per-sub (`subscribe.filter`, envelope-only vì wire signal-only), read-gate subscribe qua allowlist collections trong ticket ký (`studio-grant.ts`, fail-closed), field masking qua signal-only + `projectPayload` | n/a | — | Rà soát 2026-07-06, cập nhật 2026-07-12 khi đóng filter + read-gate. (1)–(5) KHÔNG. (6) KHÔNG backfill. Lưu ý DO sqlite class trên free plan đã xử lý ở binding hiện có; ticket claim mới (`collections`) là chi tiết wire nội bộ, không ảnh hưởng setup |
| 44 | presets-inheritance | v0.17.x | Resolution service + effective/bookmarks (bảng `presets` đã có) | n/a | — | Rà soát 2026-07-06. (1) KHÔNG seed global-default — preset tạo theo nhu cầu; vắng preset = empty effective (an toàn). (2)–(5) KHÔNG. (6) KHÔNG backfill — chỉ thêm resolution logic, không đổi schema |
| 69 | cdc-extension-integration | v0.21.x | **Đã triển khai** (`.kiro/specs/cdc-extension-integration/`, PR #244): Change Feed first-party — outbox `lumibase_cdc_change_events` (nanoid PK, keyset `(occurred_at, id)`) + `lumibase_cdc_subscriptions` + `lumibase_cdc_deliveries` (tên prefixed ADR-010), pull API cursor-based `/api/v1/cdc/events`, webhook dispatcher (HMAC + retry, ride `QueueProvider`), extension subscriber (`defineCdcSubscriber`, capability `cdc:subscribe:<collection>` enforce host-side), AI skills + MCP tools (`deleteCdcSubscription` → HITL). Kết quả rà soát: (1) KHÔNG seed. (2) Settings key tùy chọn `cdc_feed` (enabled/retentionDays, **default off/7** — vắng row = feed tắt, an toàn). (3) KHÔNG policy/grant DB mới (RLS `site_isolation` thêm cho 3 bảng trong `rls-policies.sql`; route dùng RBAC/capability sẵn có). (4) KHÔNG bước wizard. (5) **CÓ capability mới** `cdc:subscribe`/`cdc:manage` → upgrade note đã ghi ở CHANGELOG (Unreleased); KHÔNG thêm flag `/setup/capabilities` (theo tiền lệ deployments). (6) KHÔNG backfill — migration `0007_cdc_change_feed` idempotent chồng lên `0000_lumibase_init`, 3 bảng mới rỗng + RLS; instance cũ chỉ cần `db:migrate` | done | tasks.md Phase G (task 11.2, 16) | Spec tạo 2026-07-03; đồng bộ main v0.21.0 2026-07-08 (ADR-010 prefix, migration 0005→0007, ADR-011 realm). **Triển khai hoàn tất 2026-07-11** (Phases A–H): cursor đổi uuidv7→nanoid + keyset composite (quyết định Option B); feed router mount trước control-plane router cùng prefix `/cdc`. **Follow-up 2026-07-12**: openapi.yaml + SDK typed resources; capture `collections.*`/`fields.*` (migration `0008_cdc_resource_column` — ADD COLUMN `resource` DEFAULT 'item', idempotent, KHÔNG backfill, KHÔNG capability/seed/wizard mới); long-polling `wait`. Phần còn lại (settings capture, realtime, consumer-group, inbound, partition) → spec `.kiro/specs/cdc-feed-roadmap/`. **Follow-up 2026-07-16**: siết phân loại rủi ro — `createCdcSubscription`/`replayCdcSubscription` gắn cờ `dangerous: true` (trước chỉ `deleteCdcSubscription` control-plane qua tiền tố `delete`) để đường agent/MCP khớp posture admin-only của REST `/api/v1/cdc`. Setup-impact **n/a**: KHÔNG seed/settings/policy DB/wizard/capability/backfill — chỉ đổi cờ risk trên registry harness + test + docs |
| 68 | (security hardening: file upload) | v0.17.x | Đã rà soát: mở rộng `withFileUploadPolicy` để phủ luôn `POST /api/v1/media/:key` (trước đây chỉ phủ `/files`): chặn public role đẩy bytes, cap size theo bytes THẬT (không chỉ `Content-Length`), allowlist MIME, đối chiếu đuôi↔MIME, content-sniff magic bytes, và chặn SVG chứa mã động (`<script>`/`on*=`/`javascript:`/`<foreignObject>`/XXE). Serve media dưới `Content-Disposition: attachment` + `nosniff`; storage adapter map `contentType` sang field native R2/S3 để round-trip đúng. Allowlist/size giờ cấu hình được **per-site qua DB** (settings key `upload_policy`) + trang Studio **Settings → Uploads** + endpoint `GET/PUT /api/v1/uploads/config`; guard đọc DB→env→default (fail-safe). Catalogue đuôi/MIME gom về `@lumibase/shared/schemas` (single source cho server + picker). Bổ sung **polyglot deep-scan** (`imageHasEmbeddedActivePayload`): quét toàn bộ bytes raster image tìm `<?php`/`<script`/`<html` nhúng → `UPLOAD_EMBEDDED_PAYLOAD` (đồng bộ, dual-runtime). Gom thành spec hub `.kiro/specs/upload-file-controls/`. Code + test + docs only | n/a | [upload-file-controls/tasks.md](../upload-file-controls/tasks.md) | Rà soát 2026-07-05. (1) KHÔNG seed: không cần seed row `upload_policy` — vắng row → guard fallback env→default (allowlist ảnh/PDF/CSV/text + 10 MiB). (2) **Settings key mới `upload_policy`** (scope `site`, lưu trong bảng `settings` sẵn có) — operator có thể sửa qua Settings → Uploads; env `FILE_UPLOAD_MAX_BYTES`/`FILE_UPLOAD_ALLOWED_MIME_TYPES` vẫn là fallback. (3) KHÔNG policy/grant DB mới — PUT gated bằng `requireSiteAdmin`; `/media` vẫn dựa RBAC `media:create` (default-deny). (4) KHÔNG bước setup wizard mới (trang nằm trong Settings, không phải wizard). (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill/migration: `upload_policy` dùng bảng `settings` (đã tồn tại), instance cũ thiếu row → fallback tự động; object media cũ (content-type chỉ trong custom metadata) vẫn serve đúng nhờ fallback `obj.metadata?.contentType`. Xem `docs/en/security/runtime-security-guards-plan.md` mục "File upload policy" |
| 71 | (realtime co-editing) | v0.17.x | Đã rà soát: cảnh báo real-time "nhiều người cùng sửa 1 item" trên Studio item editor — banner + avatar stack lọc theo đúng `itemId`. UI-only trên kênh presence sẵn có (`SiteRoom` DO + `usePresence`), không thêm surface backend | n/a | — | Rà soát 2026-06-24. (1) KHÔNG seed: presence là session ephemeral trong DO, không bảng/row DB. (2) KHÔNG feature-flag: dùng WebSocket presence đã bật sẵn. (3) KHÔNG policy/grant: chỉ hiển thị, không tác động ghi. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability mới. (6) KHÔNG backfill: không migration, không bảng/cột mới; instance cũ tự có hành vi này khi Studio cập nhật. `CoEditingBanner` + `PresenceStack` thuần, chia sẻ 1 subscription qua `useCoEditors` |
| 70 | (security hardening: settings admin gate + out-of-scope backlog) | v1.0.x | Đã rà soát: gate `requireSiteAdmin` cho `POST`/`DELETE /api/v1/settings` (writes admin-only; reads giữ mở cho member vì editor cần đọc `locales`…) — trước đây mọi member ghi được bất kỳ settings key kể cả `upload_policy`/`media.signedTransform`. Thêm backlog trung tâm `.kiro/steering/out-of-scope-backlog.md` cho phát hiện ngoài scope + DoD §7. Code + test + docs only | n/a | [out-of-scope-backlog.md](../../steering/out-of-scope-backlog.md) | Rà soát 2026-07-10. (1) KHÔNG seed. (2) KHÔNG settings key mới. (3) KHÔNG policy/grant DB mới — chỉ thêm guard admin cho write path sẵn có. (4) KHÔNG bước setup wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill. Kèm: `GET /settings` redact field secret (B2 — `redactSecrets`) nên không còn lộ `media.signedTransform.secret` cho member |
| 72 | git-integration | v0.x | Kết nối GitHub/GitLab per-site: 6 bảng `lumibase_git_*` (migration `0009_git_integration`, additive `CREATE TABLE IF NOT EXISTS` + RLS), khởi tạo rỗng. (1) KHÔNG seed lúc setup — integration tạo theo nhu cầu; role `git-sync` thêm vào `ROLE_LIBRARY`, seed **lazy** qua `ensureSeeded()` (ngoài setup tx). (2) KHÔNG settings key DB; env tuỳ chọn `GITHUB_*`/`GITLAB_*`/`LUMIBASE_PUBLIC_URL` + `ENCRYPTION_KEY` (đã tồn tại). Preview opt-in per-integration (`sync_config.preview`). (3) Grant `git-sync` L1 (PROPOSE, `items:write`) seed **khi tạo integration đầu tiên** (`ensureGitSyncAutonomyBaseline`, idempotent), KHÔNG trong setup tx; fallback resolver cũng L1. (4) KHÔNG bước wizard — cấu hình ở Studio → Settings → Integrations → Git repositories. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: bảng mới rỗng, role/grant seed lazy/on-connect → instance cũ no-op | n/a | git-integration/tasks.md | Rà soát 2026-06-28, cập nhật sau merge main v0.22.0 (`6041970`). Migration đổi `0007→0009` (main chiếm 0007_cdc_change_feed/0008). Chi tiết: `.kiro/specs/git-integration/setup-impact.md`; token/secret mã hoá qua `CryptoService`; goal vẫn planner-only |

| 80 | (desktop+mobile shell: apps/shell) | v0.23.x | Đã rà soát: app Tauri 2 hợp nhất (`apps/shell`) wrap Studio SPA cho desktop (Win/macOS/Linux) + mobile (iOS/Android). Hybrid delivery (bundled + remote-first tới `studio.lumibase.dev`), auto-update ký minisign (desktop), deep-link `lumibase://`, và màn hình "Connect to LumiBase" (runtime API-base override trong localStorage) cho chế độ bundled. Client-only — KHÔNG đụng CMS route/schema/seed. (1) KHÔNG seed. (2) KHÔNG settings/feature-flag DB. (3) KHÔNG policy/grant DB. (4) KHÔNG bước setup wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill | n/a | — | Rà soát 2026-07-18. **Lưu ý vận hành (KHÔNG phải setup wizard)**: self-hoster muốn dùng desktop/mobile ở chế độ bundled (origin `tauri://localhost` / `https://tauri.localhost`) gọi CMS cross-origin PHẢI thêm các origin đó vào `CORS_ALLOWED_ORIGINS` của CMS. Không cần nếu dùng chế độ remote (same-origin với `studio.lumibase.dev`). Khóa ký updater (`TAURI_SIGNING_PRIVATE_KEY`) là secret CI, không commit. Chi tiết build/ký: `apps/shell/README.md` |
| 98 | (media cache policy: #388) | v0.25.x | Đã rà soát: `immutable, max-age=1y` trên `/media/:key` chuyển sang bật theo version pin `?v=<contentHash>`; fingerprint ghi vào storage metadata (`contentHash`) lúc upload; đường bytes gốc có `Cache-Control` + `ETag` + `304` tường minh. (1) KHÔNG seed. (2) KHÔNG settings key mới — chính sách cứng trong route, không có knob per-site. (3) KHÔNG policy/grant DB. (4) KHÔNG bước setup wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill DB | n/a | — | Rà soát 2026-08-10. **Lưu ý vận hành (KHÔNG phải setup wizard)**: object đã upload **trước** thay đổi này không có `contentHash` trong metadata nên vĩnh viễn nhận chính sách revalidate, không bao giờ `immutable` — degrade an toàn, không cần backfill. Muốn chúng được pin thì upload lại; không có job backfill vì hash lại toàn bộ storage tốn hơn giá trị nhận được. Object ghi qua `PUT /files/upload/:key` (stream, không buffer body) cũng không có fingerprint theo thiết kế. Bytes đã nằm trong browser của người dùng từ trước thay đổi này thì **không thu hồi được** — đó chính là lý do #388 tồn tại |
| 99 | (edge cache purge: #392) | v0.25.x | Đã rà soát: `EdgeCacheProvider.purge({urls, tags})` + index tag→URL (`edgeurls:<tag>` trong CacheProvider) + header `Cache-Tag` trên response delivery công khai. (1) KHÔNG seed. (2) KHÔNG settings key DB — cấu hình qua env, không phải per-site. (3) KHÔNG policy/grant DB. (4) KHÔNG bước setup wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill DB | n/a | — | Rà soát 2026-08-10. **Hai env var mới, đều tuỳ chọn**: `CF_PURGE_ZONE_ID` + `CF_PURGE_API_TOKEN` (token cần quyền `Cache Purge` trên zone đó; là secret, không commit). Vắng cả hai → purge tụt xuống colo-local, tức hành vi y như trước thay đổi này (hết hạn theo `s-maxage`) — degrade an toàn, KHÔNG chặn setup, KHÔNG cần cấu hình để chạy được. Docker: `NoOpEdgeCacheProvider` trả 0, không có edge nên không có gì để purge. Index `edgeurls:*` tự sinh theo traffic và có TTL, không cần khởi tạo |
| 101 | (in-process cache layer: #391) | v0.25.x | Đã rà soát: `withProcessCache` trong `@lumibase/runtime`, áp cho compiled permission bundle (`perm:{site}:v{n}:{principal}`) với store cấp module, cap 256 entry, TTL 5s. (1) KHÔNG seed. (2) KHÔNG settings key / env var mới — hằng số trong code, không có knob vận hành. (3) KHÔNG policy/grant DB. (4) KHÔNG bước setup wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG migration/backfill | n/a | — | Rà soát 2026-08-10. Thuần bộ nhớ tiến trình, không có trạng thái ngoài process nên KHÔNG có gì để khởi tạo hay migrate. **Điều cần biết khi vận hành**: version pointer `perm-ver:{siteId}` **không** được cache in-process, nên thu hồi quyền vẫn hiệu lực ở request kế tiếp (Property P9 giữ nguyên). Nếu shared cache bị flush (ví dụ Redis restart), bundle cũ có thể còn được phục vụ tối đa 5s trong mỗi process — bị chặn bởi TTL, không cần thao tác |
| 73 | (security docs) | v0.22.x | Đã rà soát: `docs/en/security/anti-abuse.md` — tài liệu tổng hợp cơ chế chống lạm dụng (registry defense-in-depth: rate-limiting/brute-force, anomaly, control-plane guard, RLS, body/upload limits, security headers, SSRF, AI load-guard, audit) + best practices + gaps; cross-link các doc security sẵn có (route-guards, runtime-security-guards-plan, cwe-top-100-audit…). Docs-only: + mirror `docs/vi/security/anti-abuse.md`, + index thư mục Security vào `docs/en/README.md` + category Security trong `apps/docs/docs.config.json`. KHÔNG đụng schema/route/seed | n/a | — | Rà soát 2026-07-01, reconcile 2026-07-14. (1) KHÔNG seed: không bảng/row khởi tạo. (2) KHÔNG feature-flag/settings key mới. (3) KHÔNG policy/grant DB. (4) KHÔNG bước UI wizard mới. (5) KHÔNG capability `/setup/capabilities` mới. (6) KHÔNG backfill: chỉ thêm/sửa file Markdown + sidebar; không thay đổi runtime. Chỉ mô tả cơ chế **đã tồn tại** trên main (đã re-verify: login limiter Postgres-backed; `body-limit.ts`/`rate-limit.ts`/`control-plane-access-guard.ts`/`security-headers.ts`; settings admin-gate + `redactSecrets`; upload phủ `/media` + `upload_policy` per-site); gap còn lại (recovery limiter in-memory, throttle non-atomic…) chỉ nêu, chưa implement |

## Lưu ý backfill

Các gap #1–#3 ảnh hưởng cả instance **đã setup** — fix không chỉ nằm trong setup wizard mà cần kèm migration/backfill idempotent (`onConflictDoNothing`) hoặc giữ lazy-init làm fallback song song.

Gap #10 ảnh hưởng instance **đã setup trước v0.6.x**: bootstrap admin của họ không có RBAC role nên sẽ bị 403 trên mọi schema/items request. Backfill idempotent cần: tạo role `Administrator` (`adminAccess=true`, `systemKey='administrator'`) cho mỗi site đã có bootstrap user, rồi insert `user_roles` cho user `is_bootstrap=true` (dùng `onConflictDoNothing` để an toàn khi chạy lại).
