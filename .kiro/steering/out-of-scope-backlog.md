# Out-of-scope findings backlog

> **Nơi duy nhất (single source) log lại mọi lỗ hổng / bug / feature / task được
> phát hiện *trong khi làm một PR* nhưng **không thuộc scope** của PR đó.**
>
> Vì sao cần: phát hiện ngoài scope hay bị nêu trong mô tả PR hoặc chat rồi
> **biến mất sau khi PR merge**. File này giữ chúng ở một chỗ để không bỏ sót.
> Bắt buộc qua DoD §7 (`definition-of-done.md`).

## Cách dùng

1. Khi đang làm PR mà phát hiện điều gì đó thật (bug/lỗ hổng/nợ kỹ thuật/feature)
   **ngoài scope** PR hiện tại → thêm một dòng vào bảng dưới **trong cùng PR đó**.
2. Đặt `Trạng thái`: `open` · `fixed` (kèm PR/commit) · `wontfix` (kèm lý do) ·
   `tracked` (đã có spec/issue riêng — ghi link).
3. Khi một mục được xử lý ở PR sau → cập nhật trạng thái + tham chiếu (đừng xoá
   dòng, giữ dấu vết).
4. Nếu mục đủ lớn thành feature → tạo `.kiro/specs/<feature>/` và đổi trạng thái
   sang `tracked` trỏ tới spec đó.
5. Mức độ: `critical` · `high` · `medium` · `low`.

## Registry

| ID | Phát hiện (ngày · PR) | Loại | Khu vực | Mô tả | Mức độ | Trạng thái | Tham chiếu |
|----|----------------------|------|---------|-------|--------|-----------|-----------|
| B1 | 2026-07-10 · upload-file-controls | vuln | `routes/settings.ts` | `POST /api/v1/settings` + `DELETE /:key` không có admin gate → mọi member đã đăng nhập ghi được **bất kỳ** settings key (kể cả `upload_policy`, `media.signedTransform`), vòng qua admin gate của các endpoint config riêng | high | `fixed` | Gated `requireSiteAdmin` cho POST/DELETE (giữ GET mở cho member) + test `routes/__tests__/settings.test.ts` |
| B2 | 2026-07-10 · upload-file-controls | vuln | `routes/settings.ts` GET | `GET /api/v1/settings` và `/:key` trả **nguyên value** cho mọi member — lộ settings chứa secret, cụ thể `media.signedTransform.secret` (dùng ký/verify signed image transform) → member thường có thể đọc rồi giả mạo transform | high | `fixed` | `redactSecrets` che field secret (theo tên: secret/token/password/apikey/…) ở cả GET list + by-key, cho mọi caller (consumer server đọc trực tiếp từ DB, không qua HTTP). Test trong `routes/__tests__/settings.test.ts` |
| B3 | 2026-07-10 · upload-file-controls | bug (infra) | CI · Cloudflare Pages | Preview build `lumibase-docs` + `lumibase-landing` fail tức thì (0 giây) trên PR — là config CF Pages project (git-integrated auto-build), không phải nội dung repo (docs build local pass; landing không bị PR đụng). Xuất hiện từ một lần merge main | low | `open` | Sửa ở Cloudflare dashboard (build config/root dir) hoặc tắt preview cho 2 project này. Không chặn merge (non-required) |
| B4 | 2026-07-10 · upload-file-controls | task (hygiene) | `apps/docs/public/releases.json` | File được git-track nhưng `pnpm docs:build` (`build-release-manifest.mjs`) sinh lại → mỗi lần build docs làm bẩn working tree (lần gặp: flip `migrationWarning`) | low | `fixed` | KHÔNG untrack (release skill hand-edit file này làm source of truth). Thay vào đó làm `build-release-manifest.mjs` **idempotent**: giữ editorial fields (`migrationWarning`/`minimumSafeUpgradeVersion`) + `releaseDate` từ manifest đã commit khi không có override (env/CHANGELOG). Vá luôn bug tiềm ẩn: deploy build trước đây clobber hand-edit về default. Diff = 0 khi build lại |
| B5 | 2026-07-05 · upload-file-controls | feature | Upload security | Re-encode ảnh để sanitize (chống polyglot không-false-positive) — hiện chỉ có deep byte-scan heuristic. Phụ thuộc `ImageAdapter` (Sharp/CF Images) | medium | `tracked` | `.kiro/specs/upload-file-controls/tasks.md` (F1) ↔ `image-transform-dsl` ↔ `runtime-security-guards-plan.md` §3 |
| B6 | 2026-07-05 · upload-file-controls | vuln (residual) | `middleware/file-upload-policy.ts` | SVG có `<use xlink:href>` trỏ resource external không bị chặn ở scan (bỏ để tránh false-positive) | low | `wontfix` | Đã trung hoà bởi serve `attachment` + `nosniff` + CSP; ghi trong `upload-file-controls/design.md` |
| B7 | 2026-07-05 · upload-file-controls | task (residual) | `studio/.../interfaces/file.tsx` | `maxSize` per-field chỉ là UI hint, không enforce server per-field | low | `wontfix` | Global upload policy đã enforce server-side; rủi ro thấp |
| B8 | 2026-07-23 · owasp-api-top-10-audit | vuln (deps) | `apps/landing`, `apps/consumer`, transitive | Gate CI `Dependency vulnerability audit` đỏ trên PR #289 (23 advisory: 11 high/11 moderate/1 low) — KHÔNG do diff PR này (`git diff origin/main...HEAD` cho `package.json`/lockfile rỗng). Nổi bật: Next.js SSRF `GHSA-89xv-2m56-2m9x` (Server Actions) + `GHSA-p9j2-gv94-2wf4` (rewrites) + DoS `GHSA-m99w-x7hq-7vfj` + middleware bypass `GHSA-6gpp-xcg3-4w24` ở `next@16.2.10` (landing/consumer, KHÔNG thuộc CMS API); `sharp@0.34.5` (transitive qua next) `GHSA-f88m-g3jw-g9cj`; `fast-uri@3.1.2` host-confusion `GHSA-4c8g-83qw-93j6`/`GHSA-v2hh-gcrm-f6hx` (transitive qua `@modelcontextprotocol/sdk`). Không nằm trong dependency-path của CMS API (Hono) | high | `fixed` | Đóng trong PR #289: bump `next → 16.2.11` (consumer+landing) + pnpm `overrides` `fast-uri >=3.1.4` & `sharp >=0.35.0`. `pnpm audit --prod --audit-level high` exit 0 (còn 1 low + 1 moderate, không chặn gate); build landing+consumer pass. FE guidance ở `docs/{en,vi}/tutorials/nextjs-quickstart.md` §6 |
| B9 | 2026-07-25 · studio-deployments-wiring | bug | `apps/studio/src/router.tsx` | Cây route admin-path (`adminPathSettingsRoute`) thiếu 2 route mà cây `/settings` có: **`change-feed`** và **`encryption`**. Sidebar hiển thị cả hai ở mọi instance, nên trên instance dùng custom admin path (`/<adminPath>/settings/...`) hai link đó không resolve — cùng class "quên nối dây" với trang Deployments của PR này, chỉ khác là lộ ở nửa admin-path. Không nằm trong scope PR (chỉ nối dây Deployments), và tripwire `deployments-page.test.tsx` cố ý chỉ kiểm nav ↔ cây `/settings` để không đỏ CI vì hai route sẵn thiếu | medium | `open` | Fix = thêm `adminPathChangeFeedRoute` + `adminPathEncryptionSettingsRoute` (copy pattern route kề bên) rồi mở rộng tripwire kiểm luôn cây admin-path (mọi nav item phải có 2 route: plain + `$adminPath`) |
| B10 | 2026-07-25 · agent-run-worker-keyprovider | bug (gap) | `apps/cms/src/index.ts` · `apps/cms/wrangler.toml` | Cloudflare Workers **không có** queue consumer cho `agent-runs`: `index.ts` chỉ `export default app` (không có `queue(batch, env)` handler) và `wrangler.toml` để `[[queues.producers]]` ở dạng comment. Hệ quả: `POST /api/v1/agent/goals` với `execution: 'async'` trên CF hoặc bị chặn ở nhánh `ASYNC_UNAVAILABLE` (không có queue adapter), hoặc enqueue rồi **không ai tiêu thụ** → run đứng ở `queued`. Comment trong `agent-run-worker.ts` ("Cloudflare Workers wire the same handler through their queue consumer export") mô tả một thứ chưa tồn tại. Phát hiện khi thread `KeyProvider` vào worker deps — chỗ đáng lẽ phải sửa cho CF thì không có call site nào | medium | `open` | Cần: `queue()` export ở `index.ts` (dựng db/runtime ngoài request), bật `[[queues.producers]]` + `[[queues.consumers]]` trong `wrangler.toml`, truyền `keys: createCloudflareKeyProvider(env)`. Đủ lớn → nên tạo spec riêng. Docker/Node **không** ảnh hưởng (`serve.ts` đã register consumer đầy đủ) |
| B11 | 2026-07-25 · fix-issues-296 (sponsor rewards store) | feature (chưa hoàn chỉnh) | `apps/landing` rewards flow | Luồng sponsor reward **chưa chạy end-to-end** và hiện KHÔNG có mặt nào lộ ra người dùng: `apps/landing` build `output: 'export'` (static) nên 2 route handler (`api/rewards/claim`, `api/webhooks/github-sponsors`) nằm ở `src/_api-routes-disabled/`, còn trang `/rewards/claim` đã bị xoá ở `31b2be9a` vì luôn 404. Issue #296 chỉ vá tầng lưu trữ (stub `saveToDatabase()` no-op + hai `Map` rời + claim không atomic) — store giờ đúng và chờ sẵn đúng cái "managed backend" mà commit xoá kia nói tới. Bật thật cần: server runtime (vd `@cloudflare/next-on-pages`) + wire `configureSponsorStore(resolveSponsorStore(env))` + binding D1 `SPONSORS_DB` + dựng lại UI claim | low | `open` | Store + migration + hướng dẫn wiring đã sẵn: `apps/landing/src/lib/rewards/`, `apps/landing/migrations/0001_sponsors.sql`, mục "Sponsor rewards store" trong `apps/landing/README.md`. Registry setup-impact #84 |
| B12 | 2026-07-25 · fix-issues-296 (sponsor rewards store) | vuln (minor) | `apps/landing/src/_api-routes-disabled/api/webhooks/github-sponsors/route.ts` | `crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expectedSignature))` **throw** khi 2 buffer khác độ dài (`signature` là header do client kiểm soát) ⇒ signature sai độ dài trả `500 Internal server error` thay vì `401`, và `WEBHOOK_SECRET` mặc định `''` nghĩa là thiếu env → mọi request vẫn được HMAC bằng secret rỗng. Không exploit được hiện tại vì route đang disabled (B11) | low | `open` | Vá khi bật lại route: check độ dài trước khi so sánh (hoặc so digest cùng độ dài) + fail-fast khi `GITHUB_WEBHOOK_SECRET` vắng |
| B13 | 2026-08-01 · deps-batch-followups (review #326) | bug (flake) | `apps/studio/src/modules/mission-control/__tests__/goals-actions.test.tsx` | Test `decomposes a goal with the entered sub-goals (Req 18.1)` timeout ở `await screen.findByRole('option', { name: 'writer' })` (dòng 87) khi chạy **full suite** trên Node 24 — đo được 1/3 lần chạy; chạy riêng lẻ pass, 2 lần chạy full sau đó pass. File KHÔNG bị PR #326 đụng, nên là flake nhạy tải (default timeout 1000ms của `findBy*` so với select role fill async) chứ không phải regression React 19 — nhưng chưa loại trừ được việc React 19 làm chậm thêm. Hệ quả: CI đỏ lẻ tẻ, không tái lập được theo yêu cầu | medium | `fixed` | Vá ở `feat/user-2fa`: `configure({ asyncUtilTimeout: 5_000 })` trong `apps/studio/src/test/setup.ts`. Nguyên nhân gốc là timeout **của Testing Library** (default 1000ms), độc lập với `testTimeout: 15_000` ở `vitest.config.ts` — nên `findBy*` bỏ cuộc trong lúc component còn "Loading…". Sửa suite-wide thay vì per-test vì cả class đều nhạy tải: quan sát được 6 file khác nhau fail giữa các lần chạy hook (kể cả `api-keys-allowed-origins.test.tsx`), luôn pass khi chạy riêng. Không nới `testTimeout`, không đụng test nào |
| B14 | 2026-08-01 · deps-batch-followups (review #326) | task (deps) | `apps/cms` — `graphql` / `graphql-yoga` | `graphql-yoga@5.21.2` khai peer `graphql: ^15.2.0 \|\| ^16.0.0` nhưng workspace cài `graphql@17.0.2` (vào main từ #312) → **peer violation đang tồn tại trên main**. 28 test `src/graphql` vẫn pass nên chưa nổ, nhưng yoga 5 không chính thức hỗ trợ graphql 17; rủi ro nằm ở path chưa có test bao (error formatting, subscription) | medium | `fixed` | Chờ `graphql-yoga` ra bản có peer `^17`, hoặc pin `graphql` về `^16` cho tới lúc đó. Không thuộc scope #326 (bump graphql là PR khác) và không thuộc scope follow-up review này. **Cập nhật 2026-08-21 (batch #396'):** vẫn mở. `pnpm install` của batch in ra đúng violation này (`graphql-yoga 5.21.2 → unmet peer graphql@"^15.2.0 || ^16.0.0": found 17.0.2`). #399 bump yoga lên 5.21.3 nhưng peer range **không đổi**, nên nó không đóng được mục này — đã tuyên bố out-of-scope trong `.kiro/specs/dependency-upgrade-batch-2026-08/requirements.md` Req 9.1. **Đóng 2026-08-31 (#442):** upstream đã ra bản cần chờ — `graphql-yoga@5.22.0` khai peer `graphql: ^15.2.0 \|\| ^16.0.0 \|\| ^17.0.0`. Verify bằng lockfile theo DoD §2e (không bằng manifest): importer `apps/cms` resolve `graphql 17.0.2` và block `graphql-yoga@5.22.0` nay liệt `^17.0.0` trong `peerDependencies`, nên `pnpm install` không còn in unmet peer. Không phải pin `graphql` về `^16` |
| B15 | 2026-08-01 · deps-batch-followups (review #326) | task (infra) | `package.json` — `pnpm.overrides` / `pnpm.patchedDependencies` / `pnpm.auditConfig` | pnpm 10 **không còn đọc** field `pnpm` trong `package.json` (`[WARN] The "pnpm" field in package.json is no longer read by pnpm … keys were ignored`, thấy khi corepack shim pnpm 10.x chạy trước khi delegate về bản pin). Hiện **chưa vỡ** vì `packageManager: pnpm@9.12.0` ghim lại, install thật vẫn là 9.12.0 nên overrides + patch vẫn áp. Nhưng #326 vừa làm repo **phụ thuộc thêm** vào chính cơ chế đó: override `@types/react`/`@types/react-dom` = `19.2.0` là thứ giữ `@lumibase/ui` typecheck cùng React major với runtime (ghi rõ trong `docs/{en,vi}/security/dependency-overrides.md`). Khi nào bump `packageManager` lên pnpm 10 → override biến mất im lặng (`@lumibase/ui` tụt về `@types/react` 18) và patch `gray-matter` không apply | medium | `open` (tiền đề đã xong) | Di chuyển 3 key sang `pnpm-workspace.yaml` (nơi pnpm 10 đọc) **trước** khi bump `packageManager`. **Cập nhật 2026-08-21:** tiền đề đã xong — #397 (đã merge) khai cả 3 key trong `pnpm-workspace.yaml` và thêm gate `pnpm settings:check` chặn hai bản khai trôi lệch. Bump `packageManager` lên pnpm 10 giờ **an toàn**: override + patch + audit-ignore sẽ vẫn áp sau khi pnpm 9 ngừng đọc key `pnpm` trong `package.json`. Còn lại đúng một việc: bump `packageManager` rồi xoá key `pnpm` khỏi `package.json` cùng `scripts/check-pnpm-settings-parity.mjs`. Warning của pnpm 10 vẫn quan sát được khi shim chạy trước lúc delegate về 9.12.0 |
| B17 | 2026-08-20 · landing intent composer | task (test gap) | `apps/landing/vitest.config.mts` | Landing chạy vitest với `environment: 'node'` và `include: ['src/**/*.{test,spec}.ts']` — chỉ `.ts`, không `.tsx`, và không có `@testing-library/react` trong workspace. Hệ quả: **không thể** viết test hành vi cho component landing; PR này phải tách toàn bộ logic ra `src/lib/intent-compile.ts` để test được (51 test), còn phần tương tác (beat machine, reduced-motion, clipping trong `FeatureCard`) chỉ verify được bằng Chrome headless chạy tay qua CDP — không có gate CI nào bắt nếu ai đó làm hỏng. Khác với `apps/studio` (đã có test `.tsx`) | medium | `open` | Fix = thêm `jsdom` + `@testing-library/react` cho landing, mở `include` sang `.tsx`, rồi thêm test cho `IntentComposerViz` (click → beat → payload) và một assertion chống clipping. Không làm trong PR này vì kéo theo dependency mới cho một app marketing. **Cập nhật 2026-08-24 (landing GA consent, nâng low → medium):** giờ có surface thứ hai không test được và lần này là **compliance**, không chỉ visual — `Analytics.tsx` + `CookiePreferences.tsx` là chỗ quyết định GA4 có load hay không. Logic thuần đã tách ra `src/lib/analytics/*.ts` và được 33 test bao (gating, validate measurement ID, snippet consent), nhưng **không có gate CI nào** chứng minh: (a) `<Script>` GA không render khi consent `null`/`denied`, (b) banner không xuất hiện khi đã có quyết định, (c) nút "Change my choice" thật sự re-open banner. Ai đó đảo một điều kiện trong JSX thì test hiện tại vẫn xanh. Hiện chỉ verify được bằng build + `grep -rl googletagmanager out/` (chứng minh bản không cấu hình GA là sạch). **Cập nhật 2026-08-24 (mở rộng sang docs):** logic đã chuyển vào `packages/analytics-consent` (26 test) và **`apps/docs` nay CÓ** component test jsdom cho đúng ba assertion đó (`src/components/__tests__/analytics-consent.test.tsx`, 9 test) vì app đó đã có `jsdom` + `@testing-library/react`. Nghĩa là mẫu test cần viết giờ đã tồn tại sẵn trong repo — port sang landing chỉ còn là việc thêm dependency + mở `include` sang `.tsx`, không phải thiết kế lại |
| B16 | 2026-08-01 · cache-penetration (Req 19) | task (gap) | `lumibase_pages` write path | Spec task 22.6 yêu cầu `forget(neg:…:page:…)` sau tạo page / đổi slug, nhưng CMS trước đây **không có** route/service tạo/sửa `pages` (chỉ insert trực tiếp trong e2e/golden-path). | medium | `fixed` | `PageService` + `routes/pages.ts` (`/api/v1/pages`) — `forgetNegative` sau create và sau rename (slug cũ + mới); test `page-service.test.ts` |
| B20 | 2026-08-21 · dependency-upgrade-batch-2026-08 | bug (silent) | `pnpm.overrides` ↔ manifest | `overrides.vite` ở `^7.3.5` trong khi `apps/studio` + `apps/docs` khai `^8.1.3`. Override của pnpm áp **cả direct dependency**, nên lockfile importer ghi `specifier: ^7.3.5 → 7.3.6` — hai app build bằng **Vite 7** suốt thời gian manifest tuyên bố Vite 8. `settings:check` không bắt được vì nó chỉ so hai bản khai override với nhau, và chúng khớp nhau; chúng chỉ cùng sai so với manifest | medium | `fixed` | Nâng `overrides.vite` → `^8.2.0` cùng manifest (importer nay resolve 8.2.2). Cơ giới hoá cả class: `scripts/check-override-drift.mjs` + gate CI `pnpm drift:check` + test `node --test` gồm chính ca vite. DoD §2e mới. Docs: mục "Override drift" trong `docs/{en,vi}/security/dependency-overrides.md` |
| B21 | 2026-08-21 · dependency-upgrade-batch-2026-08 | task (deps) | `apps/{landing,consumer}` — `eslint-config-next` | Plugin của `eslint-config-next@16.3.1` (`eslint-plugin-import@2.32.0`, `eslint-plugin-react@7.37.5`, `eslint-plugin-jsx-a11y@6.10.2`) đều khai peer chặn ở `eslint ^9`, nhưng workspace cài `eslint@10.6.0` → **4 peer violation** in ra ở mỗi `pnpm install`. Lint vẫn pass thật (đã chạy riêng cả hai app, exit 0) nên đây là gap **khai báo**, không phải vỡ runtime. Đã tồn tại trên `main` cho `apps/consumer` (khai `eslint: ^10` từ #326); batch này mở rộng sang `apps/landing` khi thống nhất eslint về một major | low | `open` | Chờ plugin ra bản khai peer `^10` (upstream `eslint-config-next`). Không hạ eslint về 9 chỉ để làm im peer: hai major eslint trong một workspace là drift đắt hơn. Xem lại khi bump `eslint-config-next` lần tới |
| B22 | 2026-08-21 · dependency-upgrade-batch-2026-08 | task (forward-compat) | `apps/studio/vitest.config.ts` | Vite 8 cảnh báo config dùng `__dirname` (dòng 23) không được `configLoader: 'native'` hỗ trợ — và native sẽ thành **default ở một major Vite tương lai**. Hiện chỉ là warning, build/test vẫn pass. **Cập nhật 2026-08-30 (rà batch dependabot):** rộng hơn mô tả ban đầu — `apps/docs/vitest.config.ts` cũng đỏ cảnh báo, và không chỉ vì `__dirname` (dòng 15) mà còn `JSON import "./package.json" without import attributes` (dòng 4). Nghĩa là "rà các config khác" ở cột fix không phải việc phòng xa mà là việc đang tồn tại | low | `open` | Đổi `__dirname` → `import.meta.dirname` và thêm `with { type: 'json' }` cho import JSON. Rà tất cả vite/vitest config trong workspace trước khi native thành default, đừng chờ nó vỡ ở lần bump major sau |
| B23 | 2026-08-21 · dependency-upgrade-batch-2026-08 | task (process) | Dọn PR sau batch | Khi đóng #369–#381 là "absorbed vào #396", **ba PR bị đóng sai lý do**: #396 chỉ gộp npm deps nên không đụng version action (#369 `upload-artifact` 4→7, #371 `scorecard-action` 2.4.3→2.4.4), và nó dừng `nanoid` ở 5.1.16 để đóng advisory chứ không lên 6.x (#377). Không reopen được vì branch dependabot đã bị xoá lúc đóng. Bài học: "gộp vào PR X" phải verify trên `main` sau merge cho **từng** PR, không suy ra từ nhãn nhóm | low | `fixed` (một phần) | Đã đăng comment sửa lại trên cả 3 PR. #369 + #371 làm trực tiếp trong `chore/action-bumps-leftover`. #377 (`nanoid` 5→6) **vẫn mở** → xem B24 |
| B24 | 2026-08-21 · dependency-upgrade-batch-2026-08 | task (deps) | root `overrides` — `nanoid` | `nanoid` 5→6 (dependabot #377, đã đóng nhầm và không reopen được) chưa được đánh giá. Hiện `apps/cms` + `packages/database` khai `^5.0.7`, override scoped ghim `nanoid@5: ^5.1.16`, còn `next → postcss` giữ nhánh 3.x qua `nanoid@3: ^3.3.17` | low | `fixed` | Đóng ở #419 (`^5.1.16` → `^6.0.1` cho `apps/cms` + `packages/database`). Hoá ra **không** cần thêm scope `nanoid@6`: 6.0.1 là bản mới nhất nên không có sàn advisory nào phải ép, và `drift:check` không đỏ vì override scoped `nanoid@5` chỉ nói cho range 5.x. Ngược lại, scope đó thành **override chết** — lockfile không còn nanoid 5 nào — nên nó được xoá khỏi cả `package.json` và `pnpm-workspace.yaml` cùng dòng registry trong `docs/{en,vi}/security/dependency-overrides.md` (đúng "Remove when" mà dòng đó đã ghi sẵn). Đã verify: ESM-only bundle được vào worker CF (không có `require("nanoid")` trong `dist/cloudflare.js`), và chỉ dùng `nanoid()` / `nanoid(n)` |
| B25 | 2026-08-24 · release 0.26.0 (rà nhánh local) | task (deps) | `chore/deps-consolidated-upgrade` (nhánh local, 4 commit, 2026-08-11) | Nhánh local gom các Dependabot major đang mở (`workers-types` v5, `lucide-react` v1, `fast-check` v4, `uuid` ^14, `vite` ^8) kèm phần source adaptation. **Đã bị #396 vượt qua hoàn toàn** (2026-08-21, "consolidate all open dependabot PRs"): main hiện ở `vite ^8.2.0`, `uuid ^14.0.1`, `lucide-react ^1.28.0`, `@cloudflare/workers-types ^5.20260801.1`, `fast-check ^4.9.0` — đúng các đích của nhánh — và #396 sửa luôn cùng bộ file mà nhánh đã sửa (`runtime/src/adapters/cloudflare/storage.ts`, `git-integration/ci-log-store.ts`, `routes/extensions.ts`, `landing/src/components/Header.tsx`, các property suite fast-check). Phần bảo mật cũng đã đóng ở #385/#397. Ghi lại vì nó là một bài học quy trình: nhánh này ngồi local 13 ngày rồi bị làm lại từ đầu bởi một PR khác — công đôi lần vì không ai biết nó tồn tại | low | `wontfix` | Superseded bởi #396. Nhánh local an toàn để xoá (không còn commit nào mang giá trị riêng). Bài học: đẩy nhánh lên remote + mở draft PR ngay khi có commit đầu tiên, kể cả khi chưa xong — nếu không thì Dependabot (hoặc người khác) sẽ làm lại đúng việc đó |
| B26 | 2026-08-30 · user-totp-2fa (rà UI trước khi chụp ảnh PR) | bug (class) | `apps/studio/src/modules/settings/encryption-page.tsx` · `security-audit-tab.tsx` · hook gọi `/api/v1/translations` | Cả một họ trang Studio fetch bằng `credentials: 'same-origin'` **không** kèm `Authorization: Bearer` — nhưng `withAuth` (`apps/cms/src/middleware/auth.ts`) chỉ đọc header `authorization`, KHÔNG có nhánh cookie nào. Verify trên server đang chạy: `/api/v1/admin/encryption/envelope`, `/api/v1/admin/security/audit-log`, `/api/v1/translations` đều trả **401** khi không có bearer; browser log của Studio cũng cho `401 GET /api/v1/translations` ở mọi lần load. Comment trong `security-audit-tab.tsx` ("uses `credentials: 'same-origin'` so the session cookie rides along") mô tả một cơ chế không tồn tại. Test component pass vì đều mock `fetch`. Cùng class với bug vừa vá ở `security-page.tsx` của PR này (đã sửa: `getActiveToken()` + `getApiBaseUrl()`), nhưng 3 chỗ kia nằm ngoài scope PR 2FA và **đã có trên `main`** | high | `open` | Sửa: dùng `getApiClient()` hoặc thêm `Authorization: Bearer ${getActiveToken()}` + `getApiBaseUrl()` (contract C2, DoD §2d) cho từng trang; bỏ comment nói về cookie. Nên cơ giới hóa: tripwire source-scan chặn `fetch('/api/v1/...` không kèm bearer trong `apps/studio/src` (giống `security-guards.wiring.test.ts`) — nếu không, class này sẽ tái diễn ở trang settings tiếp theo |
| B27 | 2026-08-30 · user-totp-2fa (rà threat model chỗ lưu seed) | bug (DX/ops) | `apps/cms/src/routes/tfa.ts` · `packages/runtime/src/adapters/shared-keys.ts` | Khi KEK không có sẵn, mọi đường 2FA trả **500 INTERNAL** đục. Hai ca đo được trên server thật: (a) không set `ENCRYPTION_KEY` → `POST /me/tfa/setup` 500 (`getActiveKey` throw, shared-keys.ts:67); (b) seed enroll dưới `v0` rồi rotate sang chỉ còn `ENCRYPTION_KEY_v1` → `verify-totp`, `DELETE /me/tfa`, `POST /me/tfa/recovery-codes` **đều 500** (`getKey` throw, shared-keys.ts:77). Ca (b) thành **dead end**: recovery code vẫn login được (PBKDF2, không bọc KEK — verify 200) nhưng tháo/regenerate/enroll lại đều đòi một TOTP code sống ⇒ hết 8 code là mất quyền vào Studio realm. Handler chỉ catch `TotpError`, throw của KeyProvider rơi xuống error boundary chung. **KHÔNG phải lỗ hổng**: không có nhánh nào ghi seed thô, fail-closed đúng như CWE-321 đã chốt | medium | `fixed` | Issue #429 — vá trên `docs/totp-key-findings-backlog`: `withKeyErrors` dịch throw của KeyProvider thành `TotpError`, routes map `ENCRYPTION_NOT_CONFIGURED` → 503 và `TFA_KEY_UNAVAILABLE` → 409 (message nêu key id), `DELETE /me/tfa` nhận recovery code thay TOTP code + UI Studio có lối đó, `recovery-codes` cố ý vẫn đòi TOTP code. Test `modules/mfa/__tests__/totp-key-errors.test.ts`; đã verify lại trên server thật (409/409/200/200 thay cho 500/500/500). Đề xuất còn lại (ép `ENCRYPTION_KEY` ở setup wizard) vẫn mở. Đề xuất: lỗi có mã (`ENCRYPTION_NOT_CONFIGURED` 503 / `TFA_KEY_UNAVAILABLE` 409) + cho `DELETE /me/tfa` chấp nhận password + recovery code khi seed không giải được + ép `ENCRYPTION_KEY` ở setup wizard (đúng việc `v1-release-criteria` §1b treo cho CWE-321) |
| B28 | 2026-08-30 · user-totp-2fa (rà threat model chỗ lưu seed) | task (docs) | `envelope-migration-worker.ts` ↔ `user_totp_credentials` · `docs/en/security/user-management.md` §4f | Vòng đời KEK của seed TOTP chưa được ghi ở đâu, và nó **khác** với item field: worker re-wrap envelope chỉ đi qua `items`/`collections` (import ở dòng 2), KHÔNG chạm `lumibase_user_totp_credentials` — nên sau `POST /admin/encryption/envelope/migrate` seed vẫn giữ `secret_key_id` cũ và **khoá cũ phải giữ mãi**. Thêm nữa KEK là một khoá dùng chung toàn deployment, không có KDF theo user (AAD `totp-secret\|<userId>` chỉ chống bê ciphertext sang user khác, không phải biên bảo mật) ⇒ **lộ `ENCRYPTION_KEY` thì rotate không cứu được**, phải ép toàn bộ user enroll lại. Bản chất TOTP (server phải giữ shared secret khôi phục được), không phải lỗi thiết kế — nhưng operator sẽ hiểu sai chữ "rotate" nếu không viết ra | medium | `tracked` | Issue #430. Cần: mục "TOTP key lifecycle" ở `user-management.md` §4f, đánh dấu `ENCRYPTION_KEY` là required-before-first-enroll + never-removable trong `environment-variables.md`, runbook KEK-compromise ở `docs/en/operations/`, kèm bản VI (CLAUDE.md rule #7) |
| B29 | 2026-08-24 · landing/docs GA consent | task (gap · blocked) | `apps/marketplace` (submodule private `lumibase-ai/marketplace`) | Yêu cầu ban đầu là bật GA4-sau-consent cho **cả ba** site public: landing, docs, marketplace. Hai site đầu đã xong trong PR này; marketplace **không làm được từ repo này** — submodule chưa checkout (`git submodule status` cho `-5958c50e…`, dir chỉ còn `.next`/`out`/`node_modules` cũ, không có `.git` lẫn `src`) và remote không đọc được bằng cả hai account đang login (`git ls-remote` qua SSH và HTTPS đều trả `Repository not found`; `gh repo view lumibase-ai/marketplace` → `Could not resolve to a Repository`). Hệ quả: `marketplace.lumibase.dev` (hoặc domain tương ứng) vẫn **không có** banner consent lẫn control opt-out, trong khi privacy policy ở landing giờ đã mô tả cơ chế consent như đặc tính chung của các site public → policy nói rộng hơn thực tế đúng một site | medium | `open` | Sửa trong repo `lumibase-ai/marketplace`: nó là Next app (build ra `apps/marketplace/out`) nên copy nguyên mẫu của `apps/landing` — `<Analytics>` + `<CookiePreferences>` dùng `@lumibase/analytics-consent`. **Lưu ý ràng buộc:** marketplace bị loại khỏi pnpm workspace (`pnpm-workspace.yaml: "!apps/marketplace"`) và install bằng `--ignore-workspace`, nên KHÔNG dùng được `workspace:*` — hoặc publish package này lên registry, hoặc copy logic kèm chú thích trỏ về `packages/analytics-consent` làm nguồn. Cần thêm `NEXT_PUBLIC_GA_ID` vào step build marketplace ở `release.yml` + `pages-deploy.yml` (hiện chỉ truyền cho step build của landing/docs) |
| B30 | 2026-08-30 · rescue blog SSRF khỏi worktree cũ | bug (flake) | `apps/docs/src/components/__tests__/analytics-consent.test.tsx` (`beforeEach` dòng 34) | Cả 9 test của file đỏ với `TypeError: Cannot read properties of undefined (reading 'clear')` tại `localStorage.clear()` — kèm warning `localStorage is not available because --localstorage-file was not provided`, tức global `localStorage` bị resolve về builtin experimental của **Node 24** (undefined) thay vì `localStorage` của jsdom. Đo được **1/2 lần** chạy `pnpm test` (turbo, 12 package song song) trên `main` sạch tại `b124d953`; ngay sau đó: file chạy riêng **pass** (9/9), full suite `apps/docs` chạy riêng **pass** (24 file · 157 test), và `pnpm test` toàn repo chạy lại **pass** (12/12 task). Không tái lập theo yêu cầu ⇒ flake nhạy tải, không phải regression. Cùng class với B13 nhưng nguyên nhân khác: đây là **global bị thiếu**, không phải timeout của `findBy*`. File do PR #426 (GA consent) thêm, nên chưa từng chạy qua nhiều chu kỳ CI | medium | `open` | Hệ quả: pre-commit hook (`husky` → `check:all` + `pnpm test`) và CI job `test` đỏ ngẫu nhiên, chặn commit ở máy local dù diff không liên quan (gặp khi commit 3 file `blog/`). Fix: không dựa vào global `localStorage` của môi trường — stub tường minh trong `beforeEach` (hoặc `vi.stubGlobal('localStorage', …)`), hoặc set `environmentOptions`/`pool` cho `apps/docs` sao cho jsdom luôn thắng builtin của Node 24. Nếu đã chuẩn hoá Node 24 toàn team thì rà cả các test khác đọc `localStorage`/`sessionStorage` trực tiếp — cùng một class |
| B33 | 2026-08-30 · rà batch dependabot (#411–#421) | bug (guard chết) | `scripts/version-check.mjs` | Script **không được gọi từ đâu cả** — không có trong `scripts` của `package.json` (`pnpm version:check` chạy `sync-version.mjs --check`, một file khác), không trong workflow nào, không trong `.husky/pre-commit` — và nếu chạy thì nó **fail**: nó assert `engines.node === '>=20'` trong khi root khai `>=22.13.0` (nay là `^22.22.2 \|\| ^24.15.0 \|\| >=26.0.0`). Nó cũng assert `packageManager` và `lockfileVersion`. Hệ quả: hàng rào cơ giới duy nhất canh `engines.node` không hề chạy — phát hiện đúng lúc jsdom 30 nâng sàn Node và không gate nào nói gì | medium | `open` | Chọn một trong hai, đừng để lửng: (a) nối vào `check:all` + đổi expectation `engines.node` thành hằng đọc từ chính `package.json` hay một sàn khai tường minh, hoặc (b) xoá file và thừa nhận `sync-version.mjs` là guard duy nhất. Nếu chọn (a) thì đây là chỗ đúng để cơ giới hoá DoD §2e: so `engines.node` của repo với `engines.node` của mọi toolchain đã cài, thay vì rà tay |
| B31 | 2026-08-30 · rà batch dependabot (#411–#421) | bug (CI) | `.github/workflows/perf-k6.yml` | Workflow đỏ ở **mọi** commit push lên `main` — kiểm 30 run gần nhất (2026-08-29 → 08-30), không có một `success` nào, kể cả trên `b124d953` (trước batch này). Không phải do batch. Vì nó không phải required check nên không chặn merge, nhưng một workflow đỏ vĩnh viễn làm mờ tín hiệu: người ta học cách bỏ qua màu đỏ, đúng lúc một cái đỏ thật xuất hiện. Liên quan `v1-release-criteria.md` §7 (k6 baseline "nên có, không chặn tag") và các task k6 đang hoãn của `high-load-cache-readiness` | low | `fixed` | **Nguyên nhân không phải thiếu môi trường tải mà là file workflow không parse được.** `on:` của nó vốn KHÔNG khai `push` (chỉ `workflow_dispatch` / `schedule` / `pull_request: [labeled]`), nhưng job `perf-gate` dùng `env.PERF_K6_FULL_RUN` trong `if:` **cấp job**, nơi context `env` không tồn tại (chỉ có `github`, `inputs`, `needs`, `vars`). Biểu thức sai không "đánh giá thành rỗng" — nó làm **cả file invalid**: GitHub không parse được nên không resolve nổi `name:` (API trả `name` = đúng đường dẫn file, dấu hiệu nhận biết), không tạo job nào, và ghi một run failed cho **bất kỳ** event nào, kể cả event mà `on:` không khai. Fix = `vars.PERF_K6_FULL_RUN`; sau đó workflow tôn trọng `on:` và không còn chạy trên push. Verify bằng `actionlint` (0 finding trên toàn bộ `.github/workflows`, gồm cả shellcheck). Chống tái diễn cả class bằng job CI mới `workflow-lint` chạy `actionlint` (pin version + sha256) — vì lý do thật sự khiến nó sống được nhiều tuần là **không có gì lint chính các workflow** |
| B32 | 2026-08-30 · rà batch dependabot (#411–#421) | bug (flake · class) | `.husky/pre-commit` ↔ `pnpm test` (turbo concurrency mặc định) | Nối tiếp B13: nâng `testTimeout` lên 15s **không** đóng được class, chỉ nâng trần. Đo tại commit này: `pnpm test` ở concurrency mặc định của turbo cho 6–8 fail rải khắp `apps/studio` (`field-inspector`, `fields-tab`, `materialize-page`, `marketplace-publish`, `security-audit-tab`, `setup-state-gate`, `mission-control`, `backup-code-page`) và `apps/cms` (`flow-service`, `backup-codes-persister`, `recovery/service`, `audit/routes`) — **tất cả** đều là `Test timed out in 15000ms`, tức là chạm đúng cái trần B13 vừa nâng. Cùng bộ đó với `TURBO_CONCURRENCY=1` thì **12/12 task xanh, 0 fail**; chạy từng package riêng cũng xanh. Nguyên nhân là tranh tài nguyên, không phải test sai. Nguy ở chỗ `.husky/pre-commit` gọi đúng `pnpm test`: commit hợp lệ bị chặn ngẫu nhiên, và cách chữa tự nhiên nhất mà người ta sẽ chọn là `--no-verify` — tắt luôn hàng rào. Đã gặp thật trong lần commit này | medium | `open` | Đừng nâng `testTimeout` lần nữa (đó là cách B13 thất bại). Giới hạn song song thay vì giới hạn thời gian: đặt `concurrency` cho task `test` trong `turbo.json` hoặc `TURBO_CONCURRENCY` trong `.husky/pre-commit`, và/hoặc hạ `poolOptions.maxThreads` cho hai suite jsdom nặng nhất. Cân nhắc cho pre-commit chạy tập bị ảnh hưởng thay vì toàn bộ suite, để hàng rào không đắt tới mức bị vòng qua |
| B34 | 2026-08-30 · rebase #441 lên main | bug (class) | `.kiro/steering/out-of-scope-backlog.md` ↔ `scripts/check-registry-numbering.mjs` | ID của **chính bảng này** không được kiểm trùng. #434 và #436 cùng cấp `B30` cho hai finding không liên quan (flake `localStorage` của `analytics-consent.test.tsx` vs guard chết `version-check.mjs`); PR merge sau phải renumber tay thành `B33`, và va chạm chỉ lộ ra dưới dạng conflict lúc rebase — không gate nào nói gì. Đúng cùng class mà `registry:check` đã cơ giới hoá cho cột `#` của Setup Impact Registry (từng có hai dòng #20/#31/#32), chỉ khác là bảng này bị bỏ sót. Nguy hơn vẻ ngoài vì ID backlog được **trích dẫn theo số** ở dòng khác ("Nối tiếp B13", "cùng class với B10", "Xem B24") và trong CHANGELOG, nên một lần renumber im lặng làm gãy tham chiếu mà không test nào phủ | medium | `fixed` | Mở rộng `scripts/check-registry-numbering.mjs` để kiểm **cả hai** bảng (`#` của setup-impact và `B<n>` của file này), báo lỗi kèm số kế tiếp an toàn và nhắc giữ lại occurrence đang được dòng khác trích dẫn. Đã verify hai chiều: chèn `B30` trùng → exit 1 đúng dòng; bỏ ra → exit 0. Chạy sẵn trong `pnpm check:all` (và do đó cả pre-commit + CI) nên không cần thêm entry mới |
| B35 | 2026-08-31 · dọn PR mở (#442/#439/#438/#337/#443) | task (test gap) | `.github/workflows/release-apps.yml` | Workflow này chỉ trigger `push: tags: v*.*.*`, nên **không PR nào chạy nó** — CI trên PR chỉ kiểm được cú pháp YAML qua gate `Workflow files are valid` (#441), không kiểm hành vi. #438 vừa bump `actions/setup-java` 5 → 6 ở đây, và v6 có commit *"Set default signature verification for supported distributions"* ([#1246](https://github.com/actions/setup-java/pull/1246)) tức bật verify chữ ký JDK theo **mặc định**. Config đã ghim `distribution: temurin` + `java-version: "17"` nên khả năng vỡ thấp, nhưng nếu vỡ thì lộ ra đúng lúc đang cắt release, trên đường build desktop/mobile của `apps/shell` — thời điểm đắt nhất. Cùng class với B31 (`perf-k6.yml` đỏ hàng tuần mà không ai thấy): workflow không được exercise thì không ai biết nó còn chạy hay không. `shell-check.yml` **không** dùng Java nên không che được chỗ này | medium | `open` | Hai hướng, không loại trừ nhau: (a) thêm `workflow_dispatch` vào `release-apps.yml` rồi chạy tay một lần sau mỗi lần bump action, ít nhất tới bước `Setup Java` + `Setup Android SDK`; (b) tách phần setup toolchain sang một job mà `shell-check.yml` cũng gọi được, để PR có che. Cho tới lúc đó: theo dõi run `Release Apps` ở release kế tiếp và đừng coi tag xanh là mặc định |
| B36 | 2026-08-31 · dọn PR mở (#428 → #443) | task (process) | Triage PR "superseded" | Đóng PR vì trùng lặp mà **không đo delta còn lại** thì mất việc thật. #428 bị đóng là "superseded by #360" — đúng cho 14/15 file, nhưng #360 là bản **squash** của cùng branch `oak-maxwell` lấy ở thời điểm sớm hơn, nên hai commit sau đó (`0a663a56` "make Phase 0 measurements reproducible", `64702033`) không nằm trong #360 và bị bỏ lại cùng lúc branch bị xoá. Phần bị bỏ lại là một fix thật: `tags: { scenario: 'detail' }` ghi đè tag mà k6 tự sinh từ tên scenario, làm sub-metric `http_req_failed{scenario:detail_throughput}` — đúng cái threshold của nó trỏ vào — rỗng, tức threshold không thể fail. Cùng class "guard trông có mà không bao giờ fire" với B31. Cứu được vì đo `git diff origin/main HEAD --stat` sau khi merge `main` vào branch, ra đúng `apps/cms/k6/load-items.js \| 1 -`; nếu tin nhãn "duplicate" thì mất | low | `fixed` | Fix đã land ở #443. Quy tắc rút ra: trước khi đóng PR là superseded, merge `main` vào branch rồi chạy `git diff origin/main HEAD --stat` — diff rỗng mới là superseded thật; khác rỗng thì tách phần dư thành PR riêng thay vì reopen (title PR cũ mô tả việc đã land). Rủi ro này sinh ra từ chính thói quen squash-merge một branch còn đang được commit tiếp — nên cũng nên xoá branch **ngay khi** squash-merge, đừng để nó nhận thêm commit |
| B37 | 2026-09-02 · rebase #330 (contracts rename) | task (docs debt) | 4 cặp `docs/{en,vi}` lệch cấu trúc mà `detect` báo up-to-date | `check-parity.mjs` thấy bốn cặp **không phải cùng một tài liệu**, nhưng `docs:i18n:detect` vẫn xếp chúng `up-to-date` vì stamp còn khớp hash — nên lệch nằm im. Chỉ lộ ra khi #330 đổi `@lumibase/shared`→`@lumibase/contracts` làm hash đổi, buộc phải re-stamp, và `stamp-pair.mjs` (chạy parity chặt hơn) từ chối. Đã verify **giống hệt** trên `origin/main` sạch, nên có sẵn, không do rename: (a) `contributing/testing.md` — EN 17 heading / VI 14, EN 12 code fence / VI 11, VI thiếu link `https://k6.io/`; (b) `architecture/realtime-websocket-implementation.md` — EN 17 heading / VI 16, VI thiếu 40 inline-code (`SiteRoom`, `resolveRoomName()`, `realtime-audience-channels`…), tức VI thiếu hẳn mục "studio plane" cuối bài (VI 254 dòng / EN 267); (c) `features/collection-preview.md` — code block #2 (jsonc) khác nội dung; (d) `features/cdc-change-feed.md` — code block #2 (jsonc) + #4 (ts) khác, cộng `docs/en/cdc/` vs `docs/vi/cdc/`. Riêng cặp (d) phần path là **false positive** (khác locale thì path phải khác); ba cặp còn lại là thiếu dịch thật | medium | `open` | #330 stamp bốn cặp bằng `--allow-structure-drift` để không kéo việc dịch vào một PR rename — cố ý, không phải bỏ qua. Việc còn lại: dịch bù cho (a)(b)(c), và cho `check-parity.mjs` bỏ qua chênh lệch chỉ do tiền tố `docs/<locale>/` để (d) không còn kêu. Đáng chú ý hơn cả: `detect` và `stamp-pair` **không cùng một mức chặt** — cặp lệch cấu trúc vẫn `up-to-date` chừng nào chưa ai chạm vào file. Cân nhắc cho `docs-i18n-sync.yml` chạy `check-parity` trên toàn cây (hiện gọi kèm `|| true`, xem DoD §4a) để lệch kiểu này không phải chờ một PR vô tình mới phát hiện |
| B38 | 2026-09-05 · landing footer and typography | bug (DX) | root `package.json` · `apps/landing/package.json` | Root `landing:preview` invokes a `preview` script that the landing package does not define. The app uses Next static export, so `next start` is not a substitute. Browser QA used a static server over `apps/landing/out/`. | low | `open` | Add a static-export preview command and make the root alias call it. |
| B39 | 2026-09-05 · landing footer browser QA | bug (layout) | existing landing decorative scenes / page overflow | At a 390px browser viewport with a 375px content area, the homepage has a 382px scroll width (7px horizontal overflow); the footer itself stays within 375px. Existing decorative scene elements extend beyond the content edge. Desktop also shows a small horizontal scrollbar. | low | `open` | Trace the existing scene overflow and clip it locally without breaking sticky scroll scenes or intentional code-panel scrolling. |
| B40 | 2026-08-31 · rà workflow trên main sau batch dependabot (rebase 2026-09-06) | vuln (availability) | `packages/runtime/src/index.ts` · `apps/cms/src/middleware/runtime.ts` · `apps/cms/wrangler.toml` | **Mọi deploy Cloudflare bị Cloudflare từ chối** kể từ khi bullmq 6 vào main (#421, 2026-08-30): `Uncaught Error: Could not determine sql-loader directory path`, validation error 10021. Nguyên nhân: `packages/runtime/src/index.ts` re-export **cả** `./adapters/docker` (và `./factory` = `createRuntime`, và `./leader-lock` import `ioredis` như **value**), còn `middleware/runtime.ts` import `createRuntime` từ barrel → bundle Worker kéo theo cả cây docker gồm `bullmq`. BullMQ 6 thêm backend Postgres, và `dist/esm/postgres/sql-loader.js` gọi `getDirname()` ở **top level module**, throw khi không có `__dirname` lẫn frame `file:///` — đúng môi trường Worker đã bundle. Worker throw lúc khởi tạo ⇒ script bị từ chối. **Vì sao mọi gate đều xanh:** `pnpm build` chạy `wrangler deploy --dry-run`, nó *bundle mà không instantiate*; job `build` của CI vì thế không thể thấy class lỗi "throw ở top level". Đây cũng là chỗ tôi kết luận sai khi rà #421 — coi "build pass + round-trip Redis thật" là đủ, trong khi chưa có gì boot Worker. Chỉ `deploy-cms.yml` (dev/staging) đỏ; production do `release.yml` sở hữu (chỉ chạy khi tag) nên chưa vỡ nhưng sẽ vỡ ở lần cắt release tới | critical | `fixed` | Tách entry point: root barrel chỉ còn thứ an toàn cho Worker; `@lumibase/runtime/docker` (adapter docker) và `@lumibase/runtime/node` (`createRuntime` + leader lock) là subpath riêng. `middleware/runtime.ts` static-import **chỉ** `createCloudflareRuntime`, nhánh docker đi qua `await import()`. Nhưng dynamic import **vẫn là static edge** với bundler (esbuild inline target — đo được: bundle vẫn chứa bullmq), nên thêm `[alias]` trong `wrangler.toml` map subpath docker sang stub `runtime-docker-unavailable.ts` (throw kèm mô tả nếu nhánh bất khả thi kia chạy). Kết quả đo: upload Worker **8848.98 KiB → 6085.28 KiB** (số trước lấy từ log deploy đỏ cuối cùng, run 33953796948 ngày 2026-09-05; số sau là build của PR này), 0 dấu vết bullmq/ioredis/aws-sdk/sql-loader ở cả 3 env. Gate mới: `pnpm verify:worker-bundle` (assert thành phần bundle — **đây** mới là hàng rào thật, đã test âm: thêm lại re-export docker → đỏ đúng) + `pnpm verify:worker-startup` (boot Worker bằng workerd). **Lưu ý đã đo:** startup gate **KHÔNG** bắt được ca này — với docker cố tình bundle lại, `wrangler dev` vẫn boot và trả `/health` 500, vì fallback quét stack `file:///` của bullmq thành công ở local mà thất bại ở Worker deploy; nên đừng đọc "startup xanh" thành "deploy được". Cả hai chạy trong CI job `worker-bundle`. Bonus: `serve.ts` trước đây inject runtime bằng middleware thứ hai đặt **sau** `withRuntime`, nên Docker mode dựng **hai** runtime mỗi process (hai kết nối Redis, hai pg pool) rồi bỏ một — nay dùng `setRuntimeFactory`, còn một Issue #459. |
