# Out-of-scope findings backlog

> **Nơi duy nhất (single source) log lại mọi lỗ hổng / bug / feature / task được
> phát hiện *trong khi làm một PR* nhưng **không thuộc scope** của PR đó.**
>
> Vì sao cần: phát hiện ngoài scope hay bị nêu trong mô tả PR hoặc chat rồi
> **biến mất sau khi PR merge**. File này giữ chúng ở một chỗ để không bỏ sót.
> Bắt buộc qua DoD §7 (`definition-of-done.md`).

## Cách dùng

1. Khi đang làm PR mà phát hiện điều gì đó thật (bug/lỗ hổng/nợ kỹ thuật/feature)
   **ngoài scope** PR hiện tại → thêm một dòng vào bảng dưới **trong cùng PR đó**.
2. Đặt `Trạng thái`: `open` · `fixed` (kèm PR/commit) · `wontfix` (kèm lý do) ·
   `tracked` (đã có spec/issue riêng — ghi link).
3. Khi một mục được xử lý ở PR sau → cập nhật trạng thái + tham chiếu (đừng xoá
   dòng, giữ dấu vết).
4. Nếu mục đủ lớn thành feature → tạo `.kiro/specs/<feature>/` và đổi trạng thái
   sang `tracked` trỏ tới spec đó.
5. Mức độ: `critical` · `high` · `medium` · `low`.

## Registry

| ID | Phát hiện (ngày · PR) | Loại | Khu vực | Mô tả | Mức độ | Trạng thái | Tham chiếu |
|----|----------------------|------|---------|-------|--------|-----------|-----------|
| B1 | 2026-07-10 · upload-file-controls | vuln | `routes/settings.ts` | `POST /api/v1/settings` + `DELETE /:key` không có admin gate → mọi member đã đăng nhập ghi được **bất kỳ** settings key (kể cả `upload_policy`, `media.signedTransform`), vòng qua admin gate của các endpoint config riêng | high | `fixed` | Gated `requireSiteAdmin` cho POST/DELETE (giữ GET mở cho member) + test `routes/__tests__/settings.test.ts` |
| B2 | 2026-07-10 · upload-file-controls | vuln | `routes/settings.ts` GET | `GET /api/v1/settings` và `/:key` trả **nguyên value** cho mọi member — lộ settings chứa secret, cụ thể `media.signedTransform.secret` (dùng ký/verify signed image transform) → member thường có thể đọc rồi giả mạo transform | high | `fixed` | `redactSecrets` che field secret (theo tên: secret/token/password/apikey/…) ở cả GET list + by-key, cho mọi caller (consumer server đọc trực tiếp từ DB, không qua HTTP). Test trong `routes/__tests__/settings.test.ts` |
| B3 | 2026-07-10 · upload-file-controls | bug (infra) | CI · Cloudflare Pages | Preview build `lumibase-docs` + `lumibase-landing` fail tức thì (0 giây) trên PR — là config CF Pages project (git-integrated auto-build), không phải nội dung repo (docs build local pass; landing không bị PR đụng). Xuất hiện từ một lần merge main | low | `open` | Sửa ở Cloudflare dashboard (build config/root dir) hoặc tắt preview cho 2 project này. Không chặn merge (non-required) |
| B4 | 2026-07-10 · upload-file-controls | task (hygiene) | `apps/docs/public/releases.json` | File được git-track nhưng `pnpm docs:build` (`build-release-manifest.mjs`) sinh lại → mỗi lần build docs làm bẩn working tree (lần gặp: flip `migrationWarning`) | low | `fixed` | KHÔNG untrack (release skill hand-edit file này làm source of truth). Thay vào đó làm `build-release-manifest.mjs` **idempotent**: giữ editorial fields (`migrationWarning`/`minimumSafeUpgradeVersion`) + `releaseDate` từ manifest đã commit khi không có override (env/CHANGELOG). Vá luôn bug tiềm ẩn: deploy build trước đây clobber hand-edit về default. Diff = 0 khi build lại |
| B5 | 2026-07-05 · upload-file-controls | feature | Upload security | Re-encode ảnh để sanitize (chống polyglot không-false-positive) — hiện chỉ có deep byte-scan heuristic. Phụ thuộc `ImageAdapter` (Sharp/CF Images) | medium | `tracked` | `.kiro/specs/upload-file-controls/tasks.md` (F1) ↔ `image-transform-dsl` ↔ `runtime-security-guards-plan.md` §3 |
| B6 | 2026-07-05 · upload-file-controls | vuln (residual) | `middleware/file-upload-policy.ts` | SVG có `<use xlink:href>` trỏ resource external không bị chặn ở scan (bỏ để tránh false-positive) | low | `wontfix` | Đã trung hoà bởi serve `attachment` + `nosniff` + CSP; ghi trong `upload-file-controls/design.md` |
| B7 | 2026-07-05 · upload-file-controls | task (residual) | `studio/.../interfaces/file.tsx` | `maxSize` per-field chỉ là UI hint, không enforce server per-field | low | `wontfix` | Global upload policy đã enforce server-side; rủi ro thấp |
| B8 | 2026-07-23 · owasp-api-top-10-audit | vuln (deps) | `apps/landing`, `apps/consumer`, transitive | Gate CI `Dependency vulnerability audit` đỏ trên PR #289 (23 advisory: 11 high/11 moderate/1 low) — KHÔNG do diff PR này (`git diff origin/main...HEAD` cho `package.json`/lockfile rỗng). Nổi bật: Next.js SSRF `GHSA-89xv-2m56-2m9x` (Server Actions) + `GHSA-p9j2-gv94-2wf4` (rewrites) + DoS `GHSA-m99w-x7hq-7vfj` + middleware bypass `GHSA-6gpp-xcg3-4w24` ở `next@16.2.10` (landing/consumer, KHÔNG thuộc CMS API); `sharp@0.34.5` (transitive qua next) `GHSA-f88m-g3jw-g9cj`; `fast-uri@3.1.2` host-confusion `GHSA-4c8g-83qw-93j6`/`GHSA-v2hh-gcrm-f6hx` (transitive qua `@modelcontextprotocol/sdk`). Không nằm trong dependency-path của CMS API (Hono) | high | `fixed` | Đóng trong PR #289: bump `next → 16.2.11` (consumer+landing) + pnpm `overrides` `fast-uri >=3.1.4` & `sharp >=0.35.0`. `pnpm audit --prod --audit-level high` exit 0 (còn 1 low + 1 moderate, không chặn gate); build landing+consumer pass. FE guidance ở `docs/{en,vi}/tutorials/nextjs-quickstart.md` §6 |
| B9 | 2026-07-25 · studio-deployments-wiring | bug | `apps/studio/src/router.tsx` | Cây route admin-path (`adminPathSettingsRoute`) thiếu 2 route mà cây `/settings` có: **`change-feed`** và **`encryption`**. Sidebar hiển thị cả hai ở mọi instance, nên trên instance dùng custom admin path (`/<adminPath>/settings/...`) hai link đó không resolve — cùng class "quên nối dây" với trang Deployments của PR này, chỉ khác là lộ ở nửa admin-path. Không nằm trong scope PR (chỉ nối dây Deployments), và tripwire `deployments-page.test.tsx` cố ý chỉ kiểm nav ↔ cây `/settings` để không đỏ CI vì hai route sẵn thiếu | medium | `open` | Fix = thêm `adminPathChangeFeedRoute` + `adminPathEncryptionSettingsRoute` (copy pattern route kề bên) rồi mở rộng tripwire kiểm luôn cây admin-path (mọi nav item phải có 2 route: plain + `$adminPath`) |
| B10 | 2026-07-25 · agent-run-worker-keyprovider | bug (gap) | `apps/cms/src/index.ts` · `apps/cms/wrangler.toml` | Cloudflare Workers **không có** queue consumer cho `agent-runs`: `index.ts` chỉ `export default app` (không có `queue(batch, env)` handler) và `wrangler.toml` để `[[queues.producers]]` ở dạng comment. Hệ quả: `POST /api/v1/agent/goals` với `execution: 'async'` trên CF hoặc bị chặn ở nhánh `ASYNC_UNAVAILABLE` (không có queue adapter), hoặc enqueue rồi **không ai tiêu thụ** → run đứng ở `queued`. Comment trong `agent-run-worker.ts` ("Cloudflare Workers wire the same handler through their queue consumer export") mô tả một thứ chưa tồn tại. Phát hiện khi thread `KeyProvider` vào worker deps — chỗ đáng lẽ phải sửa cho CF thì không có call site nào | medium | `open` | Cần: `queue()` export ở `index.ts` (dựng db/runtime ngoài request), bật `[[queues.producers]]` + `[[queues.consumers]]` trong `wrangler.toml`, truyền `keys: createCloudflareKeyProvider(env)`. Đủ lớn → nên tạo spec riêng. Docker/Node **không** ảnh hưởng (`serve.ts` đã register consumer đầy đủ) |
| B11 | 2026-07-25 · fix-issues-296 (sponsor rewards store) | feature (chưa hoàn chỉnh) | `apps/landing` rewards flow | Luồng sponsor reward **chưa chạy end-to-end** và hiện KHÔNG có mặt nào lộ ra người dùng: `apps/landing` build `output: 'export'` (static) nên 2 route handler (`api/rewards/claim`, `api/webhooks/github-sponsors`) nằm ở `src/_api-routes-disabled/`, còn trang `/rewards/claim` đã bị xoá ở `31b2be9a` vì luôn 404. Issue #296 chỉ vá tầng lưu trữ (stub `saveToDatabase()` no-op + hai `Map` rời + claim không atomic) — store giờ đúng và chờ sẵn đúng cái "managed backend" mà commit xoá kia nói tới. Bật thật cần: server runtime (vd `@cloudflare/next-on-pages`) + wire `configureSponsorStore(resolveSponsorStore(env))` + binding D1 `SPONSORS_DB` + dựng lại UI claim | low | `open` | Store + migration + hướng dẫn wiring đã sẵn: `apps/landing/src/lib/rewards/`, `apps/landing/migrations/0001_sponsors.sql`, mục "Sponsor rewards store" trong `apps/landing/README.md`. Registry setup-impact #84 |
| B12 | 2026-07-25 · fix-issues-296 (sponsor rewards store) | vuln (minor) | `apps/landing/src/_api-routes-disabled/api/webhooks/github-sponsors/route.ts` | `crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expectedSignature))` **throw** khi 2 buffer khác độ dài (`signature` là header do client kiểm soát) ⇒ signature sai độ dài trả `500 Internal server error` thay vì `401`, và `WEBHOOK_SECRET` mặc định `''` nghĩa là thiếu env → mọi request vẫn được HMAC bằng secret rỗng. Không exploit được hiện tại vì route đang disabled (B11) | low | `open` | Vá khi bật lại route: check độ dài trước khi so sánh (hoặc so digest cùng độ dài) + fail-fast khi `GITHUB_WEBHOOK_SECRET` vắng |
| B13 | 2026-08-01 · deps-batch-followups (review #326) | bug (flake) | `apps/studio/src/modules/mission-control/__tests__/goals-actions.test.tsx` | Test `decomposes a goal with the entered sub-goals (Req 18.1)` timeout ở `await screen.findByRole('option', { name: 'writer' })` (dòng 87) khi chạy **full suite** trên Node 24 — đo được 1/3 lần chạy; chạy riêng lẻ pass, 2 lần chạy full sau đó pass. File KHÔNG bị PR #326 đụng, nên là flake nhạy tải (default timeout 1000ms của `findBy*` so với select role fill async) chứ không phải regression React 19 — nhưng chưa loại trừ được việc React 19 làm chậm thêm. Hệ quả: CI đỏ lẻ tẻ, không tái lập được theo yêu cầu | medium | `fixed` | Vá ở `feat/user-2fa`: `configure({ asyncUtilTimeout: 5_000 })` trong `apps/studio/src/test/setup.ts`. Nguyên nhân gốc là timeout **của Testing Library** (default 1000ms), độc lập với `testTimeout: 15_000` ở `vitest.config.ts` — nên `findBy*` bỏ cuộc trong lúc component còn "Loading…". Sửa suite-wide thay vì per-test vì cả class đều nhạy tải: quan sát được 6 file khác nhau fail giữa các lần chạy hook (kể cả `api-keys-allowed-origins.test.tsx`), luôn pass khi chạy riêng. Không nới `testTimeout`, không đụng test nào |
| B14 | 2026-08-01 · deps-batch-followups (review #326) | task (deps) | `apps/cms` — `graphql` / `graphql-yoga` | `graphql-yoga@5.21.2` khai peer `graphql: ^15.2.0 \|\| ^16.0.0` nhưng workspace cài `graphql@17.0.2` (vào main từ #312) → **peer violation đang tồn tại trên main**. 28 test `src/graphql` vẫn pass nên chưa nổ, nhưng yoga 5 không chính thức hỗ trợ graphql 17; rủi ro nằm ở path chưa có test bao (error formatting, subscription) | medium | `fixed` | Chờ `graphql-yoga` ra bản có peer `^17`, hoặc pin `graphql` về `^16` cho tới lúc đó. Không thuộc scope #326 (bump graphql là PR khác) và không thuộc scope follow-up review này. **Cập nhật 2026-08-21 (batch #396'):** vẫn mở. `pnpm install` của batch in ra đúng violation này (`graphql-yoga 5.21.2 → unmet peer graphql@"^15.2.0 || ^16.0.0": found 17.0.2`). #399 bump yoga lên 5.21.3 nhưng peer range **không đổi**, nên nó không đóng được mục này — đã tuyên bố out-of-scope trong `.kiro/specs/dependency-upgrade-batch-2026-08/requirements.md` Req 9.1. **Đóng 2026-08-31 (#442):** upstream đã ra bản cần chờ — `graphql-yoga@5.22.0` khai peer `graphql: ^15.2.0 \|\| ^16.0.0 \|\| ^17.0.0`. Verify bằng lockfile theo DoD §2e (không bằng manifest): importer `apps/cms` resolve `graphql 17.0.2` và block `graphql-yoga@5.22.0` nay liệt `^17.0.0` trong `peerDependencies`, nên `pnpm install` không còn in unmet peer. Không phải pin `graphql` về `^16` |
| B15 | 2026-08-01 · deps-batch-followups (review #326) | task (infra) | `package.json` — `pnpm.overrides` / `pnpm.patchedDependencies` / `pnpm.auditConfig` | pnpm 10 **không còn đọc** field `pnpm` trong `package.json` (`[WARN] The "pnpm" field in package.json is no longer read by pnpm … keys were ignored`, thấy khi corepack shim pnpm 10.x chạy trước khi delegate về bản pin). Hiện **chưa vỡ** vì `packageManager: pnpm@9.12.0` ghim lại, install thật vẫn là 9.12.0 nên overrides + patch vẫn áp. Nhưng #326 vừa làm repo **phụ thuộc thêm** vào chính cơ chế đó: override `@types/react`/`@types/react-dom` = `19.2.0` là thứ giữ `@lumibase/ui` typecheck cùng React major với runtime (ghi rõ trong `docs/{en,vi}/security/dependency-overrides.md`). Khi nào bump `packageManager` lên pnpm 10 → override biến mất im lặng (`@lumibase/ui` tụt về `@types/react` 18) và patch `gray-matter` không apply | medium | `open` (tiền đề đã xong) | Di chuyển 3 key sang `pnpm-workspace.yaml` (nơi pnpm 10 đọc) **trước** khi bump `packageManager`. **Cập nhật 2026-08-21:** tiền đề đã xong — #397 (đã merge) khai cả 3 key trong `pnpm-workspace.yaml` và thêm gate `pnpm settings:check` chặn hai bản khai trôi lệch. Bump `packageManager` lên pnpm 10 giờ **an toàn**: override + patch + audit-ignore sẽ vẫn áp sau khi pnpm 9 ngừng đọc key `pnpm` trong `package.json`. Còn lại đúng một việc: bump `packageManager` rồi xoá key `pnpm` khỏi `package.json` cùng `scripts/check-pnpm-settings-parity.mjs`. Warning của pnpm 10 vẫn quan sát được khi shim chạy trước lúc delegate về 9.12.0 |
| B17 | 2026-08-20 · landing intent composer | task (test gap) | `apps/landing/vitest.config.mts` | Landing chạy vitest với `environment: 'node'` và `include: ['src/**/*.{test,spec}.ts']` — chỉ `.ts`, không `.tsx`, và không có `@testing-library/react` trong workspace. Hệ quả: **không thể** viết test hành vi cho component landing; PR này phải tách toàn bộ logic ra `src/lib/intent-compile.ts` để test được (51 test), còn phần tương tác (beat machine, reduced-motion, clipping trong `FeatureCard`) chỉ verify được bằng Chrome headless chạy tay qua CDP — không có gate CI nào bắt nếu ai đó làm hỏng. Khác với `apps/studio` (đã có test `.tsx`) | medium | `open` | Fix = thêm `jsdom` + `@testing-library/react` cho landing, mở `include` sang `.tsx`, rồi thêm test cho `IntentComposerViz` (click → beat → payload) và một assertion chống clipping. Không làm trong PR này vì kéo theo dependency mới cho một app marketing. **Cập nhật 2026-08-24 (landing GA consent, nâng low → medium):** giờ có surface thứ hai không test được và lần này là **compliance**, không chỉ visual — `Analytics.tsx` + `CookiePreferences.tsx` là chỗ quyết định GA4 có load hay không. Logic thuần đã tách ra `src/lib/analytics/*.ts` và được 33 test bao (gating, validate measurement ID, snippet consent), nhưng **không có gate CI nào** chứng minh: (a) `<Script>` GA không render khi consent `null`/`denied`, (b) banner không xuất hiện khi đã có quyết định, (c) nút "Change my choice" thật sự re-open banner. Ai đó đảo một điều kiện trong JSX thì test hiện tại vẫn xanh. Hiện chỉ verify được bằng build + `grep -rl googletagmanager out/` (chứng minh bản không cấu hình GA là sạch). **Cập nhật 2026-08-24 (mở rộng sang docs):** logic đã chuyển vào `packages/analytics-consent` (26 test) và **`apps/docs` nay CÓ** component test jsdom cho đúng ba assertion đó (`src/components/__tests__/analytics-consent.test.tsx`, 9 test) vì app đó đã có `jsdom` + `@testing-library/react`. Nghĩa là mẫu test cần viết giờ đã tồn tại sẵn trong repo — port sang landing chỉ còn là việc thêm dependency + mở `include` sang `.tsx`, không phải thiết kế lại |
| B16 | 2026-08-01 · cache-penetration (Req 19) | task (gap) | `lumibase_pages` write path | Spec task 22.6 yêu cầu `forget(neg:…:page:…)` sau tạo page / đổi slug, nhưng CMS trước đây **không có** route/service tạo/sửa `pages` (chỉ insert trực tiếp trong e2e/golden-path). | medium | `fixed` | `PageService` + `routes/pages.ts` (`/api/v1/pages`) — `forgetNegative` sau create và sau rename (slug cũ + mới); test `page-service.test.ts` |
| B20 | 2026-08-21 · dependency-upgrade-batch-2026-08 | bug (silent) | `pnpm.overrides` ↔ manifest | `overrides.vite` ở `^7.3.5` trong khi `apps/studio` + `apps/docs` khai `^8.1.3`. Override của pnpm áp **cả direct dependency**, nên lockfile importer ghi `specifier: ^7.3.5 → 7.3.6` — hai app build bằng **Vite 7** suốt thời gian manifest tuyên bố Vite 8. `settings:check` không bắt được vì nó chỉ so hai bản khai override với nhau, và chúng khớp nhau; chúng chỉ cùng sai so với manifest | medium | `fixed` | Nâng `overrides.vite` → `^8.2.0` cùng manifest (importer nay resolve 8.2.2). Cơ giới hoá cả class: `scripts/check-override-drift.mjs` + gate CI `pnpm drift:check` + test `node --test` gồm chính ca vite. DoD §2e mới. Docs: mục "Override drift" trong `docs/{en,vi}/security/dependency-overrides.md` |
| B21 | 2026-08-21 · dependency-upgrade-batch-2026-08 | task (deps) | `apps/{landing,consumer}` — `eslint-config-next` | Plugin của `eslint-config-next@16.3.1` (`eslint-plugin-import@2.32.0`, `eslint-plugin-react@7.37.5`, `eslint-plugin-jsx-a11y@6.10.2`) đều khai peer chặn ở `eslint ^9`, nhưng workspace cài `eslint@10.6.0` → **4 peer violation** in ra ở mỗi `pnpm install`. Lint vẫn pass thật (đã chạy riêng cả hai app, exit 0) nên đây là gap **khai báo**, không phải vỡ runtime. Đã tồn tại trên `main` cho `apps/consumer` (khai `eslint: ^10` từ #326); batch này mở rộng sang `apps/landing` khi thống nhất eslint về một major | low | `open` | Chờ plugin ra bản khai peer `^10` (upstream `eslint-config-next`). Không hạ eslint về 9 chỉ để làm im peer: hai major eslint trong một workspace là drift đắt hơn. Xem lại khi bump `eslint-config-next` lần tới |
| B22 | 2026-08-21 · dependency-upgrade-batch-2026-08 | task (forward-compat) | `apps/studio/vitest.config.ts` | Vite 8 cảnh báo config dùng `__dirname` (dòng 23) không được `configLoader: 'native'` hỗ trợ — và native sẽ thành **default ở một major Vite tương lai**. Hiện chỉ là warning, build/test vẫn pass. **Cập nhật 2026-08-30 (rà batch dependabot):** rộng hơn mô tả ban đầu — `apps/docs/vitest.config.ts` cũng đỏ cảnh báo, và không chỉ vì `__dirname` (dòng 15) mà còn `JSON import "./package.json" without import attributes` (dòng 4). Nghĩa là "rà các config khác" ở cột fix không phải việc phòng xa mà là việc đang tồn tại | low | `open` | Đổi `__dirname` → `import.meta.dirname` và thêm `with { type: 'json' }` cho import JSON. Rà tất cả vite/vitest config trong workspace trước khi native thành default, đừng chờ nó vỡ ở lần bump major sau |
| B23 | 2026-08-21 · dependency-upgrade-batch-2026-08 | task (process) | Dọn PR sau batch | Khi đóng #369–#381 là "absorbed vào #396", **ba PR bị đóng sai lý do**: #396 chỉ gộp npm deps nên không đụng version action (#369 `upload-artifact` 4→7, #371 `scorecard-action` 2.4.3→2.4.4), và nó dừng `nanoid` ở 5.1.16 để đóng advisory chứ không lên 6.x (#377). Không reopen được vì branch dependabot đã bị xoá lúc đóng. Bài học: "gộp vào PR X" phải verify trên `main` sau merge cho **từng** PR, không suy ra từ nhãn nhóm | low | `fixed` (một phần) | Đã đăng comment sửa lại trên cả 3 PR. #369 + #371 làm trực tiếp trong `chore/action-bumps-leftover`. #377 (`nanoid` 5→6) **vẫn mở** → xem B24 |
| B24 | 2026-08-21 · dependency-upgrade-batch-2026-08 | task (deps) | root `overrides` — `nanoid` | `nanoid` 5→6 (dependabot #377, đã đóng nhầm và không reopen được) chưa được đánh giá. Hiện `apps/cms` + `packages/database` khai `^5.0.7`, override scoped ghim `nanoid@5: ^5.1.16`, còn `next → postcss` giữ nhánh 3.x qua `nanoid@3: ^3.3.17` | low | `fixed` | Đóng ở #419 (`^5.1.16` → `^6.0.1` cho `apps/cms` + `packages/database`). Hoá ra **không** cần thêm scope `nanoid@6`: 6.0.1 là bản mới nhất nên không có sàn advisory nào phải ép, và `drift:check` không đỏ vì override scoped `nanoid@5` chỉ nói cho range 5.x. Ngược lại, scope đó thành **override chết** — lockfile không còn nanoid 5 nào — nên nó được xoá khỏi cả `package.json` và `pnpm-workspace.yaml` cùng dòng registry trong `docs/{en,vi}/security/dependency-overrides.md` (đúng "Remove when" mà dòng đó đã ghi sẵn). Đã verify: ESM-only bundle được vào worker CF (không có `require("nanoid")` trong `dist/cloudflare.js`), và chỉ dùng `nanoid()` / `nanoid(n)` |
| B25 | 2026-08-24 · release 0.26.0 (rà nhánh local) | task (deps) | `chore/deps-consolidated-upgrade` (nhánh local, 4 commit, 2026-08-11) | Nhánh local gom các Dependabot major đang mở (`workers-types` v5, `lucide-react` v1, `fast-check` v4, `uuid` ^14, `vite` ^8) kèm phần source adaptation. **Đã bị #396 vượt qua hoàn toàn** (2026-08-21, "consolidate all open dependabot PRs"): main hiện ở `vite ^8.2.0`, `uuid ^14.0.1`, `lucide-react ^1.28.0`, `@cloudflare/workers-types ^5.20260801.1`, `fast-check ^4.9.0` — đúng các đích của nhánh — và #396 sửa luôn cùng bộ file mà nhánh đã sửa (`runtime/src/adapters/cloudflare/storage.ts`, `git-integration/ci-log-store.ts`, `routes/extensions.ts`, `landing/src/components/Header.tsx`, các property suite fast-check). Phần bảo mật cũng đã đóng ở #385/#397. Ghi lại vì nó là một bài học quy trình: nhánh này ngồi local 13 ngày rồi bị làm lại từ đầu bởi một PR khác — công đôi lần vì không ai biết nó tồn tại | low | `wontfix` | Superseded bởi #396. Nhánh local an toàn để xoá (không còn commit nào mang giá trị riêng). Bài học: đẩy nhánh lên remote + mở draft PR ngay khi có commit đầu tiên, kể cả khi chưa xong — nếu không thì Dependabot (hoặc người khác) sẽ làm lại đúng việc đó |
| B26 | 2026-08-30 · user-totp-2fa (rà UI trước khi chụp ảnh PR) | bug (class) | `apps/studio/src/modules/settings/encryption-page.tsx` · `security-audit-tab.tsx` · hook gọi `/api/v1/translations` | Cả một họ trang Studio fetch bằng `credentials: 'same-origin'` **không** kèm `Authorization: Bearer` — nhưng `withAuth` (`apps/cms/src/middleware/auth.ts`) chỉ đọc header `authorization`, KHÔNG có nhánh cookie nào. Verify trên server đang chạy: `/api/v1/admin/encryption/envelope`, `/api/v1/admin/security/audit-log`, `/api/v1/translations` đều trả **401** khi không có bearer; browser log của Studio cũng cho `401 GET /api/v1/translations` ở mọi lần load. Comment trong `security-audit-tab.tsx` ("uses `credentials: 'same-origin'` so the session cookie rides along") mô tả một cơ chế không tồn tại. Test component pass vì đều mock `fetch`. Cùng class với bug vừa vá ở `security-page.tsx` của PR này (đã sửa: `getActiveToken()` + `getApiBaseUrl()`), nhưng 3 chỗ kia nằm ngoài scope PR 2FA và **đã có trên `main`** | high | `open` | Sửa: dùng `getApiClient()` hoặc thêm `Authorization: Bearer ${getActiveToken()}` + `getApiBaseUrl()` (contract C2, DoD §2d) cho từng trang; bỏ comment nói về cookie. Nên cơ giới hóa: tripwire source-scan chặn `fetch('/api/v1/...` không kèm bearer trong `apps/studio/src` (giống `security-guards.wiring.test.ts`) — nếu không, class này sẽ tái diễn ở trang settings tiếp theo |
| B27 | 2026-08-30 · user-totp-2fa (rà threat model chỗ lưu seed) | bug (DX/ops) | `apps/cms/src/routes/tfa.ts` · `packages/runtime/src/adapters/shared-keys.ts` | Khi KEK không có sẵn, mọi đường 2FA trả **500 INTERNAL** đục. Hai ca đo được trên server thật: (a) không set `ENCRYPTION_KEY` → `POST /me/tfa/setup` 500 (`getActiveKey` throw, shared-keys.ts:67); (b) seed enroll dưới `v0` rồi rotate sang chỉ còn `ENCRYPTION_KEY_v1` → `verify-totp`, `DELETE /me/tfa`, `POST /me/tfa/recovery-codes` **đều 500** (`getKey` throw, shared-keys.ts:77). Ca (b) thành **dead end**: recovery code vẫn login được (PBKDF2, không bọc KEK — verify 200) nhưng tháo/regenerate/enroll lại đều đòi một TOTP code sống ⇒ hết 8 code là mất quyền vào Studio realm. Handler chỉ catch `TotpError`, throw của KeyProvider rơi xuống error boundary chung. **KHÔNG phải lỗ hổng**: không có nhánh nào ghi seed thô, fail-closed đúng như CWE-321 đã chốt | medium | `fixed` | Issue #429 — vá trên `docs/totp-key-findings-backlog`: `withKeyErrors` dịch throw của KeyProvider thành `TotpError`, routes map `ENCRYPTION_NOT_CONFIGURED` → 503 và `TFA_KEY_UNAVAILABLE` → 409 (message nêu key id), `DELETE /me/tfa` nhận recovery code thay TOTP code + UI Studio có lối đó, `recovery-codes` cố ý vẫn đòi TOTP code. Test `modules/mfa/__tests__/totp-key-errors.test.ts`; đã verify lại trên server thật (409/409/200/200 thay cho 500/500/500). Đề xuất còn lại (ép `ENCRYPTION_KEY` ở setup wizard) vẫn mở. Đề xuất: lỗi có mã (`ENCRYPTION_NOT_CONFIGURED` 503 / `TFA_KEY_UNAVAILABLE` 409) + cho `DELETE /me/tfa` chấp nhận password + recovery code khi seed không giải được + ép `ENCRYPTION_KEY` ở setup wizard (đúng việc `v1-release-criteria` §1b treo cho CWE-321) |
| B28 | 2026-08-30 · user-totp-2fa (rà threat model chỗ lưu seed) | task (docs) | `envelope-migration-worker.ts` ↔ `user_totp_credentials` · `docs/en/security/user-management.md` §4f | Vòng đời KEK của seed TOTP chưa được ghi ở đâu, và nó **khác** với item field: worker re-wrap envelope chỉ đi qua `items`/`collections` (import ở dòng 2), KHÔNG chạm `lumibase_user_totp_credentials` — nên sau `POST /admin/encryption/envelope/migrate` seed vẫn giữ `secret_key_id` cũ và **khoá cũ phải giữ mãi**. Thêm nữa KEK là một khoá dùng chung toàn deployment, không có KDF theo user (AAD `totp-secret\|<userId>` chỉ chống bê ciphertext sang user khác, không phải biên bảo mật) ⇒ **lộ `ENCRYPTION_KEY` thì rotate không cứu được**, phải ép toàn bộ user enroll lại. Bản chất TOTP (server phải giữ shared secret khôi phục được), không phải lỗi thiết kế — nhưng operator sẽ hiểu sai chữ "rotate" nếu không viết ra | medium | `tracked` | Issue #430. Cần: mục "TOTP key lifecycle" ở `user-management.md` §4f, đánh dấu `ENCRYPTION_KEY` là required-before-first-enroll + never-removable trong `environment-variables.md`, runbook KEK-compromise ở `docs/en/operations/`, kèm bản VI (CLAUDE.md rule #7) |
| B29 | 2026-08-24 · landing/docs GA consent | task (gap · blocked) | `apps/marketplace` (submodule private `lumibase-ai/marketplace`) | Yêu cầu ban đầu là bật GA4-sau-consent cho **cả ba** site public: landing, docs, marketplace. Hai site đầu đã xong trong PR này; marketplace **không làm được từ repo này** — submodule chưa checkout (`git submodule status` cho `-5958c50e…`, dir chỉ còn `.next`/`out`/`node_modules` cũ, không có `.git` lẫn `src`) và remote không đọc được bằng cả hai account đang login (`git ls-remote` qua SSH và HTTPS đều trả `Repository not found`; `gh repo view lumibase-ai/marketplace` → `Could not resolve to a Repository`). Hệ quả: `marketplace.lumibase.dev` (hoặc domain tương ứng) vẫn **không có** banner consent lẫn control opt-out, trong khi privacy policy ở landing giờ đã mô tả cơ chế consent như đặc tính chung của các site public → policy nói rộng hơn thực tế đúng một site | medium | `open` | Sửa trong repo `lumibase-ai/marketplace`: nó là Next app (build ra `apps/marketplace/out`) nên copy nguyên mẫu của `apps/landing` — `<Analytics>` + `<CookiePreferences>` dùng `@lumibase/analytics-consent`. **Lưu ý ràng buộc:** marketplace bị loại khỏi pnpm workspace (`pnpm-workspace.yaml: "!apps/marketplace"`) và install bằng `--ignore-workspace`, nên KHÔNG dùng được `workspace:*` — hoặc publish package này lên registry, hoặc copy logic kèm chú thích trỏ về `packages/analytics-consent` làm nguồn. Cần thêm `NEXT_PUBLIC_GA_ID` vào step build marketplace ở `release.yml` + `pages-deploy.yml` (hiện chỉ truyền cho step build của landing/docs) |
| B30 | 2026-08-30 · rescue blog SSRF khỏi worktree cũ | bug (flake) | `apps/docs/src/components/__tests__/analytics-consent.test.tsx` (`beforeEach` dòng 34) | Cả 9 test của file đỏ với `TypeError: Cannot read properties of undefined (reading 'clear')` tại `localStorage.clear()` — kèm warning `localStorage is not available because --localstorage-file was not provided`, tức global `localStorage` bị resolve về builtin experimental của **Node 24** (undefined) thay vì `localStorage` của jsdom. Đo được **1/2 lần** chạy `pnpm test` (turbo, 12 package song song) trên `main` sạch tại `b124d953`; ngay sau đó: file chạy riêng **pass** (9/9), full suite `apps/docs` chạy riêng **pass** (24 file · 157 test), và `pnpm test` toàn repo chạy lại **pass** (12/12 task). Không tái lập theo yêu cầu ⇒ flake nhạy tải, không phải regression. Cùng class với B13 nhưng nguyên nhân khác: đây là **global bị thiếu**, không phải timeout của `findBy*`. File do PR #426 (GA consent) thêm, nên chưa từng chạy qua nhiều chu kỳ CI | medium | `open` | Hệ quả: pre-commit hook (`husky` → `check:all` + `pnpm test`) và CI job `test` đỏ ngẫu nhiên, chặn commit ở máy local dù diff không liên quan (gặp khi commit 3 file `blog/`). Fix: không dựa vào global `localStorage` của môi trường — stub tường minh trong `beforeEach` (hoặc `vi.stubGlobal('localStorage', …)`), hoặc set `environmentOptions`/`pool` cho `apps/docs` sao cho jsdom luôn thắng builtin của Node 24. Nếu đã chuẩn hoá Node 24 toàn team thì rà cả các test khác đọc `localStorage`/`sessionStorage` trực tiếp — cùng một class |
| B33 | 2026-08-30 · rà batch dependabot (#411–#421) | bug (guard chết) | `scripts/version-check.mjs` | Script **không được gọi từ đâu cả** — không có trong `scripts` của `package.json` (`pnpm version:check` chạy `sync-version.mjs --check`, một file khác), không trong workflow nào, không trong `.husky/pre-commit` — và nếu chạy thì nó **fail**: nó assert `engines.node === '>=20'` trong khi root khai `>=22.13.0` (nay là `^22.22.2 \|\| ^24.15.0 \|\| >=26.0.0`). Nó cũng assert `packageManager` và `lockfileVersion`. Hệ quả: hàng rào cơ giới duy nhất canh `engines.node` không hề chạy — phát hiện đúng lúc jsdom 30 nâng sàn Node và không gate nào nói gì | medium | `open` | Chọn một trong hai, đừng để lửng: (a) nối vào `check:all` + đổi expectation `engines.node` thành hằng đọc từ chính `package.json` hay một sàn khai tường minh, hoặc (b) xoá file và thừa nhận `sync-version.mjs` là guard duy nhất. Nếu chọn (a) thì đây là chỗ đúng để cơ giới hoá DoD §2e: so `engines.node` của repo với `engines.node` của mọi toolchain đã cài, thay vì rà tay |
| B31 | 2026-08-30 · rà batch dependabot (#411–#421) | bug (CI) | `.github/workflows/perf-k6.yml` | Workflow đỏ ở **mọi** commit push lên `main` — kiểm 30 run gần nhất (2026-08-29 → 08-30), không có một `success` nào, kể cả trên `b124d953` (trước batch này). Không phải do batch. Vì nó không phải required check nên không chặn merge, nhưng một workflow đỏ vĩnh viễn làm mờ tín hiệu: người ta học cách bỏ qua màu đỏ, đúng lúc một cái đỏ thật xuất hiện. Liên quan `v1-release-criteria.md` §7 (k6 baseline "nên có, không chặn tag") và các task k6 đang hoãn của `high-load-cache-readiness` | low | `fixed` | **Nguyên nhân không phải thiếu môi trường tải mà là file workflow không parse được.** `on:` của nó vốn KHÔNG khai `push` (chỉ `workflow_dispatch` / `schedule` / `pull_request: [labeled]`), nhưng job `perf-gate` dùng `env.PERF_K6_FULL_RUN` trong `if:` **cấp job**, nơi context `env` không tồn tại (chỉ có `github`, `inputs`, `needs`, `vars`). Biểu thức sai không "đánh giá thành rỗng" — nó làm **cả file invalid**: GitHub không parse được nên không resolve nổi `name:` (API trả `name` = đúng đường dẫn file, dấu hiệu nhận biết), không tạo job nào, và ghi một run failed cho **bất kỳ** event nào, kể cả event mà `on:` không khai. Fix = `vars.PERF_K6_FULL_RUN`; sau đó workflow tôn trọng `on:` và không còn chạy trên push. Verify bằng `actionlint` (0 finding trên toàn bộ `.github/workflows`, gồm cả shellcheck). Chống tái diễn cả class bằng job CI mới `workflow-lint` chạy `actionlint` (pin version + sha256) — vì lý do thật sự khiến nó sống được nhiều tuần là **không có gì lint chính các workflow** |
| B32 | 2026-08-30 · rà batch dependabot (#411–#421) | bug (flake · class) | `.husky/pre-commit` ↔ `pnpm test` (turbo concurrency mặc định) | Nối tiếp B13: nâng `testTimeout` lên 15s **không** đóng được class, chỉ nâng trần. Đo tại commit này: `pnpm test` ở concurrency mặc định của turbo cho 6–8 fail rải khắp `apps/studio` (`field-inspector`, `fields-tab`, `materialize-page`, `marketplace-publish`, `security-audit-tab`, `setup-state-gate`, `mission-control`, `backup-code-page`) và `apps/cms` (`flow-service`, `backup-codes-persister`, `recovery/service`, `audit/routes`) — **tất cả** đều là `Test timed out in 15000ms`, tức là chạm đúng cái trần B13 vừa nâng. Cùng bộ đó với `TURBO_CONCURRENCY=1` thì **12/12 task xanh, 0 fail**; chạy từng package riêng cũng xanh. Nguyên nhân là tranh tài nguyên, không phải test sai. Nguy ở chỗ `.husky/pre-commit` gọi đúng `pnpm test`: commit hợp lệ bị chặn ngẫu nhiên, và cách chữa tự nhiên nhất mà người ta sẽ chọn là `--no-verify` — tắt luôn hàng rào. Đã gặp thật trong lần commit này | medium | `open` | Đừng nâng `testTimeout` lần nữa (đó là cách B13 thất bại). Giới hạn song song thay vì giới hạn thời gian: đặt `concurrency` cho task `test` trong `turbo.json` hoặc `TURBO_CONCURRENCY` trong `.husky/pre-commit`, và/hoặc hạ `poolOptions.maxThreads` cho hai suite jsdom nặng nhất. Cân nhắc cho pre-commit chạy tập bị ảnh hưởng thay vì toàn bộ suite, để hàng rào không đắt tới mức bị vòng qua |
| B34 | 2026-08-30 · rebase #441 lên main | bug (class) | `.kiro/steering/out-of-scope-backlog.md` ↔ `scripts/check-registry-numbering.mjs` | ID của **chính bảng này** không được kiểm trùng. #434 và #436 cùng cấp `B30` cho hai finding không liên quan (flake `localStorage` của `analytics-consent.test.tsx` vs guard chết `version-check.mjs`); PR merge sau phải renumber tay thành `B33`, và va chạm chỉ lộ ra dưới dạng conflict lúc rebase — không gate nào nói gì. Đúng cùng class mà `registry:check` đã cơ giới hoá cho cột `#` của Setup Impact Registry (từng có hai dòng #20/#31/#32), chỉ khác là bảng này bị bỏ sót. Nguy hơn vẻ ngoài vì ID backlog được **trích dẫn theo số** ở dòng khác ("Nối tiếp B13", "cùng class với B10", "Xem B24") và trong CHANGELOG, nên một lần renumber im lặng làm gãy tham chiếu mà không test nào phủ | medium | `fixed` | Mở rộng `scripts/check-registry-numbering.mjs` để kiểm **cả hai** bảng (`#` của setup-impact và `B<n>` của file này), báo lỗi kèm số kế tiếp an toàn và nhắc giữ lại occurrence đang được dòng khác trích dẫn. Đã verify hai chiều: chèn `B30` trùng → exit 1 đúng dòng; bỏ ra → exit 0. Chạy sẵn trong `pnpm check:all` (và do đó cả pre-commit + CI) nên không cần thêm entry mới |
| B35 | 2026-08-31 · dọn PR mở (#442/#439/#438/#337/#443) | task (test gap) | `.github/workflows/release-apps.yml` | Workflow này chỉ trigger `push: tags: v*.*.*`, nên **không PR nào chạy nó** — CI trên PR chỉ kiểm được cú pháp YAML qua gate `Workflow files are valid` (#441), không kiểm hành vi. #438 vừa bump `actions/setup-java` 5 → 6 ở đây, và v6 có commit *"Set default signature verification for supported distributions"* ([#1246](https://github.com/actions/setup-java/pull/1246)) tức bật verify chữ ký JDK theo **mặc định**. Config đã ghim `distribution: temurin` + `java-version: "17"` nên khả năng vỡ thấp, nhưng nếu vỡ thì lộ ra đúng lúc đang cắt release, trên đường build desktop/mobile của `apps/shell` — thời điểm đắt nhất. Cùng class với B31 (`perf-k6.yml` đỏ hàng tuần mà không ai thấy): workflow không được exercise thì không ai biết nó còn chạy hay không. `shell-check.yml` **không** dùng Java nên không che được chỗ này | medium | `open` | Hai hướng, không loại trừ nhau: (a) thêm `workflow_dispatch` vào `release-apps.yml` rồi chạy tay một lần sau mỗi lần bump action, ít nhất tới bước `Setup Java` + `Setup Android SDK`; (b) tách phần setup toolchain sang một job mà `shell-check.yml` cũng gọi được, để PR có che. Cho tới lúc đó: theo dõi run `Release Apps` ở release kế tiếp và đừng coi tag xanh là mặc định |
| B36 | 2026-08-31 · dọn PR mở (#428 → #443) | task (process) | Triage PR "superseded" | Đóng PR vì trùng lặp mà **không đo delta còn lại** thì mất việc thật. #428 bị đóng là "superseded by #360" — đúng cho 14/15 file, nhưng #360 là bản **squash** của cùng branch `oak-maxwell` lấy ở thời điểm sớm hơn, nên hai commit sau đó (`0a663a56` "make Phase 0 measurements reproducible", `64702033`) không nằm trong #360 và bị bỏ lại cùng lúc branch bị xoá. Phần bị bỏ lại là một fix thật: `tags: { scenario: 'detail' }` ghi đè tag mà k6 tự sinh từ tên scenario, làm sub-metric `http_req_failed{scenario:detail_throughput}` — đúng cái threshold của nó trỏ vào — rỗng, tức threshold không thể fail. Cùng class "guard trông có mà không bao giờ fire" với B31. Cứu được vì đo `git diff origin/main HEAD --stat` sau khi merge `main` vào branch, ra đúng `apps/cms/k6/load-items.js \| 1 -`; nếu tin nhãn "duplicate" thì mất | low | `fixed` | Fix đã land ở #443. Quy tắc rút ra: trước khi đóng PR là superseded, merge `main` vào branch rồi chạy `git diff origin/main HEAD --stat` — diff rỗng mới là superseded thật; khác rỗng thì tách phần dư thành PR riêng thay vì reopen (title PR cũ mô tả việc đã land). Rủi ro này sinh ra từ chính thói quen squash-merge một branch còn đang được commit tiếp — nên cũng nên xoá branch **ngay khi** squash-merge, đừng để nó nhận thêm commit |
| B37 | 2026-09-02 · rebase #330 (contracts rename) | task (docs debt) | 4 cặp `docs/{en,vi}` lệch cấu trúc mà `detect` báo up-to-date | `check-parity.mjs` thấy bốn cặp **không phải cùng một tài liệu**, nhưng `docs:i18n:detect` vẫn xếp chúng `up-to-date` vì stamp còn khớp hash — nên lệch nằm im. Chỉ lộ ra khi #330 đổi `@lumibase/shared`→`@lumibase/contracts` làm hash đổi, buộc phải re-stamp, và `stamp-pair.mjs` (chạy parity chặt hơn) từ chối. Đã verify **giống hệt** trên `origin/main` sạch, nên có sẵn, không do rename: (a) `contributing/testing.md` — EN 17 heading / VI 14, EN 12 code fence / VI 11, VI thiếu link `https://k6.io/`; (b) `architecture/realtime-websocket-implementation.md` — EN 17 heading / VI 16, VI thiếu 40 inline-code (`SiteRoom`, `resolveRoomName()`, `realtime-audience-channels`…), tức VI thiếu hẳn mục "studio plane" cuối bài (VI 254 dòng / EN 267); (c) `features/collection-preview.md` — code block #2 (jsonc) khác nội dung; (d) `features/cdc-change-feed.md` — code block #2 (jsonc) + #4 (ts) khác, cộng `docs/en/cdc/` vs `docs/vi/cdc/`. Riêng cặp (d) phần path là **false positive** (khác locale thì path phải khác); ba cặp còn lại là thiếu dịch thật | medium | `open` | #330 stamp bốn cặp bằng `--allow-structure-drift` để không kéo việc dịch vào một PR rename — cố ý, không phải bỏ qua. Việc còn lại: dịch bù cho (a)(b)(c), và cho `check-parity.mjs` bỏ qua chênh lệch chỉ do tiền tố `docs/<locale>/` để (d) không còn kêu. Đáng chú ý hơn cả: `detect` và `stamp-pair` **không cùng một mức chặt** — cặp lệch cấu trúc vẫn `up-to-date` chừng nào chưa ai chạm vào file. Cân nhắc cho `docs-i18n-sync.yml` chạy `check-parity` trên toàn cây (hiện gọi kèm `|| true`, xem DoD §4a) để lệch kiểu này không phải chờ một PR vô tình mới phát hiện |
| B38 | 2026-09-05 · landing footer and typography | bug (DX) | root `package.json` · `apps/landing/package.json` | Root `landing:preview` invokes a `preview` script that the landing package does not define. The app uses Next static export, so `next start` is not a substitute. Browser QA used a static server over `apps/landing/out/`. | low | `open` | Add a static-export preview command and make the root alias call it. |
| B39 | 2026-09-05 · landing footer browser QA | bug (layout) | existing landing decorative scenes / page overflow | At a 390px browser viewport with a 375px content area, the homepage has a 382px scroll width (7px horizontal overflow); the footer itself stays within 375px. Existing decorative scene elements extend beyond the content edge. Desktop also shows a small horizontal scrollbar. | low | `fixed` | 2026-09-05: traced to StudioStage `w-screen` (100vw includes the vertical scrollbar). Added body `overflow-x: clip` to contain full-bleed scenes without creating a scroll container; sticky vertical scenes and code-panel scrolling remain available. |
| B40 | 2026-08-31 · rà workflow trên main sau batch dependabot (rebase 2026-09-06) | vuln (availability) | `packages/runtime/src/index.ts` · `apps/cms/src/middleware/runtime.ts` · `apps/cms/wrangler.toml` | **Mọi deploy Cloudflare bị Cloudflare từ chối** kể từ khi bullmq 6 vào main (#421, 2026-08-30): `Uncaught Error: Could not determine sql-loader directory path`, validation error 10021. Nguyên nhân: `packages/runtime/src/index.ts` re-export **cả** `./adapters/docker` (và `./factory` = `createRuntime`, và `./leader-lock` import `ioredis` như **value**), còn `middleware/runtime.ts` import `createRuntime` từ barrel → bundle Worker kéo theo cả cây docker gồm `bullmq`. BullMQ 6 thêm backend Postgres, và `dist/esm/postgres/sql-loader.js` gọi `getDirname()` ở **top level module**, throw khi không có `__dirname` lẫn frame `file:///` — đúng môi trường Worker đã bundle. Worker throw lúc khởi tạo ⇒ script bị từ chối. **Vì sao mọi gate đều xanh:** `pnpm build` chạy `wrangler deploy --dry-run`, nó *bundle mà không instantiate*; job `build` của CI vì thế không thể thấy class lỗi "throw ở top level". Đây cũng là chỗ tôi kết luận sai khi rà #421 — coi "build pass + round-trip Redis thật" là đủ, trong khi chưa có gì boot Worker. Chỉ `deploy-cms.yml` (dev/staging) đỏ; production do `release.yml` sở hữu (chỉ chạy khi tag) nên chưa vỡ nhưng sẽ vỡ ở lần cắt release tới | critical | `fixed` | Tách entry point: root barrel chỉ còn thứ an toàn cho Worker; `@lumibase/runtime/docker` (adapter docker) và `@lumibase/runtime/node` (`createRuntime` + leader lock) là subpath riêng. `middleware/runtime.ts` static-import **chỉ** `createCloudflareRuntime`, nhánh docker đi qua `await import()`. Nhưng dynamic import **vẫn là static edge** với bundler (esbuild inline target — đo được: bundle vẫn chứa bullmq), nên thêm `[alias]` trong `wrangler.toml` map subpath docker sang stub `runtime-docker-unavailable.ts` (throw kèm mô tả nếu nhánh bất khả thi kia chạy). Kết quả đo: upload Worker **8848.98 KiB → 6085.28 KiB** (số trước lấy từ log deploy đỏ cuối cùng, run 33953796948 ngày 2026-09-05; số sau là build của PR này), 0 dấu vết bullmq/ioredis/aws-sdk/sql-loader ở cả 3 env. Gate mới: `pnpm verify:worker-bundle` (assert thành phần bundle — **đây** mới là hàng rào thật, đã test âm: thêm lại re-export docker → đỏ đúng) + `pnpm verify:worker-startup` (boot Worker bằng workerd). **Lưu ý đã đo:** startup gate **KHÔNG** bắt được ca này — với docker cố tình bundle lại, `wrangler dev` vẫn boot và trả `/health` 500, vì fallback quét stack `file:///` của bullmq thành công ở local mà thất bại ở Worker deploy; nên đừng đọc "startup xanh" thành "deploy được". Cả hai chạy trong CI job `worker-bundle`. Bonus: `serve.ts` trước đây inject runtime bằng middleware thứ hai đặt **sau** `withRuntime`, nên Docker mode dựng **hai** runtime mỗi process (hai kết nối Redis, hai pg pool) rồi bỏ một — nay dùng `setRuntimeFactory`, còn một Issue #459. |
| B56 | 2026-09-09 · review PR #462 (harness redaction) | vuln (info leak, test tooling) | `apps/cms/src/__tests__/helpers/db-harness.ts` | **Redact chỉ đúng ở `error.message`, không đúng ở error chain đã render.** `DbIntegrationUnreachableError` gắn lỗi driver gốc làm `{ cause }`; mọi reporter render chain (`util.inspect`, output của vitest, phần lớn log shipper) in nested error đó nguyên văn — nên dòng cha ghi `target: postgresql://u:***@h/db` trong khi dòng dưới in đúng password mà driver đặt vào message của nó. Kèm theo, `redactSecretsInMessage` chỉ thay userinfo `user:pass@`, bỏ qua credential nằm ở query string: reproduce bằng secret giả, `driver failed postgresql://h/db?password=FAKE_REVIEW_SECRET` trả về **không đổi**. Cùng class "guard có mà không fire" với B31/B36/B53 — hàm redact tồn tại, có test, và test chỉ assert `.message` nên không bao giờ thấy chain | medium | `fixed` | Bỏ `{ cause }` (thông tin chẩn đoán được gộp vào message qua `describeCause`, đã redact) + thêm pass mask secret query param, dùng chung `SECRET_PARAM` với `redactConnectionString`. Test assert trên **chain đã render** (`util.inspect` depth 10), `String(error)`, `JSON.stringify` và `error.cause === undefined`, bằng DSN secret giả ở cả hai dạng (userinfo + query). Đã kiểm âm: gắn lại `{ cause }` → ca chain đỏ; bỏ pass query → 2 ca đỏ. Không dùng credential thật ở bất kỳ bước nào |
| B57 | 2026-09-09 · review PR #462 (integration với #456) | task (test debt) | `apps/cms/src/services/__tests__/g1-db-integration.ts` ↔ `apps/cms/src/__tests__/helpers/db-harness.ts` | Hai helper cùng hợp đồng tồn tại song song: #456 tự viết `g1-db-integration.ts` vì `db-harness.ts` của #462 chưa có trên `main` (docstring của nó ghi rõ chủ ý và hẹn xoá khi #462 land). Tripwire source-scan của #462 (`db-integration-guard.wiring.test.ts`) yêu cầu **mọi** `*.db.integration.test.ts` import `helpers/db-harness`, nên ba suite G1 của #456 sẽ đỏ ngay khi hai nhánh gặp nhau — không phải xung đột văn bản nên `git merge` không báo gì | medium | `open` | Khi #462 vào `main`: xoá `g1-db-integration.ts`, re-point ba import G1 sang `helpers/db-harness` (`connectDbIntegration` của helper chung nhận tham số `label`, nên call site đổi từ `connectDbIntegration()` thành `connectDbIntegration('g1-…')`), rồi chạy tripwire trên cây đã gộp. Không tự nới tripwire — nó chính là hàng rào |
| B58 | 2026-09-09 · review PR #462 (§3 contributor docs) | task (docs debt) | `docs/{en,vi}/contributing/testing.md` | Cặp EN/VI của `testing.md` lệch cấu trúc **sẵn có** trên branch: `check-parity` báo đúng 3 problem giống nhau cả **trước và sau** khi PR này sửa section DB-integration — EN 17 heading / VI 14, EN 12 code fence / VI 11, và link `https://k6.io/` thiếu ở VI. Nghĩa là bản VI đã rụng vài section từ trước, `docs:i18n:detect` vẫn xếp cặp này up-to-date vì hash còn khớp (đúng điểm mù B37). PR #462 chỉ viết lại một section và dịch tay đủ cả hai bên, nên stamp bằng `--allow-structure-drift` kèm lý do thay vì kéo việc dịch phần còn lại vào một PR test-harness | low | `open` | So từng heading của hai bên, dịch bù các section VI còn thiếu (gồm phần có link k6) rồi stamp lại không cần `--allow-structure-drift`. Đây là công việc dịch thuần, không đụng code |
| B41 | 2026-09-05 · rà surface public (landing/README/npm) | bug (docs) | `README.md` mục "Quick start" | Copy nguyên khối Quick start thì **không chạy được**: `pnpm install` + `pnpm --filter @lumibase/cms dev` không có bước Postgres, không có bước env — và repo **không có root `.env.example`** (chỉ có `.env` gitignored), nên không có gì để `cp`. Kèm một claim đã stale nhiều phiên bản: *"The Studio placeholder dashboard pings `/api/v1/utils/health`"* — Studio từ lâu không còn là placeholder (endpoint thì đúng, `apps/cms/src/routes/utils.ts:16`). Nặng hơn: Quick start **không nhắc `npx create-lumibase`** dù đó là bootstrapper chính thức đã publish, và lệnh Docker duy nhất bị chôn trong đoạn "Release policy". Đường vào của người mới là đường của contributor monorepo | medium | `fixed` | Quick start viết lại thành ba đường tách bạch (starter qua `create-lumibase` · platform qua Docker compose · contributor monorepo), nêu tường minh prerequisite và trỏ `docs/en/deployment/local-development.md`. Xoá claim "placeholder" |
| B42 | 2026-09-05 · rà surface public (landing/README/npm) | bug (docs) | `packages/create-lumibase/README.md` (trang npmjs.com) | `docs/en/getting-started.md` **đã có** callout phân biệt "starter vs platform" (thêm 2026-09-03), nhưng callout đó không có trên trang npm — tức nó nằm ở nơi người ta đọc **sau**, không nằm ở nơi người ta hiểu sai. README npm nói `create-lumibase` "bootstraps a ready-to-run LumiBase project", trong khi template thật (`templates/default/`) chỉ gồm `src/server.ts` + `src/db/{schema,client,migrate}.ts` với resource `posts` demo: Hono + Drizzle, **không** Collections API, **không** Studio, **không** platform. Cộng thêm lệch cosmetic khó giải thích: template chạy ở `PORT=8787` (`templates/default/_env.example:13`) trong khi cả README gốc và docs kể câu chuyện `1989`. Người gõ `npm i lumibase` cũng mong cài CMS chứ không phải client + CLI | medium | `fixed` | Callout port sang đầu README npm của `create-lumibase` kèm bảng "bạn muốn gì → dùng gì", và ghi rõ trang này là **starter**. Port 8787 giữ nguyên (template là app của người dùng, không phải CMS) nhưng được nói ra thay vì để người đọc tự vấp |
| B43 | 2026-09-05 · rà surface public (landing/README/npm) | task (docs) | `README.md` mục "Release policy" + hai danh sách feature | Mục "Release policy" là **một đoạn văn liền mạch** nối chuỗi từ `0.26.0` lùi tới `0.5.0` — đó là CHANGELOG đặt trong README, và nó đẩy "Core Features" xuống dưới nó. Hệ quả cấu trúc: README có **hai danh sách feature trùng nhau và không khớp** — "✨ Key Features" (7 bullet Content OS) ở trên và "Core Features" (5 bullet edge/multi-tenancy) ở dưới — đọc như hai bản README bị nối vào nhau. Ngoài ra khối sponsorship $29/$99 vẫn nằm trong file dưới dạng comment HTML | low | `fixed` | Release policy thu về phần gate CI + bản hiện tại + lệnh Docker, chuỗi version dài trỏ về `CHANGELOG.md`; **giữ nguyên** tuyên bố nền `v0.5.0` Content OS là baseline (DoD §4 cấm xoá di sản 0.5.0). Hai danh sách feature gộp thành một |
| B44 | 2026-09-05 · rà surface public (landing/README/npm) | bug (docs) | `README.md` → `docs/ai-skills.md` | Link chết: README trỏ `./docs/ai-skills.md`, file thật là `docs/en/ai-skills.md`. Verify bằng test tồn tại trên toàn bộ link nội bộ của README — đây là link duy nhất đứt. Nhỏ, nhưng nằm ở file được đọc nhiều nhất của repo | low | `fixed` | Sửa path. Không có gate nào kiểm link nội bộ trong Markdown — xem B45 cho phần cơ giới hoá |
| B45 | 2026-09-05 · rà surface public (landing/README/npm) | bug (drift) | `README.md` ↔ `package.json` ↔ `apps/landing/src/app/page.tsx` | Ba surface public nói **ba con số version khác nhau** cùng lúc: README `Current release: v0.26.0`, root `package.json` `1.0.0-rc.1` (tag `v1.0.0-rc.1` đã có), landing JSON-LD `softwareVersion: "1.0.0-rc.1"`. `pnpm version:check` (`sync-version.mjs`) chỉ so root version với version của các package — nó **không** đọc README lẫn JSON-LD, nên cả hai chỗ đó drift im lặng. Đáng chú ý nhất là JSON-LD: `softwareVersion` là **string hardcode** trong `page.tsx`, tức nó sẽ lệch lại ở đúng lần bump tiếp theo. Cùng class với B20 (drift chỉ lộ khi có người vô tình chạm vào) | medium | `fixed` (một phần) | README đồng bộ về bản hiện tại; JSON-LD đọc version từ `package.json` của landing thay vì hardcode. **Còn mở:** `version:check` chưa kiểm README, nên dòng `Current release` vẫn dựa vào con người. Nếu cơ giới hoá thì đây là chỗ đúng để làm cùng với B33 (script `version-check.mjs` đang chết) |
| B46 | 2026-09-05 · rà surface public (landing/README/npm) | task (marketing/UX) | `apps/landing/src/components/Hero.tsx` · `page.tsx` | Landing thuyết phục xong rồi **không có đường cài**: grep `npx\|npm i\|pnpm \|docker \|create-lumibase` trong `apps/landing/src` trúng đúng **một** chỗ, là chữ "Docker" trong câu trả lời FAQ. Cả hai CTA chính ("Start building" ở Hero và ở CTA cuối) trỏ về `github.com/khuepm/lumibase`; nav chỉ có Docs + GitHub (`Header.tsx:33`); không demo hosted; không screenshot Studio thật (`StudioStage` là hình minh hoạ). Người đọc bị đẩy sang GitHub để tự tìm cách cài — nối trực tiếp vào B41. Cộng: `/pricing` bị ẩn có chủ ý bằng `robots` + `public/_redirects` nên không có surface giá nào | medium | `open` (hoãn có chủ ý) | Cần: khối cài đặt copy-paste trên landing + đổi "Start building" trỏ vào đó + một screenshot Studio thật. **Chặn bởi**: screenshot cần `studio.lumibase.dev` deploy được, hiện đang lỗi — điều tra deploy Studio là tiền đề. Tách khỏi loạt fix 2026-09-05 vì cần quyết định nội dung marketing, không chỉ sửa văn bản |
| B47 | 2026-09-05 · rà surface public (viết lại Quick start) | bug (docs) | `docs/en/deployment/local-development.md` §Quick start ↔ `docker/docker-compose.prod.yml` | Hai claim sai trong đúng cái doc mà README trỏ tới cho người mới. (a) Bước 3 bảo `cp .env.example .env` nhưng **root `.env.example` không tồn tại** — đáng chú ý là `.gitignore:20` đã có `!.env.example`, tức file này *được dự tính* có mặt và bị thiếu, không phải cố ý bỏ. (b) Bước 4 ghi "Start infrastructure (PostgreSQL, Redis, MeiliSearch, **Logto**)" và bảng dưới liệt `Logto | http://localhost:3001`, nhưng `docker/docker-compose.yml` **không có service Logto** nào (chỉ postgres/redis/minio/minio-init/meilisearch/imgproxy/cms/cms-web/cms-worker/bull-board) — khớp với ghi chú trong `AGENTS.md` rằng auth local đi bằng JWT + setup wizard. Riêng phần (c): chồng `docker-compose.prod.yml` lên stack local sẽ **không kết nối được DB** vì override đặt `DATABASE_SSL_MODE` default `require` trong khi postgres của compose không có TLS — không phải bug của override (production nên require) mà là cái bẫy khi doc trình bày nó như đường "thử ở local" | medium | `open` (một phần fixed) | (a) **fixed**: tạo root `.env.example` (16 key khớp `.env` local đang dùng + ghi rõ không có dotenv nào tự load, phải `set -a && source .env`), nên câu lệnh trong doc và trong README mục 3 giờ đúng. (b) **fixed 2026-09-15** (#333, lượt đo delta): Logto bị xoá khỏi bước 4 và khỏi bảng service ở **cả hai** locale, thay bằng MinIO / imgproxy / Bull Board với port đối chiếu trực tiếp từ `docker/docker-compose.yml` (`BULL_BOARD_PORT:-3001→3000`, `MINIO_API_PORT:-9000`, `IMGPROXY_PORT:-8080`). Cùng lượt sửa luôn ba claim sai khác trong file: `db:reset` và `wrangler:dev` **không tồn tại** ở bất kỳ package nào, và `STUDIO_PORT` cũng vậy — port Studio hardcode `server.port: 2026` ở `apps/studio/vite.config.ts:68`, không có env override, nên mục "Port conflicts" trước đây hướng người đọc sửa một biến không ai đọc. (c) **còn mở**: prod override cần nói rõ `DATABASE_SSL_MODE=disable` khi chạy với postgres non-TLS của compose |
| B48 | 2026-09-05 · điều tra deploy studio.lumibase.dev | bug (CI) | `.github/workflows/pages-deploy.yml` ↔ `.github/workflows/release.yml` job `deploy-pages` | Hai workflow **cùng deploy đúng bốn Pages project** trên **cùng một trigger** `push: tags: v*.*.*`. Verify trên `v0.26.0`: run `Release` (32695793563) deploy docs/landing/marketplace/studio, và run `Deploy Pages` (32695793562) deploy lại đúng bốn cái đó — mỗi tag stable deploy hai lần, song song, hai `concurrency` group khác nhau nên không chặn nhau. Với tag prerelease thì hai file **nói ngược nhau**: `release.yml` có `if: !contains(github.ref_name, '-')` nên bỏ qua Pages một cách êm, còn `pages-deploy.yml` khai trigger glob `v*.*.*` (glob này **khớp** `v1.0.0-rc.1`) rồi chặn lại ở validator `^v[0-9]+\.[0-9]+\.[0-9]+$` → job chết ở step 4, trước khi build gì cả (run 33715778982, 13s). Cùng ca thứ hai: `workflow_dispatch` không truyền `tag` thì `RELEASE_TAG` fallback về `github.ref_name` = `main`, cũng trượt regex (run 32755571789) — tức dòng mô tả input *"Defaults to the selected ref"* hứa một hành vi mà validator cấm. Cùng class B31/B36: guard đọc thì đúng nhưng mâu thuẫn với chính bề mặt quanh nó | medium | phần red-run `fixed` (theo #451) · phần deploy trùng `open` | **Đối chiếu #451 trước khi sửa (2026-09-06)** theo yêu cầu của #331 (*"Scope #451 có Pages và desktop/mobile; thay đổi local hiện có phải đối chiếu trước"*). #451 nêu đúng ca này (run 33715778982) và chốt cách xử lý: *"RC tags should **skip** this stable production workflow rather than create a red run"*. Nên fix đi theo đúng câu đó, KHÔNG bỏ trigger tag như bản nháp đầu: thêm `if: github.event_name == 'workflow_dispatch' \|\| !contains(github.ref_name, '-')` ở cấp job — cùng biểu thức `release.yml` dùng cho `deploy-pages` để hai file không lệch định nghĩa "prerelease"; chỉ dùng context `github` trong `if` cấp job (context `env` ở đó làm invalid cả file, xem B31). Kèm theo: `inputs.tag` thành `required: true` (hết ca fallback ra `main`), validator nhận cả prerelease `^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.]+)?$` — verify bằng bash: nhận `v1.0.0-rc.1`/`v0.26.0`/`v1.0.0`, loại `main`/`v1.2`, loại chuỗi có khoảng trắng (input người truyền chảy vào `git`/`wrangler`). Nhờ đó redeploy tay một RC **làm được**, trước không. **CÒN MỞ — cần quyết định của owner:** phần deploy trùng bốn project trên mọi tag stable vẫn nguyên, vì nó **ngoài scope #451** và #331 cấm tự mở rộng scope. Bằng chứng đã có: `v0.26.0` → run 32695793563 (`Release`) và 32695793562 (`Deploy Pages`) đều deploy docs/landing/marketplace/studio. Chọn một trong hai chủ sở hữu deploy-theo-tag rồi bỏ bên kia. Cũng **không** đóng #451: các mục Android NDK / macOS codesign / `tauri-action` input trong `release-apps.yml` chưa ai chạm (liên quan B35). **Lưu ý:** đây KHÔNG phải lý do Studio hỏng — xem B49 |
| B49 | 2026-09-05 · điều tra deploy studio.lumibase.dev | bug (production) | `api.lumibase.dev` (CMS Worker) — subsystem cache + route `/api/v1/setup/*` | Studio "lỗi" **không phải** do Pages: `https://studio.lumibase.dev/` trả **200**, `<title>LumiBase Studio`, bundle `/assets/index-A99KRh8s.js` có mặt — SPA được serve bình thường (bản `v0.26.0`, vì RC không deploy Pages theo thiết kế). CORS cũng đúng: preflight với `Origin: https://studio.lumibase.dev` trả 204 + `ACAO` khớp, còn `Origin: https://evil.example.com` **không** được cấp `ACAO`. Cái vỡ nằm ở CMS production: `GET /health` → `{"status":"degraded"}`, `GET /health/ready` → **503** (theo doc của route nghĩa là cache degraded hoặc down), và **cả hai** endpoint mà Studio gọi đầu tiên đều **500 INTERNAL**: `/api/v1/setup/state` (requestId `6RGPxXhmcutG`) và `/api/v1/setup/capabilities` (requestId `Ci3K2h3TLdPz`). `GET /api/v1/utils/health` vẫn 200 `{"status":"ok"}` — nhưng đó là probe nông (chỉ env + timestamp), nên nó xanh trong lúc probe sâu đỏ. Studio chết ngay ở setup-state gate, đúng biểu hiện "vào không được". Không đọc thêm được vì `services` breakdown của `/health` bị gate sau `canReadObservabilityDetail` và log Worker cần credential production | high | `open` | Cần người có quyền production: `wrangler tail` trên Worker CMS để lấy stack của hai requestId trên, hoặc gọi `/health` kèm token đọc observability để biết subsystem nào đỏ. Nghi vấn đầu tiên theo thứ tự: binding cache/KV (khớp 503 ở `/health/ready`) rồi tới Hyperdrive/DB (khớp 500 ở hai route setup vì cả hai đều đọc DB). **Chặn mục B46**: chưa vào được Studio thì chưa chụp được screenshot thật cho landing. Cũng đáng chú ý: `/health` trả 200 khi degraded nên uptime monitor kiểu "200 là ổn" sẽ không báo — chính là lý do trạng thái này sống được mà không ai thấy |
| B50 | 2026-09-06 · #450 (tái hiện scaffold) | vuln (deps) | `packages/create-lumibase/templates/{default,cloudflare}/package.json.hbs` | Project scaffold từ template **default** install được nhưng mang sẵn advisory: `npm audit --audit-level=high` exit 1 với **`GHSA-gpj5-g38j-94v9` — Drizzle ORM SQL injection via improperly escaped SQL identifiers** (severity high, fix ở `drizzle-orm@0.45.2`, là breaking), tổng `5 vulnerabilities (4 moderate, 1 high)`. Cả hai template khai `drizzle-orm: ^0.44.0`. Nghĩa là mọi người mới chạy `npm create lumibase` hôm nay đều bắt đầu dự án với một lỗ SQL injection đã có advisory công khai — đúng chỗ đắt nhất, vì template được quảng bá là "LumiBase conventions" nên người ta tin và copy tiếp. Repo có gate `Dependency vulnerability audit` cho chính nó (B8) nhưng **không** gate nào chạy trên output của scaffolder. Phát hiện khi tái hiện #450; **ngoài scope #450** (issue đó chỉ nói `@cloudflare/workers-types` vs Wrangler) và #331 cấm tự mở rộng scope | high | `open` | Nâng `drizzle-orm` lên `^0.45.2` ở cả hai template (breaking upstream → cần đọc changelog drizzle rồi sửa `src/db/schema.ts`/`client.ts` của template nếu cần) và cân nhắc thêm `npm audit --audit-level=high` vào `scripts/smoke-scaffold.mjs` để class này có gate thay vì trông vào người rà. Cần issue riêng (hoặc mở rộng #450 có duyệt) — không tự làm trong lượt này |
| B51 | 2026-09-06 · audit tiền đề #332 | bug (docs) | `docs/en/deployment/overview.md` · `docker/docker-compose{,.prod}.yml` · `docker/Dockerfile` | **Không có đường nào cho self-hoster lấy được Studio UI**, trong khi docs nói ngược lại. `docs/en/deployment/overview.md` viết *"Docker serves the Studio from the same origin as the CMS"* — sai: `grep -c studio` trên **cả hai** compose file trả **0**, `docker/Dockerfile` chỉ `COPY apps/cms/dist`, và CMS **không có** static serving nào (`serveStatic` không xuất hiện ở đâu trong `apps/cms/src`; hai chỗ `c.html()` duy nhất là trang `test-auth` dev và callback OAuth git). Verify trên CMS production đang chạy: `GET https://api.lumibase.dev/setup` → **404** `application/json` `{"errors":[{"code":"NOT_FOUND"}]}`, và `/`, `/studio`, `/admin` cũng 404. Nghĩa là `docker compose up` cho **API only**; muốn có UI phải tự build/host `apps/studio/dist` với `VITE_API_URL`, hoặc chạy setup bằng `POST /api/v1/setup/complete`. Đây là **tiền đề trực tiếp của #332** ("A new user … can provision/connect CMS **and Studio** using explicit prerequisites"): với artifact hiện tại, acceptance đó **không đạt được** — không phải vì code sai mà vì chưa có artifact nào chứa Studio | high | `fixed` | Tôi tự vấp chính lỗi này: bản nháp README của lượt trước viết *"open http://localhost:1989/setup to create the first admin"* — một URL trả 404. Đã sửa trong cùng PR #457. **Đóng 2026-09-07 bằng option B** (owner chọn): CMS serve Studio ở Docker mode — `apps/cms/src/serve-studio.ts` + `docker/Dockerfile` build & copy `apps/studio/dist` → `/app/studio`, và `/assets/` + `/sw.js` vào bypass list của `adminPathGuard`. Claim ở `docs/{en,vi}/deployment/overview.md` nay **đúng** thay vì bị xoá, kèm bảng path và hai env var mới. Verify bằng ảnh build thật: `Serving Studio from /app/studio`, `/setup` + `/<adminPath>/…` trả shell, `/` + `/admin` + `/studio` + admin-path-lệch-một-ký-tự vẫn trả canonical 404 (Hide-Login nguyên vẹn), 11/12 ref của shell resolve — cái thứ 12 là `favicon.svg` thiếu sẵn, xem B52 |
| B52 | 2026-09-07 · option B (#332) verify ảnh Docker | bug (asset thiếu) | `apps/studio/index.html` ↔ `apps/studio/public/` | `index.html` của Studio tham chiếu `favicon.svg` nhưng **file không tồn tại ở đâu trong repo** — `git ls-files \| grep -i favicon` chỉ ra `apps/consumer/src/app/favicon.ico`, và `apps/studio/public/` chỉ có `sw.js`. Trên Cloudflare Pages lỗi này **im lặng**: `https://studio.lumibase.dev/favicon.svg` trả **200 `text/html`** (SPA fallback của Pages phục vụ shell làm icon), nên browser chỉ hiện icon mặc định và không ai để ý — đã tồn tại bao lâu không rõ. Phát hiện ngay lần chạy đầu của option B vì `serve-studio.ts` **cố ý** 404 asset thiếu thay vì trả HTML: 11/12 ref của shell resolve, ref thứ 12 là cái này. Không phải regression của option B — nó chỉ làm lỗi sẵn có hiện ra | low | `open` | Thêm `apps/studio/public/favicon.svg` (hoặc bỏ thẻ `<link rel="icon">` khỏi `index.html` nếu cố ý không có icon). Đáng ghi lại như một điểm cộng của quyết định lệch khỏi Pages: nếu bắt chước Pages trả 200 HTML thì lỗi này vẫn đang ẩn |
| B53 | 2026-09-05 · rà surface public (landing/README/npm) | bug (release) | `.github/workflows/release.yml` bước "Publish public packages" ↔ npm registry | Hai package resolve về **hai generation khác nhau** của cùng sản phẩm: `npm i lumibase` cho `1.0.0-rc.1` còn `npm i @lumibase/sdk` cho `0.26.0`. Đo bằng `npm view <pkg> dist-tags`: `lumibase` và `@lumibase/contracts` có `latest = 1.0.0-rc.1`, trong khi `@lumibase/sdk` / `create-lumibase` / `@lumibase/mcp-server` / `@lumibase/extension-sdk` đúng `latest = 0.26.0` + `next = 1.0.0-rc.1`. Workflow **không sai**: nó đã tính `distTag = version.includes('-') ? 'next' : 'latest'` và publish `--tag next`. Nguyên nhân là hành vi của npm — **lần publish đầu tiên của một package mới luôn set `latest`**, bất kể `--tag`. `npm view lumibase versions` chỉ có duy nhất `1.0.0-rc.1`, xác nhận cả hai package sinh ra ở đúng bản RC. Cùng class "guard có mà không fire" với B31/B36: điều kiện `--tag next` đọc thì đúng nhưng không tạo ra kết quả nó hứa | medium | `fixed` (guard, report-only) · registry là quyết định đã chốt | Guard `scripts/check-npm-dist-tags.mjs` + test `scripts/__tests__/check-npm-dist-tags.test.mjs`, gọi trong `release.yml` sau bước publish. Nó phân biệt hai ca: **fail** khi `latest` trỏ prerelease trong lúc đã có bản stable (drift sửa được), **warning** khi chưa có stable nào (đúng ca này — không có gì để trỏ `latest` về, và npm không cho `dist-tag rm ... latest`). **Đối chiếu #331/#448 (2026-09-06):** epic #331 tuyên bố "không tự đổi dist-tag", và comment của #448 ghi *"owner chose to keep this behavior for lumibase"* — tức `latest = 1.0.0-rc.1` là **quyết định đã chốt**, không phải drift cần vá. Vì vậy CI chạy **report-only**, `--fix` bỏ khỏi workflow và chỉ còn là lệnh tay `pnpm dist-tags:fix`. Trạng thái registry sẽ tự nhất quán khi `1.0.0` stable phát hành; tới lúc đó đây không phải việc mở |
| B55 | 2026-09-07 · deployment-integrations task 12.3 (webhook signature) | bug (docs) | `docs/{en,vi}/api/hono-api-spec.md` | Task 16.1 của spec `deployment-integrations` được tick `[x]` với nội dung *"section 7b endpoints deployment"*, nhưng **section đó không tồn tại**: `grep -n "deployment"` trên `docs/en/api/hono-api-spec.md` không trúng một endpoint nào (chỉ trúng chữ "deployment" trong mô tả rate-limit/encryption), `grep "7b"` trả rỗng, và bản VI cũng vậy — mục lục nhảy từ `## 7. Flows / Automation` sang `## 8. AI Copilot`. Nghĩa là toàn bộ surface `/api/v1/deployments/*` (targets CRUD, trigger, list/detail/logs/refresh, webhook vào) **không có trong API spec** dù spec đó là artifact được `v1-release-criteria.md` §2 xếp vào "API surface freeze". Feature doc `docs/{en,vi}/features/deployment-integrations.md` §5 có bảng REST đầy đủ nên thông tin không mất, nhưng nó nằm sai chỗ so với cái checklist DoD §4 đang trỏ tới. Cùng class "checkbox tasks.md không phải bằng chứng" mà chính `v1-scope-classification.md` cảnh báo | medium | `open` | Thêm `## 7c. Deployments` vào `docs/en/api/hono-api-spec.md` + bản VI cùng PR (DoD §4a) rồi re-stamp pair; ghi rõ header chữ ký bắt buộc của endpoint webhook (`x-vercel-signature` / `x-webhook-signature`) + `401 INVALID_SIGNATURE`. Không làm trong task 12.3 vì đó là một section API spec mới cho cả feature, không phải một dòng thuộc scope verify chữ ký |
| B54 | 2026-09-07 · deployment-integrations task 6.2 (rate-limit) | task (docs debt) | `docs/{en,vi}/security/anti-abuse.md` | Cặp EN/VI của `anti-abuse.md` lệch cấu trúc sẵn trên `main`: `stamp-pair.mjs` từ chối stamp vì EN có 4 in-page anchor link còn VI có 0, và link tới `../api/graphql-api-spec.md#abuse-guards` ở VI trỏ anchor đã dịch (`#chống-lạm-dụng`). Phần anchor đã dịch là **false positive** (heading khác locale thì slug phải khác — cùng loại với ca (d) của B37), nhưng 4 anchor nội trang thiếu ở VI là thiếu thật: bản VI viết "xem phần Khoảng trống & khuyến nghị bên dưới" bằng chữ thay vì link, nên người đọc VI không nhảy được tới mục đó. `docs:i18n:detect` xếp cặp này `up-to-date` vì hash còn khớp — đúng cái điểm mù B37 đã nêu. Ngoài scope task 6.2 (chỉ thêm một bullet registry vào §1) | low | `fixed` | Đóng 2026-09-10 khi tích hợp #458 vào #457: thêm đủ 4 liên kết nội trang ở bản VI; `check-parity.mjs` so đường dẫn của liên kết Markdown tương đối nhưng cho phép fragment heading đã dịch khác nhau, còn fragment URL ngoài vẫn được giữ nguyên. Hai test khóa cả hai hành vi; cặp được verify 72 code reference, stamp lại và vượt parity không cần waiver cấu trúc. |
| B59 | 2026-09-09 · review PR #458 (parity gate) | bug (CI guard không fire) | `.github/workflows/docs-i18n-sync.yml` · `scripts/docs-i18n/gate-changed-parity.mjs` | **Gate parity bỏ qua đúng ca nó cần bắt.** Workflow lấy danh sách file bằng `git diff --name-only --diff-filter=d`, mà `--diff-filter=d` **loại file bị xoá**. Một PR xoá riêng `docs/vi/x.md` để bản EN mồ côi nhưng **không cung cấp file doc nào** cho gate → danh sách rỗng → gate exit 0 và PR xanh. Rename cũng cùng lỗ, qua đường path cũ đã biến mất. Bản thân script **đã có** xử lý orphan từ đầu — nó chỉ không bao giờ nhận được path đó, nên đây là class "guard có mà không fire" giống B31/B36/B53/B56: đọc code script thì thấy đúng, đọc workflow mới thấy sai | medium | `fixed` | Bỏ `--diff-filter=d` và dùng `--no-renames` để luôn nhận cả path cũ lẫn mới; gate tự phân loại theo số locale còn sống: cả hai còn → so parity, còn một → **đỏ** (orphan), không còn bên nào → **xanh** (cho doc về hưu, không cần override). Tách `relsFromChangedFiles` + `classifyPairs` thành hàm thuần và thêm `scripts/__tests__/gate-changed-parity.test.mjs` (11 ca `node --test`: add một bên, edit hai bên, xoá một bên mỗi phía, xoá cả hai, rename hai bên, rename một bên, cộng hai ca dựng **repo git thật** để chứng minh workflow giữ deletion và path cũ của rename). Đã kiểm âm: coi half-deleted là hợp lệ → 5 ca đỏ. `scripts:test` đã nằm trong `check:all` của CI |
| B64 | 2026-09-13 · A-02 review PR #468 (#332) | vuln (availability) | `apps/cms/src/middleware/tenant.ts` · `middleware/auth.ts:93` · `modules/audit/worker.ts:139-141` | **DoS không cần xác thực, một request là đủ.** `withTenant` chỉ shape-check `X-Lumi-Site`, không kiểm tra site tồn tại; khi từ chối api key, `auditApiKeyUseDenied` ghi audit dưới chính site id client gửi → vi phạm FK `lumibase_audit_log_site_id_lumibase_sites_id_fk`. `AuditLogger.write` **có** bắt lỗi cho insert đồng bộ (`logger.ts:485-490`), nhưng đường queue thì batcher bắt rồi **`throw err`** trong flush fire-and-forget (`void this.scheduleFlush()`) ⇒ unhandled rejection ⇒ chết process. Batch còn gộp nhiều site trong một insert nên một hàng hỏng **mất luôn audit của site hợp lệ** (mất dấu vết bảo mật, không chỉ downtime). Chỉ site id **không tồn tại** mới gây crash: probe với site thật trả 401 và server vẫn khỏe | high | `fixed` | Issue **#469**. **Đã đóng hoàn toàn trên `main` TRƯỚC lượt PR #481**, ba lớp, không phải hai: (1) `ca870dbf` thêm `withTenantExists` — chạy sau `withDb` và **trước `withAuth`** (middleware đầu tiên ghi audit mang `siteId`), có cache `site-exists-cache`, trả **404 `TENANT_NOT_FOUND`**; đây là lớp đóng đúng cái gốc mà dòng này mô tả; (2) `85ed2925` cho `auditApiKeyUseDenied` ghi dưới site của **chính key**, giữ id do client cấp ở `metadata.requestedSiteId` (cột không có FK); (3) `fe2f3c75` cho `scheduleFlush` giữ tail luôn fulfilled nên nhánh fire-and-forget không còn unhandled rejection. **Đo trên CMS thật (2026-09-20, DB riêng `lumibase_a03`)**: 4 probe `X-Lumi-Site: site_does_not_exist_a03` (3 lượt không xác thực + 1 lượt kèm API key hợp lệ của site khác) đều trả `404`, `/utils/health` vẫn `200` sau đó, `lumibase_audit_log` **không tăng một hàng nào** và có **0** hàng dưới site giả. PR #481 thêm một lớp thứ tư ở tầng batcher (`AuditLogBatcher.insertWithSalvage`): multi-row INSERT là atomic nên một hàng FK sai vẫn xoá tới 99 hàng cùng batch, và đường HTTP không còn tạo được hàng như vậy **nhưng đường ngoài request thì có** — job audit nằm trong queue rồi site bị xoá trước khi flush, hoặc cron/CDC/worker truyền một `siteId` đã biến mất. Salvage chỉ chạy trên đường lỗi (giữ nguyên lợi ích batching), log hàng bị drop kèm `siteId` + `event` mà **không** in metadata, và vẫn rethrow để hợp đồng của `worker-fk-crash.test.ts` không bị nới. **Đính chính:** bản trước của dòng này (và commit `f89e5b57`) viết rằng `withTenant` chỉ shape-check nên "một request không cần xác thực có thể kích hoạt" — đúng về `withTenant` nhưng **sai về hệ thống**, vì `withTenantExists` đã chặn từ `ca870dbf`. Sai vì kết luận từ đọc một middleware thay vì đo chuỗi middleware; đã sửa sau khi probe CMS thật. Starter #332 có thể bỏ cờ `LUMIBASE_VERIFY_CROSS_TENANT=1` |
| B65 | 2026-09-13 · A-02 review PR #468 (#332) | bug | `apps/cms/src/modules/setup/setup-token.ts:148` · `apps/cms/src/serve.ts` | **`LUMIBASE_REQUIRE_SETUP_TOKEN=true` khoá chết instance.** `printSetupTokenIfRequired` sinh token, lưu hash và in một dòng `[lumibase-cms] SETUP_TOKEN=…` — nhưng **không được gọi từ đâu** lúc khởi động (grep toàn repo: 3 kết quả, đều trong chính file đó). Phía đọc đã nối đủ (`verifySetupToken`, nhánh `requiresSetupToken`, mã `SETUP_TOKEN_REQUIRED`), nên hệ thống *kiểm tra* một token mà không có gì *sinh* ra nó ⇒ `/setup/complete` trả `SETUP_TOKEN_REQUIRED` vĩnh viễn, không có đường phục hồi ngoài tắt cờ + tạo lại container. Requirement ghi rõ phải sinh **lúc startup** (`admin-setup-wizard/requirements.md:63`); unit test gọi thẳng hàm nên vẫn xanh — khoảng trống giữa "hàm đúng" và "hàm được nối vào". Hệ quả: instance expose **không thể** bảo vệ theo cách tài liệu mô tả | medium | `fixed` | Issue **#470** — nhánh `fix/setup-token-startup-470`: `serve.ts` await `runSetupTokenStartup` trước `serve()`, ghi có điều kiện (đa replica in đúng 1 token), một parser cờ chung; Cloudflare = chỉ Node/Docker, `/setup/complete` trả `503 SETUP_TOKEN_NOT_ISSUED`; test đi qua đường khởi động + tripwire (Setup Impact #136). **Còn lại, ngoài scope:** starter #332 vẫn ghim image CMS **trước** bản sửa nên giữ cờ tắt + bind loopback là đúng cho image đó; khi ghim lại image có bản sửa thì xem lại README/compose/`.env.example`/`bootstrap.mjs` của template. Các chỗ đó còn nói helper "unit-tested" — thực tế trước #470 không có test nào cho `setup-token.ts` |
| B61 | 2026-08-31 · rà workflow trên main sau batch dependabot (rebase 2026-09-06) | vuln (availability) | `packages/runtime/src/index.ts` · `apps/cms/src/middleware/runtime.ts` · `apps/cms/wrangler.toml` | **Mọi deploy Cloudflare bị Cloudflare từ chối** kể từ khi bullmq 6 vào main (#421, 2026-08-30): `Uncaught Error: Could not determine sql-loader directory path`, validation error 10021. Nguyên nhân: `packages/runtime/src/index.ts` re-export **cả** `./adapters/docker` (và `./factory` = `createRuntime`, và `./leader-lock` import `ioredis` như **value**), còn `middleware/runtime.ts` import `createRuntime` từ barrel → bundle Worker kéo theo cả cây docker gồm `bullmq`. BullMQ 6 thêm backend Postgres, và `dist/esm/postgres/sql-loader.js` gọi `getDirname()` ở **top level module**, throw khi không có `__dirname` lẫn frame `file:///` — đúng môi trường Worker đã bundle. Worker throw lúc khởi tạo ⇒ script bị từ chối. **Vì sao mọi gate đều xanh:** `pnpm build` chạy `wrangler deploy --dry-run`, nó *bundle mà không instantiate*; job `build` của CI vì thế không thể thấy class lỗi "throw ở top level". Đây cũng là chỗ tôi kết luận sai khi rà #421 — coi "build pass + round-trip Redis thật" là đủ, trong khi chưa có gì boot Worker. Chỉ `deploy-cms.yml` (dev/staging) đỏ; production do `release.yml` sở hữu (chỉ chạy khi tag) nên chưa vỡ nhưng sẽ vỡ ở lần cắt release tới | critical | `fixed` | Tách entry point: root barrel chỉ còn thứ an toàn cho Worker; `@lumibase/runtime/docker` (adapter docker) và `@lumibase/runtime/node` (`createRuntime` + leader lock) là subpath riêng. `middleware/runtime.ts` static-import **chỉ** `createCloudflareRuntime`, nhánh docker đi qua `await import()`. Nhưng dynamic import **vẫn là static edge** với bundler (esbuild inline target — đo được: bundle vẫn chứa bullmq), nên thêm `[alias]` trong `wrangler.toml` map subpath docker sang stub `runtime-docker-unavailable.ts` (throw kèm mô tả nếu nhánh bất khả thi kia chạy). Kết quả đo: upload Worker **8848.98 KiB → 6085.28 KiB** (số trước lấy từ log deploy đỏ cuối cùng, run 33953796948 ngày 2026-09-05; số sau là build của PR này), 0 dấu vết bullmq/ioredis/aws-sdk/sql-loader ở cả 3 env. Gate mới: `pnpm verify:worker-bundle` (assert thành phần bundle — **đây** mới là hàng rào thật, đã test âm: thêm lại re-export docker → đỏ đúng) + `pnpm verify:worker-startup` (boot Worker bằng workerd). **Lưu ý đã đo:** startup gate **KHÔNG** bắt được ca này — với docker cố tình bundle lại, `wrangler dev` vẫn boot và trả `/health` 500, vì fallback quét stack `file:///` của bullmq thành công ở local mà thất bại ở Worker deploy; nên đừng đọc "startup xanh" thành "deploy được". Cả hai chạy trong CI job `worker-bundle`. Bonus: `serve.ts` trước đây inject runtime bằng middleware thứ hai đặt **sau** `withRuntime`, nên Docker mode dựng **hai** runtime mỗi process (hai kết nối Redis, hai pg pool) rồi bỏ một — nay dùng `setRuntimeFactory`, còn một Issue #459. |
| B60 | 2026-09-09 · review PR #456 (§4a docs) | task (docs debt) | `docs/{en,vi}/features/agent-harness-layer.md` | Bản VI của `agent-harness-layer.md` **không phải bản dịch** của bản EN mà là một tài liệu khác: một đề xuất 8 mục có số, 10 heading so với 25 của EN, dài **25%** bản EN, có bảng và code fence mà EN không có và thiếu 253 inline-code identifier EN có. Front matter vẫn ghi `syncStatus: human-translated`, và `docs:i18n:detect` xếp cặp này up-to-date vì hash còn khớp — đúng điểm mù B37/B54/B58 nhưng ở quy mô cả file. PR #456 thêm phần approval claim/quarantine/reopen vào **cả hai** bên trong cùng commit nên không làm tệ thêm, và stamp bằng `--allow-structure-drift` kèm lý do | medium | `open` | Dịch lại bản VI từ bản EN hiện tại (hoặc quyết định bản VI là một tài liệu roadmap riêng và **đổi tên** nó, bỏ `translatedFrom`/`syncStatus` để nó không còn giả vờ là một nửa của cặp). Việc dịch thuần, không đụng code. **Cập nhật 2026-09-10:** parity gate (#458) đã merge và chặn PR #456 vì cặp này; đã thêm waiver `<!-- check-parity: allow headings code-fences inline-code links tables bulk -->` vào đầu bản VI để gate không chặn thay đổi không liên quan. Waiver ghi nhận hiện trạng chứ không chấp nhận nó — **gỡ waiver khi làm xong mục này**, lúc đó cặp phải qua parity mà không cần miễn trừ |
| B62 | 2026-09-11 · verify batch dependabot (#466/#465/#464/#417) | task (deps hygiene) | `apps/shell/package.json` ↔ `apps/shell/src-tauri/Cargo.lock` | Bốn dependency npm `@tauri-apps/*` của `apps/shell` **không thể ảnh hưởng gì** nhưng vẫn sinh PR dependabot đều đặn, và tệ hơn là tạo cảm giác sai rằng JS↔Rust đang được giữ đồng bộ. App này **không có source JS/TS nào** (chỉ `src-tauri/`; `frontendDist` trỏ sang build của `apps/studio` — contract C3), và grep `@tauri-apps/plugin` trên toàn `apps/` + `packages/` trả **0 hit**: plugin chỉ được dùng từ Rust (`src-tauri/src/lib.rs` — `tauri_plugin_dialog::DialogExt`, `tauri_plugin_updater::UpdaterExt`, `tauri_plugin_deep_link::DeepLinkExt`). Phía thật sự chạy là crate do Cargo resolve, mà `Cargo.toml` khai `"2"` nên `Cargo.lock` mới là nơi chốt version — hiện `tauri-plugin-dialog 2.7.1` / `deep-link 2.4.9` / `updater 2.10.1`, trong khi #466 nâng phía npm lên `^2.7.3` / `^2.4.10` / `^2.11.0`. Nghĩa là con số trong `package.json` mô tả một thứ không được cài để dùng. Không phải bug đang gây hỏng — nhưng đúng class "guard/khai báo đọc thì đúng, thực tế không nói gì" của B20/B31/B53 | low | `open` | Chọn một: (a) xoá 4 dep npm `@tauri-apps/plugin-*` khỏi `apps/shell/package.json` (giữ `@tauri-apps/cli`, và `@tauri-apps/api` nếu tauri CLI cần để cảnh báo lệch version) rồi ghi một dòng trong `apps/shell/README.md` rằng plugin sống ở Cargo; hoặc (b) giữ chúng nhưng thêm gate so `Cargo.lock` với `package.json` để hai bên không trôi lệch âm thầm. Kèm theo: `Cargo.lock` chưa được refresh sau nhiều đợt bump — `cargo update -p tauri-plugin-dialog` v.v. nên nằm trong quy trình bump shell (liên quan B35: `release-apps.yml` không được PR nào exercise) |
| B63 | 2026-09-11 · verify batch dependabot (#417 TypeScript 7) | task (deps · blocked upstream) | `packages/{sdk,contracts,cli,mcp-server,extension-sdk,create-lumibase}` (tsup `--dts`) · `apps/{landing,consumer}` (lint) | **TypeScript 7.0.2 chưa nâng được, và lý do không phải code của repo.** Đo trực tiếp trên nhánh thử: `tsc --noEmit` xanh **18/18** package sau khi bỏ 2 chỗ `baseUrl` (đã land, xem CHANGELOG), và nhanh hơn hẳn — **8.5s so với 2m9s** trên TS 5.9.3. Nhưng hai chỗ vỡ, cả hai là upstream từ chối tường minh: (1) `pnpm lint` chết ngay lúc `require` với **`Error: typescript-eslint does not support TS 7.0.`** (`typescript-eslint@8.70.0`, peer `>=4.8.4 <6.1.0`, vào cây qua `eslint-config-next`) → job CI `Lint stable packages` đỏ cho `apps/landing` + `apps/consumer`; (2) `tsup --dts` chết với `TypeError: Cannot read properties of undefined (reading 'useCaseSensitiveFileNames')` bên trong `rollup-plugin-dts`, vì TS 7 **không còn export compiler API**: entry `.` của package trỏ `./lib/version.cjs` và `require('typescript')` trả **đúng 2 key** (`version`, `versionMajorMinor`). Đã kiểm âm để chắc nguyên nhân là DTS chứ không phải tsup: cùng lệnh tsup **bỏ `--dts`** build xong trong **80ms**. Hệ quả là build của **cả 6 package được publish** đứt — trong đó type của `@lumibase/sdk` là surface đã freeze cho 1.0 (`v1-release-criteria.md` §2/§6), nên đây là chặn cứng chứ không phải bất tiện. Những thứ **vẫn chạy** trên TS 7: `next build` (landing), `vite build` + prerender (docs), build Worker qua wrangler (cms), `drizzle-kit` | medium | `open` (chặn bởi upstream) | Đừng merge #417 ở dạng hiện tại. Ba đường, không loại trừ nhau: (a) **chờ TS 7.1**, bản mà release notes hứa có API programmatic ổn định — rẻ nhất, và là điều kiện để `typescript-eslint` + `rollup-plugin-dts` ra bản hỗ trợ; (b) **cài song song** theo đúng hướng dẫn của TS 7 (`typescript: "npm:@typescript/typescript6@^6.0.2"` cho tsup/eslint, `@typescript/native` cho `tsc` trong CI) — mua được cú tăng tốc typecheck ~15x ngay, nhưng thêm một trục drift phải canh, và DoD §2e nói rõ hai major của cùng một tool trong một workspace là drift đắt; (c) rời DTS sang thứ không cần TS API. Việc nên làm **trước** khi thử lại: lên TS 6 và dọn sạch mọi cảnh báo deprecation ở đó (repo đang ở 5.9.3, tức nhảy 5→7 biến mọi deprecation của 6 thành lỗi cứng một lượt). Đã dọn trước một phần: hai `baseUrl` (TS5102) đã bỏ, `tsconfig.base.json` vốn đã dùng `moduleResolution: Bundler` + `esModuleInterop: true` + `strict: true` + `target ES2022` nên không đụng nhóm option bị xoá |
| B66 | 2026-09-15 · A04 onboarding docs (#333) | bug (guard không fire) | `apps/docs/docs.config.json` ↔ `apps/docs/src/plugins/vite-plugin-docs-loader.ts` | **Một entry sidebar trỏ tới slug không tồn tại bị bỏ đi im lặng** — không lỗi, không cảnh báo, chỉ mất mục trên nav. `deriveSlug` cố ý thu `<dir>/index.md` về slug thư mục cha (`cli/index.md` → `cli`, để Cloudflare Pages không 308 vào một file tĩnh không có), nên mọi entry viết dạng `<dir>/index` **không bao giờ khớp**. Đo được trên build thật: `"tutorials/index"` có trên `main` từ lâu và category Tutorials render **rỗng** phần index (chỉ còn `nextjs-quickstart`); hai entry tôi thêm trong PR này (`cli/index`, `sdk/index`) rụng đúng cùng cách trước khi sửa. Cùng class "khai báo đọc thì đúng nhưng không tạo ra kết quả nó hứa" với B31/B36/B53: config parse hợp lệ, build exit 0, sitemap vẫn có URL của trang — chỉ có nav là thiếu, mà nav lại là thứ duy nhất người đọc dùng để tìm trang. **Rà tiếp thì lỗ hổng rộng hơn 3 entry:** trên `main` có **6** entry sidebar không resolve (`architecture/runtime-abstraction`, `architecture/multi-tenancy`, `architecture/permission-engine`, `api/openapi`, `guides/backup-recovery`, `guides/tooling-recommendations` — hai cái cuối làm cả category "Guides" render rỗng), và **navbar còn tệ hơn im lặng** vì `to` là href chứ không phải slug nên nó không được lọc: `/docs/api` là **404 thật** trên `docs.lumibase.dev` (đo 2026-09-15T04:01Z: `404`), còn `/docs/tutorials/index` chỉ sống nhờ Cloudflare `308` sang `/docs/tutorials/` | medium | `fixed` | Đã sửa trong PR này: slug `tutorials/index` → `tutorials`, `cli/index` → `cli`, `sdk/index` → `sdk`; navbar `/docs/tutorials/index` → `/docs/tutorials` và `/docs/api` → `/docs/api/hono-api-spec`; 6 entry trỏ vào trang không tồn tại được **bỏ khỏi nav** (category "Guides" rỗng bị xoá) — trang thiếu ghi riêng ở B68. Verify bằng build thật + parse lại sidebar đã render ở **cả hai** locale (item là `<button>` chứ không phải `<a>`, nên grep `href` không thấy gì — phải đọc `span.truncate`). **Hàng rào đã dựng:** `scripts/check-docs-nav.mjs` + `pnpm docs:nav:check` trong `check:all` (do đó cả CI và pre-commit) — fail khi một `items[]` không khớp slug nào, khi một category render rỗng, hoặc khi navbar/footer trỏ `/docs/<slug>` không tồn tại. Test `scripts/__tests__/check-docs-nav.test.mjs` (10 ca `node --test`) dựng lại đúng ba sự cố lịch sử; đã kiểm âm trên repo thật: dựng lại `tutorials/index` + `/docs/api` → exit 1 đúng hai dòng, bỏ ra → exit 0 |
| B67 | 2026-09-15 · A04 onboarding docs (#333) | bug (link chết) | `docs/vi/README.md` dòng 34 | Link `[en/llms.txt](./llms.txt)` trong bản VI resolve về `docs/vi/llms.txt` — **file không tồn tại** (`docs/llms.txt` và `docs/en/llms.txt` có, VI không). Text link nói "en/llms.txt" nhưng đường dẫn tương đối lại tính từ `docs/vi/`, nên đúng ý định thì phải là `../en/llms.txt`. Có sẵn trên `origin/main` (verify bằng `git show origin/main:docs/vi/README.md`), không do PR này. Đáng chú ý ở chỗ **`check-parity` không thể bắt**: nó so *đường dẫn* của link giữa hai locale (đúng theo bản vá B54), và ở đây hai bên đều là `./llms.txt` — khớp hoàn hảo, trong khi một bên sống và một bên chết. Bản EN đúng vì `./llms.txt` = `docs/en/llms.txt` | low | `open` | Sửa href bản VI thành `../en/llms.txt` (hoặc thêm `docs/vi/llms.txt` nếu muốn có index VI — DoD §4a xếp llms.txt vào ngoại lệ "tooling/index cố ý không có pair", nên sửa link là đủ). Không gộp vào PR này để không trộn một sửa link ngoài scope vào một PR onboarding; ghi lại đây theo DoD §7. Cơ giới hoá đáng làm cùng B44/B66: một gate kiểm link Markdown nội bộ trên toàn `docs/` + `README.md` — hiện **không có** gate nào làm việc đó |
| B68 | 2026-09-15 · A04 onboarding docs (#333) | task (docs thiếu) | `docs/{en,vi}/architecture/*` · `docs/{en,vi}/api/openapi.md` · `docs/{en,vi}/guides/*` | Sáu trang được **khai trong sidebar nhưng chưa từng được viết**: `architecture/runtime-abstraction`, `architecture/multi-tenancy`, `architecture/permission-engine`, `api/openapi`, `guides/backup-recovery`, `guides/tooling-recommendations`. Đáng chú ý là ba trang `architecture/*` đầu tiên chính là các chủ đề mà `.kiro/steering/tech.md` và `structure.md` trỏ tới như tài liệu nền (runtime abstraction, multi-tenancy, permission engine) — tức chỗ trống nằm đúng phần kiến trúc mà người mới cần nhất sau onboarding. Chúng nằm im được lâu vì nav lọc entry không resolve trong im lặng (B66) nên không ai thấy thiếu. Phát hiện khi cơ giới hoá B66; ngoài scope A04 (viết mới sáu trang kiến trúc/guide là việc của người sở hữu từng vùng, không phải một PR onboarding) | low | `open` | Hai lựa chọn cho từng trang, đừng để lửng: viết trang (kèm cặp EN/VI + stamp theo DoD §4a) rồi thêm lại entry vào `docs.config.json`, hoặc chấp nhận không có và giữ nguyên việc đã bỏ khỏi nav. `pnpm docs:nav:check` nay chặn việc khai trước-viết-sau, nên thêm entry mà chưa có trang sẽ đỏ CI thay vì mất tích |
| B69 | 2026-09-15 · đo delta nhánh A04 (#333) | bug (lệnh sai trong docs) | `docs/{en,vi}/deployment/local-development.md` · `contributing/index.md` · `agent-setup/{claude-code,cursor,windsurf}.md` · `tutorials/nextjs-quickstart.md` · `AGENTS.md` | **`pnpm -F @lumibase/database db:migrate` không phải script tồn tại** — `packages/database` khai `migrate` / `generate` / `studio`, tiền tố `db:` chỉ có ở script **root**; còn `db:reset` thì không tồn tại ở đâu cả. Đo trực tiếp: `pnpm -F @lumibase/database run db:migrate` → `None of the selected packages has a "db:migrate" script`. Hiện diện **22 lần trong 12 file `docs/` + `AGENTS.md`**, trong đó chỗ đắt nhất là **bước 5 của Local Development** và **bước 1 của Next.js quickstart** — tức người mới gõ theo hướng dẫn là chết ngay lần cài đầu, trước khi kịp thấy bất kỳ thứ gì chạy. Nghịch lý: `CLAUDE.md:96` **đã ghi sẵn** cảnh báo "`-F @lumibase/database db:migrate` is not a valid script" — luật nội bộ biết từ trước, tài liệu người mới đọc thì vẫn in ra. Phát hiện khi đo delta nhánh `feat/issue-331-child-2de7a6` (nhánh đó sửa được vài chỗ, không phải tất cả) | medium | `fixed` | Đã đổi cả 22 chỗ sang script root (`pnpm db:migrate` / `db:generate` / `db:studio`) ở 13 file, verify `grep` còn 0 hit và đối chiếu từng tên script với `package.json` tương ứng. **Còn mở là hàng rào:** không có gate nào kiểm lệnh trong code fence của docs có thật hay không — cùng họ với B44 (link nội bộ) và B66 (nav). Cơ giới hoá khả thi: quét `pnpm <script>` / `pnpm -F <pkg> <script>` trong `docs/**` + `AGENTS.md` + `CLAUDE.md` rồi so với `scripts` của `package.json` tương ứng, giống cách `check-docs-nav.mjs` so slug với cây file. Bốn hit còn lại trong `.kiro/specs/*/tasks.md` là **ghi chép lịch sử**, cố ý không sửa |
| B70 | 2026-09-19 · #454 governed tool contract | task (governance gap · đã khai) | `packages/mcp-server/src/governed.ts` `UNGOVERNED_MUTATIONS` | 27/74 mutation tool của stdio đi qua governed harness; phần còn lại vẫn gọi REST trực tiếp nên **không** qua autonomy/HITL/kill switch/run audit. Đây là khoảng trống **được khai báo** (mỗi tool có lý do, và tripwire `S16` fail nếu một mutation tool không thuộc bảng nào) chứ không bị che — nhưng vẫn là khoảng trống. Ba nhóm, ba cách đóng khác nhau: (a) **4 tool** `contract-narrower-than-tool` (`create_collection` thừa 16 property, `create_policy` 5, `create_role` 1 `systemKey`, `cdc_subscription_replay` có `cursor`) — cần quyết định mở rộng canonical schema hay thu hẹp schema quảng bá, và thu hẹp là **breaking** cho caller đang gửi các arg đó; (b) **14 tool** `no-canonical-contract` — chỉ cần thêm schema vào `AgentToolSchemas`, việc cơ học; (c) phần còn lại `no-skill` — cần skill mới ở `CORE_SKILLS`, tức feature thật (release, preset, share, materialization, editorial review, marketplace install…) | medium | `open` | Đóng theo nhóm, nhóm (b) trước vì rẻ nhất và không breaking. Nhóm (a) cần quyết định của owner về việc có chấp nhận breaking cho `create_collection`. Nhóm (c) đủ lớn để thành spec riêng. Số đo hiện tại chốt trong `S16` nên mọi lần đóng đều phải cập nhật con số đó — cố ý, để thay đổi hiện ra trong review **Cập nhật 2026-09-26 (nhóm b đóng):** đo lại trên `main` `d64588d1`, nhóm (b) có **10** tool chứ không phải 14. Cả 10 skill nay có schema `.strict()` trong `AgentToolSchemas`; **6** được route (`create_relation`, `create_intent`, `update_translation`, `create_webhook`, `update_webhook`, `delete_cdc_subscription` với rename `id → subscriptionId`), `create_cdc_subscription` sang nhóm (a) (`payload_mode` không được handler chuyển tiếp), và `create_flow`/`install_extension`/`update_extension` sang lý do mới `skill-weaker-than-rest` (B87). Số đo `S16` hiện tại: **33/88** governed; còn 5 `contract-narrower-than-tool` + 3 `skill-weaker-than-rest` + 47 `no-skill`; `no-canonical-contract` rỗng. Vẫn `open` vì nhóm (a) và (c) |
| B71 | 2026-09-19 · #472 capability resolution | task (gap thiết kế) | `packages/database/src/schema/ai.ts` (`aiApprovals`, `agentApprovals`) · `apps/cms/src/services/ai-harness.ts` `executeApproved` | Approval được phê duyệt thực thi với capability của **người quyết định**, không phải người yêu cầu, và capability của người yêu cầu **không** được re-resolve lúc quyết định. `EffectiveCapabilityService` tồn tại chính để làm việc đó (`AuthenticatedPrincipalRef` cố ý không mang capability), và queue payload đã dùng nó — nhưng hai bảng approval **không có cột** lưu principal ref nên đường resume không có gì để resolve lại. Hệ quả: một hành động do user A park lại, sau khi A bị hạ quyền hoặc bị vô hiệu hoá, vẫn thực thi được khi admin B phê duyệt. Có thể coi đó là đúng (B nhận trách nhiệm dưới quyền của B) — nhưng hiện nó là **hệ quả của schema thiếu cột**, không phải một quyết định được ghi ra | medium | `open` | Thêm cột `requested_by_principal` (jsonb) vào `lumibase_agent_approvals`, migration additive + idempotent, rồi cho `executeApproved` resolve lại principal của người yêu cầu và giao với capability của người quyết định (lấy min, không lấy max). Nếu owner chốt rằng dùng quyền người quyết định là đúng thì vẫn nên ghi quyết định đó vào `docs/{en,vi}/mcp/governed-tool-contract.md` §4 thay cho phần 'giới hạn đã biết' hiện tại |
| B72 | 2026-09-19 · review #481 (fix P1 RBAC chat async) | bug (guard không fire) | `apps/cms/src/__tests__/item-service-rbac-context.test.ts` | Tripwire canh `new ItemService(...)` ngoài factory **đã bỏ lọt đúng file bypass RBAC**. Pattern của nó là `/\bnew\s+ItemService\s*\(/`, còn `services/ai-chat-run-worker.ts` dựng service bằng `new (await import('./item-service')).ItemService({…})` — dynamic import nằm GIỮA `new` và tên class nên chuỗi `new ItemService(` không bao giờ xuất hiện. Kết quả: nửa async của `POST /ai/chat` chạy không có `permissionCtx` (không row/field RBAC) trong khi nửa sync dùng `itemServiceForRequest(c)`, và guard dựng riêng để bắt lớp lỗi đó vẫn xanh suốt. Cùng lớp 'guard có mà không fire' với B31/B36/B53/B56/B59 — nhưng đây là ca đắt nhất vì file né guard chính là file có lỗ hổng | medium | `fixed` | Thêm pattern thứ hai cho dạng `new (…).ItemService(`; inner class phải chịu được dấu `)` lồng (`[^;{}]{0,200}?` — dùng `[^)]*` thì pattern **im lặng không khớp** vì `await import('…')` đã có một `)`), và từ chối `;{}` để không lang thang qua câu lệnh khác. Kèm test kiểm âm chính pattern đó + kiểm âm trên file thật (dựng lại dạng cũ → guard đỏ đúng `services/ai-chat-run-worker.ts`). Thêm posture thứ ba `itemServiceForPrincipal(deps, permissionCtx)` vào factory để worker có đường hợp lệ thay vì phải tự `new` |
| B73 | 2026-09-19 · #455 reconciler repair loop | bug (class "producer không có consumer") | `apps/cms/src/services/agent-run-service.ts:330-356` · `apps/cms/src/routes/agent.ts:619-626` | `POST /api/v1/agent/runs/:id/retry` INSERT một run mới với `status: 'running'` rồi **trả về ngay** — không enqueue vào `agent-runs`, không gọi harness. Nghĩa là "retry" tạo một dòng trông như đang chạy mà **không ai thực thi**, và vì status là `running` (không phải `queued`) thì cả dispatcher lẫn consumer cũng không bắt được nó; nó nằm `running` vĩnh viễn, còn tệ hơn `queued` vì UI đọc nó thành "đang chạy". Đo được: queue spy nhận **0** job sau khi gọi `retryRun`, run mới ở `running`. Đúng cùng lớp lỗi mà #455 vừa vá cho reconciler goal (unit đúng, không ai gọi) — chỉ khác là lần này người bấm nút là con người nên họ sẽ chờ một kết quả không bao giờ tới | medium | `open` | Fix: `retryRun` tạo run `queued` rồi enqueue lại payload của run gốc (payload phải lưu được — hiện `agent_runs` không giữ `skillName`/`arguments`, nên cần cột hoặc đọc lại từ `agent_tool_calls`), hoặc thu hẹp endpoint thành "chỉ tạo bản ghi retry" và nói rõ điều đó trong API spec + UI. Không sửa trong #455 vì đường retry của người dùng khác đường dispatch của reconciler và cần quyết định về việc lưu payload |
| B74 | 2026-09-19 · #455 reconciler repair loop | bug | `apps/cms/src/services/drift-service.ts:462-472` (`applyBatch`, nhánh `toReopen`) | Khi một drift đã `resolved` tái xuất hiện, scan set `status='open', goalId=null` — tức **bỏ rơi goal cũ** mà không đóng nó. Goal đó vẫn ở `open`/`in_progress` với `driftFingerprint` trỏ tới drift vừa reset, nên sau #455 dispatcher sẽ thấy nó và đẩy tiếp một chuỗi repair thứ hai song song với goal mới mà reconcile vừa tạo cho cùng fingerprint ⇒ hai goal, hai draft, hai approval cho một vi phạm. Hiện chưa nổ vì khoá nhánh draft là tiền định nên draft thứ hai bị `VERSION_EXISTS` (goal thứ hai block `DRAFT_MISSING`), nhưng đó là may mắn của một lớp bảo vệ khác, không phải thiết kế | medium | `open` | Fix: khi reopen, đóng goal cũ tường minh (`status='cancelled'` + `metadata.reason='drift_reopened'`) trong cùng câu lệnh đổi `goalId=null`; hoặc không xoá `goalId` và để dispatcher tiếp tục goal đang có. Cần một quyết định về ngữ nghĩa "drift tái xuất hiện là việc mới hay việc cũ chưa xong" |
| B75 | 2026-09-19 · #455 reconciler repair loop | task (thiếu ràng buộc) | `packages/database/src/schema/ai.ts:166-172` (`agentGoals`) ↔ `apps/cms/src/services/reconciler-service.ts:176-212` | `content_drifts` có `uniqueIndex(siteId, fingerprint)` nhưng `agent_goals` **không** có unique nào trên `(siteId, driftFingerprint)`, và INSERT goal + UPDATE drift trong `reconcileIntent` **không nằm trong một transaction**. Hai reconcile chạy đồng thời cho cùng intent (cron + endpoint thủ công, hoặc hai process không có leader lock) đều đọc drift ở `open` rồi cùng tạo goal ⇒ hai goal cho một fingerprint, và DB không chặn. Tính đúng đắn hiện dựa vào việc không có ai chạy song song, chứ không dựa vào ràng buộc | medium | `open` | Fix hai phần: (a) migration additive + idempotent thêm partial unique index `(site_id, drift_fingerprint) WHERE drift_fingerprint IS NOT NULL AND origin = 'reconciler'` — lưu ý theo DoD §2b phải ghi rõ điều kiện FAIL nếu dữ liệu đang có trùng, kèm bước de-dup; (b) gói INSERT goal + UPDATE drift vào `db.transaction`. Ngoài scope #455 (issue đó nối dispatch, không đổi schema) |
| B76 | 2026-09-19 · #455 reconciler repair loop | bug (success-shaped placeholder) | `apps/cms/src/services/ai-harness.ts` — handler `createVersion`/`updateVersion`/`deleteVersion`/`promoteVersion` | Bốn handler version trả `{ created: true }` / `{ updated: true }` / `{ deleted: true }` / `{ promoted: true }` khi `contentVersionService()` là null (thiếu `itemService`/`db`/`siteId`). Đó là hành vi "offline-safe" cho registry dùng chung, nhưng trên một đường chạy thật thiếu service thì run sẽ **succeeded** trong khi không có gì được ghi — đúng thứ mà acceptance của #455 gọi là success-shaped placeholder. `repairTranslation` cố ý throw `CONTENT_VERSIONS_NOT_CONFIGURED` thay vì bắt chước mẫu này, nên hiện dispatcher phát hiện được ca đó (`DRAFT_MISSING` một lượt sau); nhưng nếu ai đó dispatch `promoteVersion` qua một harness thiếu service thì goal sẽ đi tới `verify` rồi block `VERIFY_FAILED` — thông báo sai nguyên nhân | low | `open` | Fix: dùng `requireService` cho nhóm này như các skill write khác, và cho registry offline một cờ tường minh (`registryOnly: true`) thay vì để handler tự đoán. Việc này đụng cả họ skill versioning nên không gộp vào #455 |
| B77 | 2026-09-19 · #455 reconciler repair loop | task (khai báo chết) | `packages/database/src/schema/content-os.ts` (`contentIntents.schedule`) · `content_drifts.status` | Hai khai báo không có ai dùng. (a) `contentIntents.schedule` là cột **NOT NULL** chứa biểu thức cron (`'0 * * * *'`), nhưng grep toàn repo không có chỗ nào đọc nó để lên lịch — chu kỳ reconcile chạy qua cron tick chung hoặc flow op `drift-scan`, không đọc cột này. Người vận hành sẽ đặt `schedule` rồi chờ một hành vi không tồn tại. (b) `content_drifts.status` có giá trị `'stale'` được `diffDrifts` xử lý (coi như reopen được) nhưng **không có đường ghi nào** set `'stale'` ở bất cứ đâu | low | `open` | (a) Hoặc đọc `schedule` thật trong tick dispatch/scan (mỗi intent theo cron riêng), hoặc bỏ cột khỏi surface và ghi rõ rằng lịch là toàn cục — một cột NOT NULL không ai đọc là lời hứa sai. (b) Xoá `'stale'` khỏi nhánh xử lý hoặc cấp cho nó một đường ghi (ví dụ drift của item đã xoá mềm) |
| B78 | 2026-09-20 · review #481 nhóm 2 (R3/R4/R5) | task (process) | quy trình verify của chính tôi | **Một thay đổi hợp đồng đã được push với 13 test đỏ ở 5 file mà tôi không biết.** Commit `e3298433` (R2 — approval re-resolve) đổi hợp đồng của `executeApproved`: nó nay cần provenance và đọc thêm `agent_approvals`. Tôi verify bằng các suite **nhắm mục tiêu** (`g2-approval-requester` 11/11, G1 59/59, typecheck 18/18) rồi push. Hồi quy chỉ lộ ra ở lượt sau, khi pre-commit hook chạy full `pnpm test`: `ai-integration`, `mcp-control-plane-backstop`, `agent-harness-layer.property`, `ai-harness-approval.property`, `ai-harness-failure-pending.property` — tất cả đỏ vì mock DB nông của chúng không có nhánh đọc mới. Không suite nào trong số đó nằm trong tập tôi chọn, và không có gì trong quy trình của tôi buộc chọn đúng. Đáng chú ý: typecheck **không thể** bắt được lớp này (mock `as unknown as Database`), và bản thân đây là biến thể của chính bài học "đo, đừng suy luận" — tôi suy rằng tập nhắm mục tiêu là đủ | medium | `fixed` (lần này) · quy trình `open` | Đã sửa trong `ab32f058`: thứ tự kiểm được sửa để lý do denial đúng (approval hết hạn/đã xử lý báo đúng nó, không báo "thiếu provenance"), hai lần đọc gộp thành một snapshot, và fixture + stub được đặt ở `apps/cms/src/test-utils/approval-provenance.ts` kèm lý do. **Phần quy trình còn mở**: quy tắc nên là "đổi hợp đồng của một service dùng chung ⇒ chạy full suite của package đó trước khi push, không chỉ suite mới viết". Cơ giới hoá được: `pnpm -F @lumibase/cms test` trước mỗi push đụng `services/`, hoặc pre-push hook (hiện chỉ có pre-commit). Lưu ý B32 vẫn đúng — full suite ở concurrency mặc định là flaky, nên dùng `TURBO_CONCURRENCY=2` như hai commit vừa rồi |
| B79 | 2026-09-20 · review #481 nhóm 2 (R3) | bug (guard không fire) | `apps/cms/src/services/__tests__/g3-dispatch-reliability.db.integration.test.ts` — ca "two concurrent dispatch passes" | Ca test tôi viết để chứng minh dispatch lease **không chứng minh được nó**: gỡ hẳn `claimGoal` thì ca đó vẫn **xanh**, vì pass thứ hai đọc run `queued` của pass thứ nhất rồi skip `RUN_ACTIVE`. Tức tôi đã định viết bằng chứng cho lease và thực tế viết một bằng chứng cho lớp khác. Phát hiện đúng lúc kiểm âm — nếu bỏ bước kiểm âm thì một bản viết lại `claimGoalLease` thành read-then-write sẽ đi qua CI. Thêm một nửa nữa: `Promise.all` hai `claimGoalLease` **không** phân biệt được (driver tuần tự hoá chúng), nên assert trên đó cũng sẽ là một guard không thể fire — đúng lớp B31/B36/B53/B56/B59 | medium | `fixed` | Đóng trong `ab32f058`: `claimGoalLease`/`releaseGoalLease` tách thành hàm module-level đo được, ca lease chỉ assert phần deterministic (lease sống thì từ chối claim tiếp, không phải holder thì release là no-op), và **bảng đối chiếu hai cách phá** (gỡ claim vs read-then-write — mỗi cách bị một ca khác nhau bắt) được ghi thẳng trong test file để không ai đọc ca end-state thành bằng chứng cho lease. Bài học chung: kiểm âm phải thử **nhiều cách phá**, không chỉ một; một cách phá duy nhất cho cảm giác an toàn sai |
| B80 | 2026-09-20 · review #481 vòng 2 (F1) | task (gap thiết kế) | `apps/cms/src/services/permission-dsl.ts` ↔ `ai-harness.ts` `scopeToRequester` | **Không có phép giao hai `MagicContext`.** F1 đóng bằng cách thực thi approval dưới context của **người yêu cầu**; phần row/field của **người quyết định** cố ý không áp, vì policy DSL không có toán tử giao (mỗi context là tập policy + magic var + header/ip, và "giao" của hai tập policy không phải một policy). Hiện vô hại trên thực tế: người duyệt nắm `approvals:decide` hoặc admin, tức không có mask nào để áp. Nhưng nếu một site cấp `approvals:decide` cho editor có mask hẹp hơn người yêu cầu, thì approval **không** bị thu hẹp theo mask của editor — nghĩa là editor duyệt được một phép ghi mà chính họ không làm được trực tiếp. Không phải lỗ hổng leo quyền cho người yêu cầu (họ vẫn bị giới hạn ở quyền của họ), nhưng là một bất đối xứng chưa ai quyết định | medium | `open` | Ba đường: (a) chấp nhận và giữ nguyên tài liệu hiện tại (đã ghi ở `governed-tool-contract.md` §4) — rẻ nhất, đúng khi người duyệt luôn là admin; (b) fail-closed: từ chối approve nếu người quyết định không phải admin/`controlPlaneAdmin` VÀ skill có ghi item, kèm mã lỗi riêng — siết, có thể phá UX của site đã cấp `approvals:decide` cho non-admin; (c) làm phép giao thật: `intersectPermissionContexts(a, b)` trả context mà mọi kiểm đều phải qua cả hai bên (thực tế là chạy hai `PermissionService` và lấy min của field mask + AND của row rule). (c) là việc đúng nhưng đủ lớn để thành spec riêng |
| B81 | 2026-09-20 · review #481 vòng 2 (F3/F5) | bug (test không đo được) | `apps/cms/src/services/__tests__/agent-run-state-machine.test.ts` (`fakeRunsDb`) · các fake db tương tự | **Fake db áp mọi `set` bất kể `where`**, nên nó báo thành công cho MỌI conditional UPDATE. Hệ quả đã đo: ca `markRunning refuses cancelled and terminal runs` "pass" trong khi hàm thật chỉ từ chối được nhờ đã **đọc** row trước — đúng cái read-then-write khiến hai delivery cùng chạy một run (F3). Nghĩa là một họ test đang xác nhận hành vi loại trừ mà chúng về mặt cơ học không thể quan sát. Cùng lớp "guard có mà không fire" với B31/B36/B53/B56/B59/B79, nhưng ở tầng test double. Đã gặp lại ngay trong lượt này: ca `cancelled while queued` phải chuyển sang DB thật, và `g1-approval-execution` dùng `new Proxy({}, { get: () => async () => undefined })` làm ItemService nên mọi method trả Promise — rebind context của F1 gán Promise làm service và nổ `setProvenance is not a function` | medium | `open` | Hai hướng, không loại trừ nhau: (a) cho các fake db đánh giá `where` tối thiểu (so sánh eq trên cột đã set) để chúng có thể trả 0 row — hoặc rõ hơn là **không** cho fake db trả `returning()` thành công khi không khớp; (b) quy tắc: mọi hành vi phụ thuộc conditional UPDATE / row lock chỉ được nghiệm thu trên `*.db.integration.test.ts`, và fake db chỉ dùng cho ca "đường đi". Đã áp (b) thủ công cho 3 ca trong lượt này kèm chú thích; nên cơ giới hoá bằng tripwire scan (ví dụ: file dùng fake db không được assert `toBe(false)` trên các hàm claim/CAS) |
| B82 | 2026-09-22 · review #481 vòng 3 (R3.1) | task (gap vận hành) | `apps/cms/src/services/agent-run-service.ts` `quarantineStaleRuns` · Mission Control | Run bị bỏ rơi nay được **quarantine** (`failed` + `stopReason: 'stale_unverified'`) thay vì tự chạy lại — đúng, vì thời gian không chứng minh được lần chạy trước chưa ghi gì. Nhưng phần **sau** khi quarantine thì chưa có gì: không có nút "retry an toàn" cho người vận hành, không có màn hình liệt kê riêng các run `stale_unverified`, và cũng không có công cụ nào giúp trả lời câu hỏi thật sự khó — "lần chạy đó đã ghi tới đâu rồi". Hiện người vận hành phải tự đọc `agent_tool_calls` rồi quyết định bằng tay. Với deployment ít run thì chấp nhận được; với deployment nhiều thì đây là nợ | medium | `open` | Ba mảnh, không loại trừ nhau: (a) lọc theo `stopReason` trong Mission Control để `stale_unverified` không lẫn với failure thường; (b) một API/route "retry run này" tạo run MỚI (không hồi sinh run cũ) và ghi audit rằng người nào quyết định là an toàn; (c) checkpoint/idempotency key cho từng tool call, để câu hỏi "đã ghi tới đâu" trả lời được bằng dữ liệu thay vì suy đoán. (c) là việc lớn nhất và cũng là thứ duy nhất khiến automatic recovery trở nên đúng đắn — nếu làm thì nên có spec riêng. **Cập nhật 2026-09-26 (nhánh `fix/agent-run-retry-enqueue`)**: phần **(b) phía API đã có** — `POST /api/v1/agent/runs/:id/retry` tạo run MỚI `queued`, enqueue thật, tối đa một lần thực thi mỗi request, ghi `agent_run.retried` vào `activity` kèm principal người quyết định (Setup Impact #137). Còn mở: (a), nút retry trong Studio, và (c) |
| B83 | 2026-09-22 · review #481 vòng 3 (R3.2) | task (test tooling) | `/tmp/lumibase-review-6086dd30.xA3Ar2/probes.mts` (probe của reviewer) ↔ `goal-dispatch-service` | Probe F4 của reviewer **treo vô hạn** trên code đã sửa thay vì báo kết quả. Không phải bug của bản sửa: probe hook `db.insert(agent_runs)` ở **cấp db** và tạm dừng ở đó, nhưng INSERT nay nằm trên **transaction handle** bên trong `db.transaction`, nên hook không bao giờ bắn và `await paused.promise` không bao giờ resolve. Nói cách khác, cửa sổ mà probe chèn vào đã bị đóng — nhưng cách nó biểu hiện (treo) lại giống hệt một deadlock thật, nên rất dễ đọc sai. Đã viết ca thay thế (`R3.2: a suspended dispatcher cannot write after another one commits`) hook ở tầng `transaction` và kết thúc được nhờ `lock_timeout` | low | `open` | Nếu reviewer chạy lại bộ probe: hook `transaction` chứ không `insert`, và đặt timeout cho probe để "treo" hiện ra thành lỗi có thông điệp thay vì một tiến trình đứng im. Bài học chung cho probe tương lai: pause ở một điểm **quan sát được từ ngoài** thì mong manh theo cấu trúc bên trong; tốt hơn là pause theo **trạng thái** (đọc DB thấy điều kiện X) như ca `outlived its lease` đã đổi sang |
| B84 | 2026-09-19 · #454 governed tool contract | bug (drift) | `packages/mcp-server/src/index.ts:12` | `serverInfo.version` hardcode `'0.9.0'` trong khi `package.json` của chính package là `1.0.0-rc.1`. Đây là giá trị **client nhìn thấy** — MCP client đọc `serverInfo` lúc `initialize`, nên một client muốn phân biệt phiên bản server sẽ đọc sai. Không có gate nào canh: `version:check` (`sync-version.mjs`) so root version với version trong `package.json` của các package, nó không đọc chuỗi hardcode trong source. Cùng lớp với B45 (README/JSON-LD trôi lệch so với manifest) — khai báo đọc thì hợp lý nhưng không mô tả thực tế. KHÔNG sửa trong PR này vì đổi `serverInfo.version` là đổi một giá trị client-visible trong một PR về governance | low | `open` | Đọc version từ `package.json` thay vì hardcode (tsup có `define`, hoặc import JSON with attributes), rồi cân nhắc mở rộng `sync-version.mjs` để bắt cả chuỗi version hardcode trong `src/`. Tương tự `apps/cms/src/services/mcp-service.ts` `MCP_SERVER_INFO` — cần kiểm cùng lúc |
| B92 | 2026-09-26 · fix/agent-run-retry-enqueue | bug (gap quản trị) | `apps/cms/src/routes/agent.ts` `POST /goals` (`execution: 'async'`) ↔ `agent-run-worker.ts` | **Budget của goal async không được enforce.** Route lưu `parsed.data.budget` vào `agent_runs.budget` qua `ensureRun`, nhưng `AgentRunJobPayload` nó enqueue **không có** `budget`; worker chỉ truyền `budget` vào envelope của harness khi payload có (`...(payload.budget ? { budget } : {})`), nên `maxToolCalls` / `maxWritesPerMinute` người gọi khai cho một goal async được lưu mà không nơi nào đọc. Đường sync (harness nhận `envelope.budget` trực tiếp) và đường reconciler (dispatch gửi `budget` của intent) không bị. Retry (PR này) gửi `run.budget` khi khác rỗng, nên một lần **thử lại** lại enforce budget mà lần chạy gốc bỏ qua — nhất quán theo hướng chặt hơn, nhưng lộ ra lỗ ở đường gốc | medium | `open` | Sửa: thêm `budget: parsed.data.budget` vào payload ở `routes/agent.ts` (cạnh `principal`) + test DB chứng minh `maxToolCalls` chặn được run async |
| B93 | 2026-09-26 · fix/agent-run-retry-enqueue | bug (gap phục hồi) | `apps/cms/src/routes/agent.ts` `POST /goals` · `goal-dispatch-service.ts` (R4) · `agent-run-service.ts` `quarantineStaleRuns` | **Run `queued` mất job của goal không-phải-reconciler kẹt vĩnh viễn.** `POST /goals` async gọi `queue!.enqueue(...)` không có try/catch: enqueue lỗi → 500 và run nằm `queued` mãi. Phục hồi R4 (`STALE_QUEUED_RUN_MS`) chỉ chạy cho goal `origin = 'reconciler'`, còn stale sweep chỉ xử lý `running`, nên không có gì dọn run này. Từ PR này hệ quả rõ hơn: một run `queued` là "in flight", nên mọi retry trên goal đó bị từ chối `RUN_ACTIVE` cho tới khi có người `POST /runs/:id/cancel` bằng tay | low | `open` | Tối thiểu: bọc enqueue ở `POST /goals` như dispatch/retry (settle run `failed` + `stopReason: 'enqueue_failed'`). Đầy đủ: mở rộng sweep cho `queued` quá hạn ở mọi origin — an toàn vì run `queued` chưa thực thi gì (#481 R3.1) |
| B94 | 2026-09-26 · fix/agent-run-retry-enqueue | bug (docs drift) | `docs/en/features/agent-harness-layer.md` mục "Run lifecycle and async execution" | Câu "Capabilities are captured from the enqueuing session and never widened" **sai từ #472**: job mới mang `principal` và worker **resolve lại** quyền lúc nhận job (`resolveWorkerCapabilities`), snapshot `capabilities` chỉ còn là trường tương thích `@deprecated`. Bản VI là tài liệu khác (B60) nên không có câu tương ứng | low | `open` | Sửa câu EN cho khớp #472 rồi stamp lại cặp; gộp vào lượt B60 nếu làm cùng lúc |
| B85 | 2026-09-26 · #454 follow-up (canonical contracts) | bug (governance) | `apps/cms/src/services/intent-service.ts` `update` ↔ `routes/intents.ts` `PATCH /:id` | **PATCH một intent âm thầm reset `autonomyCap` về 2 và `budget` về mặc định.** `update()` parse bằng `intentInputSchema.partial()`, và Zod 4 vẫn áp `.default(2)` của `autonomyCap` cùng `.prefault({})` của `budget` cho field vắng mặt, rồi code ghi chúng vì chúng `!== undefined`. Đã đo: `intentInputSchema.partial().parse({ name: 'renamed' })` trả về `autonomyCap: 2` và budget mặc định (probe vitest tạm, đã xoá). Hệ quả [Inference từ đọc code]: đổi tên một intent đang bị hạn L0/L1 sẽ nâng trần autonomy lên L2 — nới governance mà không ai yêu cầu. Đi qua REST và qua tool stdio `update_intent` (`no-skill` ⇒ REST) | high | `open` | Sửa: dựng schema PATCH không có default (hoặc lọc key có mặt trong body trước khi áp). Kèm test hành vi: PATCH `{name}` giữ nguyên `autonomyCap`/`budget`. Cùng class với B86 |
| B86 | 2026-09-26 · #454 follow-up (canonical contracts) | bug | `routes/webhooks.ts` · `routes/extensions.ts` · `routes/flows.ts` `PATCH /:id` | **`.partial()` trên schema có `.default()` áp lại default khi PATCH (Zod 4).** Đo bằng bản sao schema của từng route trên zod 4.6.1 (không gọi route thật): webhooks `{name}` ⇒ thêm `actions: []`, `collections: []`, `headers: {}`, `status: 'active'`; extensions `{version}` ⇒ thêm `enabled: false`, `manifest: {}`, `capabilities: []` (và `patchActions` thấy key `enabled` nên đòi thêm quyền `enable`); flows `{name}` ⇒ thêm `status: 'draft'`, `triggerOptions: {}`. Các route này `.set()` kết quả parse, nên [Inference] một PATCH một field reset các field kia — với flow là tắt một flow đang `active`. Đường governed của `update_webhook` (PR này) không bị: harness nhận đúng patch người gọi gửi | medium | `open` | Rà mọi `.partial()` trong `apps/cms/src/routes/` (danh sách grep có ở PR); sửa theo class, kèm tripwire test: PATCH một field không đổi field khác |
| B87 | 2026-09-26 · #454 follow-up (canonical contracts) | task (governance gap) | `apps/cms/src/services/ai-harness.ts` skill `createFlow` / `installExtension` / `updateExtension` / `createCdcSubscription` | **Handler skill yếu hơn route REST cùng thao tác.** `createFlow` bỏ qua gate graph cho flow `active`, gate cron của trigger `schedule` và không đặt `nextRunAt` (sweep chỉ chọn `nextRunAt IS NOT NULL` ⇒ flow schedule tạo qua skill không bao giờ chạy). `installExtension` bỏ qua xác minh chữ ký bundle, namespace `lumibase-*` và probe quyền `extensions:install/enable/grant_capability`; `updateExtension` bỏ qua probe theo hành động, việc từ chối bật extension official chưa verify, evict cache sandbox và sync subscription CDC. `createCdcSubscription` không chuyển `payload_mode`, và harness dựng `SubscriptionService` không có `cache`/`audit` (không audit log; cache cờ feed không bị xoá — với tạo mới, [Inference] event có thể không được ghi cho tới hết TTL). Tất cả đã **reachable qua `POST /api/v1/mcp`** (cần admin principal + approval dưới autopilot) | medium | `open` | Chặn việc route `create_flow`/`install_extension`/`update_extension`/`create_cdc_subscription` trên stdio (khai trong `governed.ts`). Sửa: đưa gate dùng chung (graph/cron từ `routes/flows.ts`, `ExtensionVerifierService` + permission probe từ `routes/extensions.ts`) vào service mà cả route lẫn skill gọi, rồi route |
| B88 | 2026-09-26 · #454 follow-up (canonical contracts) | vuln | `ai-harness.ts` skill `updateWebhook` / `updateTranslation` / `updateExtension` | **Handler update đưa patch nguyên vẹn vào `.set()`.** Không có schema canonical nên harness fail-open, và `siteId` (với extension: `isOfficial`, `verifiedAt`; `siteId: null` nghĩa là global) được chuyển thẳng vào câu lệnh update qua `POST /api/v1/mcp`. Cần admin principal và, dưới autopilot, một người duyệt; tác động thực tế trên Postgres (RLS có chặn hay không) chưa đo | medium | `fixed` | PR này: schema `.strict()` cho cả ba ⇒ `VALIDATION` trước `ensureRun`. Test: `g2-agent-tool-schemas.test.ts` (key cột bị từ chối) + `g2-mcp-contract-repro.test.ts` (nhánh governed, 0 row) |
| B89 | 2026-09-26 · #454 follow-up (canonical contracts) | bug (contract) | `@modelcontextprotocol/sdk` `McpServer.validateToolInput` ↔ `packages/mcp-server/src/tools/*` | **SDK stdio strip arg không quảng bá trước khi handler chạy.** Tool đăng ký `inputSchema` dạng raw shape ⇒ SDK dựng `z.object(shape)` (strip) và đưa handler bản đã parse. Đo bằng client MCP thật trong `governed-canonical-routing.test.ts`: `update_webhook` với `bogus_field` tới harness thành `{ id, status }`. Nên trên stdio, arg lạ rụng âm thầm ở tầng SDK cho **mọi** tool (governed lẫn REST) — tầng dispatcher/harness từ chối được nhưng không bao giờ thấy nó | low | `open` | Cân nhắc truyền `z.object(shape).strict()` làm `inputSchema` (SDK nhận ZodObject) để client nhận lỗi `-32602` thay vì im lặng; kiểm tác động lên client đang gửi thừa key |
| B90 | 2026-09-26 · #454 follow-up (canonical contracts) | task (docs drift) | `docs/{en,vi}/ai-skills.md` (mục Standalone MCP server) | Vẫn gọi server stdio là "an ungoverned passthrough", trong khi từ #454 một phần tool (nay 33) đi qua governed harness khi site bật `contentOs.mcp`. Không sửa trong PR này để giữ PR gọn | low | `open` | Sửa cả hai locale + stamp pair; trỏ tới `docs/{en,vi}/mcp/governed-tool-contract.md` §6 |
| B91 | 2026-09-26 · #454 follow-up (canonical contracts) | bug (contract drift) | `packages/ai-skills/src/skills.ts` `createCollection` ↔ `AgentToolSchemas.createCollection` | Mô tả tool Copilot (`llm-provider.ts` đưa `parameters` của `@lumibase/ai-skills` cho LLM) khai `description` cho `createCollection`, nhưng schema chuẩn `.strict()` chỉ nhận `name`/`singleton` ⇒ Copilot gửi `description` sẽ nhận `VALIDATION`. Là mismatch **duy nhất** trong mọi skill có schema chuẩn (đo bằng test mới) | low | `open` | Tripwire đã thêm ở PR này: `g2-agent-tool-schemas.test.ts` ("mô tả tool của Copilot … không hứa property mà schema chuẩn từ chối") với danh sách ngoại lệ chỉ được co lại. Sửa: bỏ `description` khỏi mô tả, hoặc cho handler đọc nó rồi khai vào schema (liên quan nhóm (a) của B70 với `create_collection`) |
| B95 | 2026-09-26 · RC.2 clean-install acceptance | bug (release selection) | `packages/create-lumibase/templates/nextjs/` | Published RC.2 scaffold pins older CMS digest `3f125caa…` and `^1.0.0-rc.1` resolves RC.1 client. Successful default bootstrap therefore does not establish RC.2 acceptance. Exact RC.2 override passes local API checks. | high | `open` | Update release inputs and validate resolved versions from the published tarball. Evidence: `docs/review-agent/2026-09-26-rc2-acceptance.md`. |
| B96 | 2026-09-26 · RC.2 Studio acceptance | bug (navigation) | `apps/studio/src/modules/content/items-list.tsx:332` · `item-detail.tsx:222` | Content navigation loses configured private admin prefix: `/admin-a7f3c1` → `/content/posts`; SPA initially renders but reload returns 404 while prefixed route returns 200. | high | `open` | Audit router base and internal links; test navigation plus reload under a non-root admin path. Evidence: RC.2 acceptance report. |
| B97 | 2026-09-26 · RC.2 Studio acceptance | task (editorial UX) | Studio content list/editor | Tested posts list has no Create action; editor lacks Review/Approve/Publish/Preview actions. API editorial journey passes, but does not satisfy the complete UI acceptance criterion. | medium | `open` | Implement or expose the intended editorial actions with permission-aware states, then test the full UI journey. Observation limited to tested content surfaces. Evidence: RC.2 acceptance report. |
| B98 | 2026-09-26 · RC.2 local load measurement | task (performance validation) | `apps/cms/k6/` · high-load-cache-readiness baseline | Shared ARM64 Docker run with 500k items breaches item list/detail/create and WebSocket-connect p95 thresholds. Public delivery is faster, but mixed workload hits default API limiter (429). Neither shared-host latency nor nginx cache hit ratio establishes Cloudflare readiness or a version regression. | high | `open` | Profile under isolated controlled resources; explicitly configure `LUMIBASE_API_RATE_LIMIT=0` for capacity runs; retain rate-limited runs separately; repeat on Cloudflare staging. Raw measurements and environment in baseline/2026-09-26-rc2-docker. |
