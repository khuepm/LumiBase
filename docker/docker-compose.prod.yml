# Production override.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up
#
# Differences from dev:
# - Uses the production multi-stage Dockerfile (no hot-reload)
# - No source code volume mounts
# - NODE_ENV=production
# - Keeps stateful services on the private Compose network
# - Supports Docker secret files via *_FILE environment variables

services:
  postgres:
    ports: !reset []

  redis:
    ports: !reset []

  minio:
    ports: !reset []

  meilisearch:
    environment:
      MEILI_ENV: production
      MEILI_MASTER_KEY: ${MEILISEARCH_API_KEY:-}
    ports: !reset []

  imgproxy:
    environment:
      AWS_ACCESS_KEY_ID: ${S3_ACCESS_KEY:-}
      AWS_SECRET_ACCESS_KEY: ${S3_SECRET_KEY:-}
    ports: !reset []

  cms:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      NODE_ENV: production
      LUMIBASE_ENV: production
      DATABASE_URL: ${DATABASE_URL:-}
      DATABASE_URL_FILE: ${DATABASE_URL_FILE:-}
      DATABASE_SSL_MODE: ${DATABASE_SSL_MODE:-require}
      REDIS_URL: ${REDIS_URL:-}
      REDIS_URL_FILE: ${REDIS_URL_FILE:-}
      JWT_SECRET: ${JWT_SECRET:-}
      JWT_SECRET_FILE: ${JWT_SECRET_FILE:-}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-}
      ENCRYPTION_KEY_FILE: ${ENCRYPTION_KEY_FILE:-}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-}
    volumes: !reset []

  bull-board:
    ports: !reset []
