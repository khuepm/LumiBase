{
  "name": "@lumibase/extensions",
  "version": "1.0.0-rc.1",
  "private": true,
  "description": "Extension discovery + loading for LumiBase. Scans source folders, validates manifests, and resolves entrypoints (Directus-style). No bundling — works on source.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@lumibase/contracts": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
