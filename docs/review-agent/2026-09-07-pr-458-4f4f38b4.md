## 🔎 Reviewer — Verifier review — changes required

Head `4f4f38b446b5cc00f28feb249166b64c95c37472`.

### P2: deleting one locale bypasses the new gate

The workflow uses `git diff --name-only --diff-filter=d`, explicitly excluding deleted files. A PR deleting only `docs/vi/x.md` leaves EN orphaned but supplies no changed doc to this gate, which exits 0. Rename operations can orphan the old counterpart too. Include deleted paths; check a pair when either locale still exists, and allow intentional deletion of both. Add automated add/edit/delete/rename cases. This finding follows directly from the diff filter and the gate's empty-list early exit; no live workflow mutation was performed.

### DoD disposition

§1 PARTIAL: CI green; claimed seven manual cases omit deletion and no automated regression exists. §2 pending justified n/a registry entry. §2b/§2c/§2d/§2e n/a: CI docs tooling only, no tenant/auth/shell/dependency changes. §3/§4: update the workflow/DoD description that still says parity is entirely advisory; document changed-pair scope and narrow exceptions, with EN/VI if locale docs are touched. §5 n/a for product tutorials. §6 FAIL until deletion/rename regression is covered. §7 record confirmed existing backlog, avoiding a duplicate audit ticket.

The PR description's 'never ran on Actions' is stale: the current head has a successful `Detect language drift (report only)` check. That alone does not prove the negative changed-doc cases ran. Refresh the evidence with exact step/run and head. No merge by verifier.
