# 🔎 Reviewer — fix round on all four PRs (2026-09-09)

Owner instruction: fix #456, #462, #458 and #457 against the 2026-09-07 reviews, and fix anything further found along the way. Records below separate what was verified by running it from what is inherited claim.

## Disposition

| PR | Head before | Head after | Findings addressed | Status |
|---|---|---|---|---|
| #462 | `87dac6d1` | `f38a3aed` | P1 redaction (both halves) | fixed + tripwired |
| #458 | `4f4f38b4` | `5e7db874` | P2 deletion/rename bypass | fixed + tripwired |
| #456 | `abb95944` | `68166926` | §2d recovery surface, §2/§4/§4a | fixed; 3 code P1/P2 already closed at `abb95944` |
| #457 | `7bb1e140` | `f7e2a353` | deployment-integrations items 6.2/9.3/12.3 | pushed; image evidence still owed |

## #462 — both halves of the redaction finding reproduced, then fixed

The review was right on both counts, and both were reproduced with a synthetic secret before any edit:

- Query-string credentials were untouched. `driver failed postgresql://h/db?password=<synthetic>` came back **unchanged**.
- The raw driver error travelled as `{ cause }`, so `util.inspect` printed the password under a parent line that read `target: postgresql://u:***@h/db`.

Fix: cause dropped (diagnosable parts folded in via `describeCause`), query params masked with a `SECRET_PARAM` list shared with `redactConnectionString`. Tests now assert the rendered chain, `String(error)` and `JSON.stringify`, plus `error.cause === undefined`.

Verified by running: `db-harness.test.ts` + `db-integration-guard.wiring.test.ts` → 138 passed, `tsc --noEmit` exit 0. Negative checks: re-attaching `{ cause }` reddens the chain case; dropping the query pass reddens two.

## #458 — the gate never received the path it was meant to judge

`--diff-filter=d` excluded deletions, so a PR deleting one locale handed the gate an empty list and it exited 0. The script's orphan handling was never reached. Classified as B59, same "guard exists, never fires" shape as B31/B36/B53/B56.

Fix: deletions included; each pair classified by survivors — both exist → parity checked, one survives → fail, neither survives → pass (a deliberate retirement must not need an override).

Verified by running: 10 new `node --test` cases, `scripts:test` 20/20 passed. One case builds a real git repo and asserts the deletion is visible only without `--diff-filter=d`. End-to-end against the real tree: one locale removed → exit 1 with the orphan message; both removed → exit 0. Negative check: treating half-deleted as fine reddens 5 cases.

## #456 — the three code findings were already closed; the operator gap was not

Source inspection at `abb95944` confirms all three 2026-09-07 findings were fixed before this round: the sweeper quarantines into `failed` (not `pending`), the three internally-built services and `runFlow` route through `trackTouch`/`markTouched`, and both the sweeper and the reopen route commit their transition and audit inside one transaction.

What remained open was §2d, and it was a real gap: `GET /agent/approvals` returns every status, but the Studio rendered `failed` as a bare status string with no way to act on it — the quarantine preserved the "stuck and invisible" condition it was meant to end. Added the recovery surface (interruption callout, the recorded reason, Reopen with a required reason on those rows only).

Verified by running: 4 new tests, full Studio suite 68 files / 383 passed, `turbo run typecheck --force` 18/18. Negative check: removing the empty-reason guard reddens the refusal case.

Also recorded, not silently deferred: §2 registry row 123 is `n/a` **with an upgrade note** rather than a bare `n/a` — no migration is needed, but `deciding`/`failed` are new durable states and an interrupted approval now waits for a human instead of retrying silently. That was the review's point that "no migration" is not "no persisted-state impact".

## #457 — pushed, acceptance still pending on the same evidence

The five commits close deployment-integrations 6.2, 9.3 and 12.3. Verified by running: 130 passed across the deployment, serve-studio and admin-path-guard suites; `tsc --noEmit` exit 0.

The 2026-09-07 acceptance condition is **unchanged and still owed**: clean-image browser evidence for the Docker/Studio path. Not produced in this round. The DB-integration idempotency suite's "5/5 on Postgres 16" also remains implementer evidence — no Postgres was running here, and the suite skips cleanly without one, which was confirmed.

## Findings raised in this round

| ID | What | Status |
|---|---|---|
| B56 | Harness redaction true only for `.message`, not the rendered chain | fixed |
| B57 | #456's `g1-db-integration.ts` will trip #462's source scan on merge — no textual conflict, so a merge reports nothing | open |
| B58 | Pre-existing EN/VI drift on `contributing/testing.md` | open |
| B59 | The parity gate's own `--diff-filter=d` bypass | fixed |
| B60 | `features/agent-harness-layer.md` VI side is not a translation but a different document, while its front matter claims `human-translated` | open |

B57 is the one to act on at merge time: it is a cross-PR interaction that neither branch's CI can see.

Registry numbering was reconciled rather than guessed: #457 holds 118–120, so #462 took 121, #458 122 and #456 123.

No merge, deployment or issue closure. Nothing was force-pushed; every branch was pushed as a fast-forward on its own head.
