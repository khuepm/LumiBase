# 🔎 Reviewer — PR #457: acceptance pending

Inspected head: `7bb1e1403cde19f22c55181c6679b6614a34d215`. Related issues: #450, #451, #333, #332 and #448.

## Progress

The previous missing-CI finding is resolved: this head has successful quality/build, DB, workflow lint, Worker bundle and generated-starter install/typecheck checks. The new change also builds Studio into the Docker image and mounts its SPA from the Node server, retaining a separate Workers/Pages deployment model.

Source inspection confirms reserved API prefixes, GET/HEAD-only fallback, missing-asset handling, HTML revalidation and a Studio opt-out. The implementation includes middleware tests and EN/VI deployment documentation. No new confirmed code blocker is asserted from this inspection.

Independent check: `env -u DATABASE_URL pnpm -F @lumibase/cms exec vitest run src/__tests__/serve-studio.test.ts src/middleware/__tests__/admin-path-guard.test.ts` completed with 2 files / 76 passed, exit 0. Local Node 24.14.0 triggers the repository engine warning; this is supplemental evidence, not supported-runtime image acceptance.

## Remaining acceptance

The scope now includes production Docker/Studio behavior, beyond the initial template fix. Demonstrate the actual built image on a disposable fresh database: setup page/assets, setup completion, configured admin path, login, API JSON errors and the Studio-disabled mode. A middleware fixture and a successful Node build alone do not prove the image/real-app middleware path. Keep #332's complete website/seed/publish journey separate.

DoD: §1 CI pass, independent clean-image/browser evidence pending; §2 setup/upgrade/env impact must reflect the bundled Studio and opt-out; §2b no new tenant data model, verify setup context through the full chain; §2c/§2d actual admin guard/auth/base URL/assets/CORS and shell compatibility need full-path evidence; §2e generated installs pass CI; §3 final deployment/setup specs must match; §4/§4a EN/VI changes supplied, final provenance/claims need integrated verification; §5 verify changed onboarding/tutorial contracts; §6 middleware regressions supplied, image regression evidence pending; §7 reconcile registry/backlog numbering with main/#456.

Do not close all of #451: desktop/mobile items remain separate. Do not claim new Docker behavior is in a published release before shipment. No merge or deployment by reviewer.
