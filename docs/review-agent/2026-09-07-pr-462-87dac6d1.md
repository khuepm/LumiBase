## 🔎 Reviewer — Verifier review — changes required

Head `87dac6d15bfe9aac8495ddf8cd533fc462ed702a`, Refs #427.

The absent-DB=skip / requested-but-unreachable=fail convention is correct. Existing CI checks pass. I reviewed the helper and tripwire; I did not independently rerun the submitted PostgreSQL matrix in this pass.

### P1: redacted diagnostics retain raw secrets

`DbIntegrationUnreachableError` passes the original driver error as native `{ cause }`. Error reporters can print that original cause even though the parent message is redacted. Separately, `redactSecretsInMessage` only replaces URL userinfo and leaves query credentials untouched. Independent synthetic reproduction using the function extracted from this head: `driver failed postgresql://h/db?password=FAKE_REVIEW_SECRET` is returned unchanged. Native Error inspection also prints a raw cause containing the synthetic password.

Sanitize or omit the raw cause, and sanitize query credentials in embedded DSNs. Test the rendered full error chain with synthetic credentials, not just `error.message`. No real credentials were used in this reproduction.

### Integration dependency with #456

#456 now adds `g1-approval-concurrency.db.integration.test.ts` using the prohibited early-return pattern. Your claim of integration in any order is no longer true: the tripwire must remain strict and G1 must migrate to the helper. Verify the combined tree; do not weaken the scan.

### DoD disposition

§1 PARTIAL: CI green, submitted real-DB matrix is implementer evidence; redaction fails review. §2 BLOCKED: add the justified n/a setup registry entry. §2b/§2c/§2d n/a for production behavior: test harness only, no production query/route/shell changes. §2e n/a: no dependency delta. §3 document final testing convention where applicable. §4/§4a: CHANGELOG and contributor-guide impact disposition pending; any docs changes need EN/VI parity. §5 n/a for user API tutorials; contributor testing guidance does change. §6 PARTIAL: good shared helper/tripwire, add full error serialization regression and integrate G1. §7 record confirmed out-of-scope findings under a unique registry ID, or explicit n/a with rationale.

Keep #427 open pending fixes and integrated verification. No implementation edits or merge by verifier.
