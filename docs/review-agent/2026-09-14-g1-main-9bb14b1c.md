# 🔎 Reviewer — G1 independent acceptance on main (C-03)

- **Canonical issue:** [#453](https://github.com/khuepm/LumiBase/issues/453) — `fix(agent): G1 approval decision must execute or resume the governed action` (parent #331)
- **Implementation under review:** PR [#456](https://github.com/khuepm/LumiBase/pull/456), merged at `497897d64689f1481115ff9e5794c7747e9f1784`
- **Inspected head (pinned):** `9bb14b1cd24e6542a492c65ccb21278694ea6e73` — `feat(cms): resolve governed principal capabilities`
- **Main tip at review time:** `6717182bdaab400e11407b3a47bdf02a8c35dc3e` — delta `9bb14b1c..6717182b` touches only `docs/` + `examples/` (PR #474 + i18n stamps); **zero** changes under `apps/cms`, `packages/database`, `packages/contracts`, `packages/ai-skills`, so the evidence below applies to the current tip.
- **Handoff ref (C-03):** baseline `d280771a…`; delta `d280771a..9bb14b1c` additionally landed PR #473 (tenant-exists FK-crash fix) and the dormant `effective-capability-service` (no production callers — verified by grep).
- **Verifier:** Devin1 (Devin CLI), lane C — independent of lane B implementer. Session ACK: [#453 comment](https://github.com/khuepm/LumiBase/issues/453#issuecomment-5670022193).

## Verdict

**G1 ACCEPTED** — all nine acceptance criteria are evidenced on a dedicated disposable PostgreSQL 16 (real `*.db.integration` runs, zero silent skips), real-HTTP e2e, and source review. Two non-blocking test-hardening proposals and two documentation/registry follow-ups are listed under *Follow-ups*. Merge of #456 does not by itself close #453; this record is the independent acceptance the gate was waiting for. #472 (G2-CAP) and the B02 mapping/result/pack/real-MCP gates remain open and are NOT covered by this verdict.

## Environment

- Worktree: `/Users/khuepm/workplace/AI/Lumibase-c03` on branch `feature/453-c03-verification`, clean at pinned SHA.
- Disposable infra (created for this review, safe to destroy): container `lumibase-g1-verify` = `postgres:16` on host port 55440, fresh DB `lumibase_g1_verify`, migrated via `pnpm db:migrate` (exit 0); container `lumibase-g1-verify-redis` = `redis:7` on host port 56380 (required by golden-path e2e — login/audit enqueue work). No starter/B02 database was touched.
- Toolchain: pnpm workspace install at pinned SHA; vitest 5.0.0; Node per repo engines.

## Reviewer-run evidence (commands + exit codes)

All commands from `apps/cms` unless noted. `DATABASE_URL=postgresql://lumibase:***@localhost:55440/lumibase_g1_verify` (credentials redacted).

| # | Command | Result |
|---|---------|--------|
| 1 | `vitest run` 7 G1 files (`routes/__tests__/g1-approval-execution`, `services/__tests__/g1-{approval-concurrency,claim-sweeper,failed-outcome}.db.integration`, `g1-{missing-service,reviewer-execution,service-touch}`) with DATABASE_URL | **59 passed / 0 skipped**, exit 0 — incl. all 22 real-PG integration tests |
| 2 | Same 3 `*.db.integration` files with `DATABASE_URL` unset | **22 skipped** (not passed), exit 0 — #427 skip-gate correct |
| 3 | `g1-approval-concurrency.db.integration` with DATABASE_URL pointing at a dead port | suite **fails** loudly, exit 1 — fail-fast preflight confirmed, no silent pass |
| 4 | `vitest run` 22 related files (ai-harness-*/agent-*/reviewer/veto/kill-switch/effective-capability + ai-approvals route tests) with DATABASE_URL | **98 passed**, exit 0 — incl. `effective-capability-service.db.integration` (4, real PG) |
| 5 | `vitest run` guard/delta files (`security-guards.wiring`, `db-integration-guard.wiring`, `tenant-exists`, `worker-fk-crash`) | **186 passed**, exit 0 |
| 6 | `vitest run` `ai-integration`, `ai-harness-keys-context`, `agent-org.property`, `veto-list-pending.db.integration`, `cdc-feed-skills-hitl` | **39 passed**, exit 0 |
| 7 | `vitest run` `golden-path.e2e` + `upgrade-path.e2e` with `LUMIBASE_RUNTIME=docker LUMIBASE_ENV=development JWT_SECRET=*** REDIS_URL=redis://localhost:56380` | **2 passed**, exit 0 — real HTTP through the full middleware stack, incl. setup wizard + two-site isolation |
| 8 | `pnpm -F @lumibase/cms typecheck` (repo root) | exit 0, clean |
| 9 | Studio: `vitest run agent-harness-approvals-recovery.test.tsx` | **4 passed**, exit 0 |
| 10 | `pnpm docs:i18n:detect` (repo root) | 148 files, all up-to-date; artifacts reverted after run |

First attempt at #6's golden-path failed on missing env (`JWT_SECRET`/`REDIS_URL` absent → login 500s/timeouts) — environment issue, resolved by supplying the CI-documented env; recorded for honesty, not a code failure.

## CI evidence (integrated main, head `9bb14b1c`)

`gh run list --branch main`: **CI** success, Docker success, Docker Publish success, Performance (k6) success, Push on main success, Scorecard success, Docs i18n Sync success. **Deploy CMS: failure** — post-deploy staging health check returns HTTP 500; same failure on prior heads `e2bd0f49`, `59821274`. Environmental/pre-existing staging outage (same symptom class as the API outage recorded 2026-07-28 in `memory.md`), not attributable to the G1 diff — flagged for the coordinator, not counted against G1.

## Implementer-reported evidence (from #456 reviews, not re-run by me)

Reviewer-authored final fix commit `2935ac27` reported: 3 G1 DB suites on disposable PG16 = 22 passed/0 skipped; no-DB run 20 passed/22 skipped; #462 source-guard 139 passed; commit hook CMS 2530 passed/29 skipped, Studio 383 passed. Consistent with my independent run (rows 1–2 above reproduce the same counts on a different machine and DB).

## Acceptance criteria — per-AC evidence table

| AC | Verdict | Evidence |
|----|---------|----------|
| 1. Dangerous call parks w/o content/schema side effect; approval ID resolves to decision endpoint + pending run | **PASS** | `g2-mcp-contract-repro` GP4 (governed path): `deleteItem` → `pending_approval`, item-service spy = 0 calls, exactly 1 `ai_approvals` + 1 `agent_approvals` insert. `ai-integration`: legacy park leaves `schemaService.calls` empty until approve. `agent-run-state-machine`: `awaitApproval` parks run at `awaiting_approval`, `markRunning` resumes without replaying completed calls. Route tests drive `POST /agent/approvals/:id/decide` with the stored `agentApprovalId` → 200 executes. Park code path: `ai-harness.ts:2404–2455`. |
| 2. Authorized human approval executes/resumes stored action w/ real deps, correct site + caller context; completed calls not replayed | **PASS** | `g1-approval-execution` "runs the stored action exactly once": real route → real harness → real `SchemaService` spy called once with stored args, both records `approved`. `buildAuthorizedHarness` (`ai.ts:65`) wires schema/item/access/intent/config/extensions/llm/queue/keys — the missing-dependency gap from the baseline is closed. `g1-approval-concurrency` "does not replay": second `executeApproved` denied, spy still 1 call. `ai-integration` asserts `decidedBy`/`decidedAt`. `decide` route resolves `agentApprovalId → legacyApprovalId` site-scoped. |
| 3. Rejection, expiry, cancellation, kill switch, wrong tenant, insufficient permissions never execute | **PASS with 2 noted boundary-test gaps** | Evidenced: reject (route 200, spy 0 calls; `rejectApproval` claims `agent_approvals` first); expiry (route 409 + property test `expired` deny); wrong tenant (route 404 before capability check — no id-probing); insufficient permission (route 403 `approvals:decide` + harness `checkCapabilities` deny + property test). Kill switch: `runClaimedApproval` checks `frozenScopeFor` post-claim → release + denied (`ai-harness.ts:2773–2782`); mechanism property-tested (`kill-switch.property`, 5 tests). Cancellation: `isCancelled` → release + denied (`ai-harness.ts:2762`); `isCancelled` unit-tested; throwing-isCancelled fault covered by sweeper suite. **Gaps G-A/G-B**: no test drives "frozen"/"cancelled-run + approve → denied + zero side effects + claim released" at the boundary — see Follow-ups. |
| 4. Duplicate/concurrent approve, approve-vs-reject, retry: no duplicated side effects or overwritten decision (count side effects) | **PASS** | Real PG: 5 concurrent `executeApproved` → exactly 1 `executed`, spy 1 call; approve+reject race → ≤1 mutation, both records agree; reject-wins keeps both records `rejected`, late approve denied, spy 0 calls. Fake-DB route suite mirrors: concurrent approves → 1 mutation, {200,409}; approve+reject → ≤1 mutation, statuses agree. `updateGuards` in the fake mirror the real CAS so the assertions are meaningful. |
| 5. Missing required service/provider fails explicitly; no success-shaped stub | **PASS** | `g1-missing-service` (10 tests): every skill rejects explicitly when its service is unconfigured; read skills may answer offline but never claim a write. `NO_STORED_ACTION` 422 when an approval has no linked stored action. `g1-service-touch` (5 tests): injected + internally-built services (deployments, content-versions) and direct-db handlers all tracked. |
| 6. Execution failure observable + explicit retry; approval/run/tool-call/audit/provenance agree | **PASS** | `g1-failed-outcome` (7, real PG): skill fails after touching service → `failed`, not retryable; fails before → `pending`; `failed` not decidable; sweeper never revives; mutation→crash→sweep→decide quarantined; persistence failure post-mutation quarantined; reopened approval executes again exactly once. `g1-claim-sweeper` (10, real PG): stale `deciding` → `failed` + `approval.claim_quarantined` audit in one transaction (audit-failure rolls back); fresh claims and decided rows untouched; concurrent sweeps → one release, one audit row. Route: failing skill reported as 409 `EXECUTION_FAILED` with reason, never success-shaped; no `deciding` rest state. Sweeper wired in both runtimes (`serve.ts:379`, `cloudflare.ts:103`). |
| 7. Agent-reviewer decisions audited for the same gap; confidence/escalation/veto preserved | **PASS** | `g1-reviewer-execution` (7): confident approve executes stored action via the same fully-wired harness before finalizing; annotation guarded on `approved` so a racing human reject is not clobbered (escalates instead); execution failure / missing executor → escalate, stays pending; reject / low-confidence / veto-kind never execute. `agent-reviewer.property` (5): self-review forbidden via goal-tree ancestry, `review:<domain>` mapping. |
| 8. Legacy + Content OS endpoints compatible; no feature-flag/admin-backstop/token-RBAC weakening | **PASS** | `/ai/approvals/:id/decide` (legacy) and `/agent/approvals/:id/decide` + `/agent-decide` all serialize on the same conditional `agent_approvals` claim — single owner of the transition. Legacy path still executes (route-level pre-checks + `buildAuthorizedHarness`). No new flags/backstops/RBAC changes in #456's diff (file list reviewed). Post-merge `effective-capability-service` is dormant (no callers) — no weakening. |
| 9. Independent verifier reproduces positive + negative paths on dedicated test DB; integrated-main CI + DoD before closure | **PASS (this record)** | Rows 1–10 above: disposable PG16 + Redis, never the shared DB; skip/fail-fast gates demonstrated; CI green on pinned head (Deploy CMS env failure noted). DoD review below. |

## Definition of Done — checked against pinned head

| § | Item | Result | Evidence class |
|---|------|--------|----------------|
| 1 | `pnpm typecheck` workspace | PASS | reviewer-run (cms; exit 0). Full-workspace typecheck: CI-reported green |
| 1 | `pnpm test` | PASS (scoped) | reviewer-run: 388 tests across 38 files, 0 fail; full suite: CI green on `9bb14b1c` |
| 1 | Non-negotiables (nanoid/uuidv7, site_id, runtime abstraction, HITL, response format) | PASS | source review: `agent_approvals.id` = `id()` (nanoid), every query site-scoped, harness takes injected deps (no CF imports), decide/reopen return `{data}`/`{errors}` |
| 2 | Setup-impact registry | **GAP (n/a row missing)** | reviewer-run: no row records this change; per DoD an `n/a` dated row is still required. Reserved file → exact-file proposal G-D |
| 2b | Multi-tenant | PASS | reviewer-run: wrong-tenant route test (404, 0 side effects); all claim/release/reject updates carry `siteId`; sweeper is intentionally cross-site but guards each row by its own `siteId`; golden-path e2e two-site isolation passed |
| 2b | Worker/request-path provider parity | PASS | reviewer-run: `ai-harness-keys-context.test.ts` tripwire passed; `buildAuthorizedHarness` supplies `keys`; `agent-run-worker-keys.test.ts` passed |
| 2c | Route-guard security | PASS | reviewer-run: `/api/v1/agent` in `CONTROL_PLANE_PATHS` (`control-plane-access-guard.ts:9`); `security-guards.wiring` 25/25; new routes reuse existing prefix + `approvals:decide` capability; 404-before-403 ordering prevents cross-tenant id probing |
| 2d | Shell impact | PASS + observation | reviewer-run: #456's Studio change reuses the pre-existing `agentRequest` helper (`getActiveToken`/`getActiveSite` via `@/lib/api` + `X-Lumi-Client: studio`); no auth-token/base-URL/build changes. Observation (pre-existing, not introduced by #456): `agentRequest` fetches same-origin `/api/v1/agent…` rather than via `getApiBaseUrl()` — logged as out-of-scope note below |
| 2e | Dependency hygiene | n/a | no manifest/lockfile changes in the verified diff |
| 3 | Spec hygiene | n/a | issue-driven fix under #453/#331; no `.kiro/specs/` task file owns G1 — content-os spec already tracks the feature; see G-D |
| 4 | API spec updated | **GAP** | `hono-api-spec.md` (EN+VI) lacks rows for `POST /api/v1/agent/approvals/:id/decide` (semantics changed: now executes) and `POST /api/v1/agent/approvals/:id/reopen` (new). Feature doc `agent-harness-layer.md` covers reopen in both locales. Exact-file proposal G-C |
| 4 | CHANGELOG | PASS | #456 diff includes `CHANGELOG.md` entry |
| 4a | Docs EN/VI parity | PASS | reviewer-run: `docs:i18n:detect` — 148 files all up-to-date; both touched pairs (`agent-harness-layer.md`, `contributing/docs-i18n.md`) exist EN+VI and are stamped |
| 5 | Tutorial impact | n/a | approval decision endpoints are not a tutorial-pinned contract |
| 6 | DoD evolution | PASS | the fix class (claim/serialization + silent-skip DB suites) is already locked by `db-integration-guard.wiring` (151 tests) + `security-guards.wiring` — no new DoD row needed |
| 7 | Out-of-scope findings | recorded | Studio `agentRequest` same-origin `/api/v1` fetch (pre-existing pattern — shell C2 question); `Deploy CMS` staging health-check failing on main (environmental). Both noted for `.kiro/steering/out-of-scope-backlog.md` (reserved file — coordinator to log) |

## Follow-ups (exact-file proposals — not fixed by the verifier, per C-03 scope)

- **G-A (test, P2):** add a case asserting "frozen site/role → `POST /agent/approvals/:id/decide` approved → 409/denied, 0 service calls, claim released". Suggested file: `apps/cms/src/routes/__tests__/g1-approval-execution.test.ts` (seed an `agentFreezes` row — the table is already stubbed) or a new `g1-freeze-approve.db.integration.test.ts` if real-PG evidence is preferred.
- **G-B (test, P2):** add a case asserting "cancelled run → approve → denied, 0 service calls, approval back to `pending`". Same suggested files as G-A (seed `agentRuns.status='cancelled'`).
- **G-C (docs, P3):** add spec-table rows for `POST /api/v1/agent/approvals/:id/decide` and `POST /api/v1/agent/approvals/:id/reopen` in `docs/en/api/hono-api-spec.md` + `docs/vi/api/hono-api-spec.md` (bilingual pair, same PR).
- **G-D (registry, P3):** add an `n/a` setup-impact row for this change in `.kiro/specs/admin-setup-wizard/setup-impact.md` (reserved file — needs coordinator/implementer grant).

## Limitations / unverified

- The `deciding`-claim window under *process crash* is verified only through the sweeper's real-PG tests (quarantine path); no actual process-kill was performed.
- Concurrency evidence is `Promise.all` interleavings + real-PG CAS on one node — not multi-instance/network-partition testing.
- `effective-capability-service` (post-merge delta) has no production callers; reviewed for harmlessness only, not adopted-path behavior.
- Studio recovery UI verified by its 4 unit tests only — no browser check.
- Reviewer did not run the full `pnpm -F @lumibase/cms test` suite locally; full-suite green is CI-reported on the pinned head.
