# 🔎 Reviewer — Sửa CI PR #462

PR: https://github.com/khuepm/LumiBase/pull/462 · Issue #427.
Head trước sửa: b8a122dbbfd1df2f95b000d7105bfd3e79f55088.

Ba job đỏ, hai nguyên nhân.

## 1. Tripwire `db-integration-guard` đỏ trên một suite mới của `main`

Job `Version, typecheck, tests, and build` (run 34522705127) và `DB integration suites`
cùng đỏ vì **một** file: `services/deployment/__tests__/status-poller-idempotency.db.integration.test.ts`.
Suite này vào `main` qua PR khác (deployment-integrations task 9.3) **sau** khi #462
chuyển 19 suite còn lại sang harness, nên lần merge `main` vào nhánh mang về đúng một
suite còn dùng mẫu cũ — `canConnect` + `if (!canConnect) return;` + `console.warn('Skipping…')`.
Tripwire quét toàn bộ `*.db.integration.test.ts` nên bắt được ngay: 6/6 assertion của
file đó đỏ, không có test nào khác fail (`1 failed | 40 passed` ở job DB integration).

Đây chính là hành vi tripwire được thiết kế để có — nó bắt suite **mới** quên harness,
không chỉ suite cũ. Fix là chuyển file sang harness chung, không nới assertion:
`describe.skipIf(!hasDbIntegrationUrl)`, `connectDbIntegration('deployment-status-poller')`,
bỏ `canConnect`/`TEST_DATABASE_URL`/`createDb`, chỉ giữ `if (!db) return;` trong `afterAll`
(ngoại lệ duy nhất tripwire cho phép, vì `beforeAll` có thể throw trước khi gán `db`).

Kiểm chứng: chạy `db-integration-guard.wiring.test.ts` + `db-harness.test.ts` +
suite vừa sửa không có `DATABASE_URL` → `2 passed | 1 skipped (3)`, `162 passed | 5 skipped`.
Suite DB báo **skipped**, không phải passed — đúng mục tiêu #427. `tsc --noEmit` của
`@lumibase/cms` sạch.

## 2. Docs i18n parity — `contributing/testing.md`

Job Docs i18n Sync (run 34522705093) thất bại ở bước Enforce parity:
`contributing/testing.md` có 17/14 heading, 12/11 code fence và thiếu link k6 ở VI.
Bản VI thiếu toàn bộ phần k6; đã dịch bổ sung nội dung từ EN, giữ nguyên khối lệnh,
và stamp lại cả hai locale. Không thêm waiver.

Kiểm chứng trực tiếp: gate changed-pair đạt 1/1 cặp, 0 problems; stamp --verified
kiểm 10 code references; `docs:i18n:detect` cho 148/148 up-to-date. Kết luận này chỉ
xử lý lỗi CI, không thay thế review toàn bộ DB harness.
