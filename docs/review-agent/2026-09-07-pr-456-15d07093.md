## 🔎 Reviewer — Verifier re-review — changes required

Head: `15d07093258905f8ac54dd63578e07038b277741`, issue #453. This replaces stale statements that the current human approve route necessarily returns 409. The claim now belongs to the harness; the normal roundtrip tests pass. The two original findings have received substantive fixes, but this is not acceptance.

### P1: failure handling still permits unsafe retry / stranded claims

In `executeApprovedWithAudit`, `isCancelled`, `markRunning`, `frozenScopeFor`, and `appendToolCall` execute after claiming `deciding` and outside the try/catch. Any exception there leaves a durable claim stranded, including ordinary DB/provider errors before the skill runs. The catch covers only `runSkill`, not the entire claimed lifetime. Mission Control still filters pending approvals, so these records disappear from the inbox. A process crash has the same unresolved operator-recovery problem.

Conversely, a skill that mutates then throws/fails is reset to pending without distinguishing an ambiguous side effect. Retrying can repeat that mutation. Define a durable failed/unknown outcome and safe recovery/idempotency policy; do not blanket-reset uncertain execution to pending. Add fault injection before execution and after a mutation, with actual side-effect assertions. This is source review; these fault cases were not independently run against PostgreSQL in this pass.

### P1: the new DB suite reintroduces #427 and conflicts with #462

`g1-approval-concurrency.db.integration.test.ts` uses `TEST_DATABASE_URL`, `canConnect`, direct `createDb`, warn-and-return hooks and early-return tests. Independent run with DATABASE_URL unset: four focused files / **32 passed**, exit 0, including five DB tests that did not execute. The other 27 are route/service tests with fake DB/leaf service boundaries. Do not present the 32 as DB evidence.

#462 scans every `.db.integration.test.ts` and rejects this exact pattern. Adapt this new suite to its shared helper and verify the combined tree. No textual overlap does not mean independent merge order. Report real DB evidence separately.

### DoD disposition

- §1: scoped tests pass; error/retry correctness remains FAIL. CI checks are green at this head, not proof of fault recovery.
- §2: BLOCKED: G1 setup-impact/recovery/upgrade record remains missing; deciding is durable state even without a migration.
- §2b: PARTIAL: tenant-scoped claims reviewed; no independent two-site SQL/runtime acceptance in this pass.
- §2c: PARTIAL: full-chain guards must be demonstrated; PR's claim that any ordinary tenant member could decide ignores the existing control-plane backstop.
- §2d: BLOCKED: pending-only Studio inbox plus deciding recovery needs evidence. Other token/base/CORS contracts are unchanged.
- §2e: no G1 dependency delta; shared carried commits require serialized integration with #457.
- §3/§4/§4a/§5: BLOCKED: final approval/API/retry contract, EN/VI docs, CHANGELOG and tutorial-impact disposition remain necessary.
- §6: FAIL: fault-recovery regression and #462 integration required.
- §7: record valid out-of-scope findings; core recovery cannot simply be deferred.

Keep #453 open. Update the stale PR description (old route-owned claim, old test counts, no-PR note, blanket SQL recovery, `Fixes #453`). Use `Refs #453` while post-merge acceptance remains. No merge or deployment performed by verifier.
