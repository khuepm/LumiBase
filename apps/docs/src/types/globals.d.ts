/**
 * Build-time constants injected via Vite `define`.
 *
 * `__APP_VERSION__` is the docs package version, which `pnpm version:sync`
 * keeps aligned with the root LumiBase release version.
 */
declare const __APP_VERSION__: string;
