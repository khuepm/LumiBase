{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capability: signed auto-update and the confirm-restart dialog. These plugins are not built on mobile, where the app stores handle updates.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["dialog:default", "updater:default"]
}
