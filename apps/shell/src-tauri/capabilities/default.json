{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window: core webview + auto-update + relaunch.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "process:default",
    "process:allow-restart",
    "dialog:default",
    "updater:default"
  ]
}
